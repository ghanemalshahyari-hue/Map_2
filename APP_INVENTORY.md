<!-- AUDIT_SHA: b37f5a838c03ac766cd181f0ce61197418d1e4c5 -->
# APP_INVENTORY — RMOOZ / CMO feature map

> **The single map of what this app has, what it doesn't, and what's drifting.**
> Read this before building anything. If you'd add a module/card/endpoint, find it here first.
> Refresh with `/audit-app` (see `.claude/skills/audit-app/`). The freshness check at session
> start reads the `AUDIT_SHA` marker on line 1 to tell you how stale this is.

## Current audit checkpoint - RMOOZ-AUDIT-APP-2

**Last audited:** `b37f5a83` · 2026-07-02 · by /audit-app (**RMOOZ-AUDIT-APP-2 — scoped refresh after Scenario Evidence v9 + Node 22 test fix**). Scope results: **Batch 9 (QA-83..91) documented** — §Scenario Evidence QA stack rows v1..v9 + tag `scenario-evidence-v9`; gate 89/89. **Scenario Evidence drawer order documented** (17-panel order in `docs/cmo-evidence-demo-handoff.md` + runbook) and **live-verified in-browser**: 17/17 scenario-level blocks re-parent into `#scenario-evidence-body` in exactly the documented DOM order; selected-unit evidence (contact/engagement/decision-chain/timeline/snapshot) stays in Unit Status. **Node 22 navigator stub standardized** — zero `global.navigator =` assignments remain repo-wide; 8 gate tests migrated to `Object.defineProperty(global,'navigator',{...,configurable:true})` (commits `389a38e5` + `b37f5a83`). **Main/offline shell parity:** 31/32 shared modules byte-identical; the exception is `scenario-import-wizard.js` → **NEW Drift D6** (offline copy ~407 lines behind main; offline app.html loads it at :4848). **No DOCX staging** (`stage-doc`/`SLOT_FILE`) in active client/server/offline code — archived doc/test mentions only. **⚠️ Runtime-file finding:** `UI_MOdified/TestingAI/WarGamingGEN/inputs/scenario_overrides.json` is **tracked in git** (not ignored) yet is app-written runtime state — it stays perpetually dirty and collided with upstream during the 2026-07-02 fast-forward; **owner ruling needed** (gitignore + `git rm --cached` vs keep as committed seed and stop runtime writes). **Smoke test PASS:** real server `:8000` — `/api/ai/scenarios` · `/api/wargame-sim/status` · `/api/ai/models` all 200; verify stub `:8001` — `app.html` 200 (525 KB) + `auth/me` 200. Verification stack green: inventory-audit 8/8 · handoff-acceptance 89/89 · handoff-package 47/47 · ux-polish 9/9 · offline-operational-scenario 0 failed · `git diff --check` clean. **Drift D1–D5 remain RESOLVED; D6 NEW/OPEN.**

**Prior:** `03c9113e` · 2026-07-02 · by RMOOZ-AUDIT-APP-1. That refresh covered the post-CMO evidence/actionability and Scenario-QA surface area, with the active offline runtime treated as the deployment baseline.

Current top baseline:

```text
03c9113e feat(scenario): add evidence repair planner
87fd06bf fix(cmo): stop unit status panel above the footer bars
bb718a1a style(cmo): extend unit status panel to cover the footer band
368a0c3e feat(scenario): add evidence review queue
05e3fa58 style(cmo): raise unit status evidence panel readability
64178d84 feat(scenario): add evidence completeness validation pack
6f310c00 feat(cmo): add evidence actionability commander pack
f7e9a88f feat(cmo): add evidence recommendations
```

### CMO evidence and actionability stack

The active CMO evidence workflow is read-only UI evidence, aggregation, export, report, print, and operator recommendation guidance. It does not add combat math, backend evidence routes, doctrine mutation, auto-fire, or AI decision overrides.

```text
CMO-1   Engagement Evidence
CMO-2   Contact Detection Evidence
CMO-3   Sensor-to-Shooter Decision Chain
CMO-4   Map Evidence Overlays
CMO-5   Selected-Unit Evidence Timeline
CMO-6   Selected-Unit Evidence Export
CMO-7   Force Readiness Matrix
CMO-8   Matrix Filters + Drilldown
CMO-9   Evidence Alert Badges
CMO-10  Force Evidence Event Feed
CMO-11  Force Evidence Report
CMO-12  Evidence Quality Gate
CMO-13  Quality Gate Drilldown
CMO-14  Evidence UX Polish
CMO-15  Printable Evidence Report Layout
CMO-16  Evidence Recommendations
CMO Actionability Batch  cmo-blocker-remediation + cmo-alternative-shooters + cmo-commander-brief + cmo-evidence-coverage
```

Active CMO/evidence scripts mirrored between `UI_MOdified/client/app.html` and `UI_MOdified/Offline_Deployment/offline_app/client/app.html`:

```text
cmo-evidence-labels.js
cmo-evidence-timeline.js
contact-evidence.js
engagement-evidence.js
decision-chain-evidence.js
cmo-evidence-recommendations.js
cmo-recommendation-drilldown.js
cmo-evidence-readiness-matrix.js
cmo-evidence-alerts.js
cmo-evidence-quality-gate.js
cmo-evidence-coverage.js
cmo-blocker-remediation.js
cmo-alternative-shooters.js
cmo-commander-brief.js
cmo-force-evidence-feed.js
cmo-force-evidence-report.js
evidence-map-overlays.js
cmo-evidence-export.js
unit-status-panel.js
```

### Scenario Evidence QA stack

Scenario-evidence v1..v9 is active as a browser-local quality/workflow layer over the same selected-unit and force evidence path (this section was refreshed 2026-07-02 with Batch 9; v4–v8 rows had been missing — recorded now, statuses verified against the tags):

```text
scenario-evidence v1  Scenario Evidence Completeness validation pack
scenario-evidence v2  Scenario Evidence Review Queue
scenario-evidence v3  Scenario Evidence Repair Planner
scenario-evidence v4  Manual Evidence Fix workflow (browser-local statuses)
scenario-evidence v5  Persistent evidence review sessions (per scenario fingerprint)
scenario-evidence v6  Evidence Review Closeout gate
scenario-evidence v7  Evidence Review Audit Trail (browser-local history)
scenario-evidence v8  Evidence Handoff Package (export/import, fingerprint validation)
cmo-evidence-rehome-v1  Scenario Evidence drawer split — Unit Status = selected-unit only;
                        scenario-level panels rehomed to the Scenario Evidence drawer
scenario-evidence v9  Handoff Acceptance (QA-83..91): package diff vs local session,
                      Accept / Reject / Accept with Warnings, acceptance receipt export,
                      audit-trail + commander-brief + force-report integration,
                      runbook/handoff docs updated for the Scenario Evidence drawer
```

Active Scenario-QA scripts mirrored between main and offline app shells:

```text
scenario-evidence-completeness.js
scenario-evidence-normalizer.js
objective-x-evidence-health.js
scenario-evidence-review-queue.js
scenario-evidence-repair-planner.js
scenario-evidence-fix-status.js
scenario-evidence-manual-fix.js
scenario-evidence-review-session.js
scenario-evidence-review-closeout.js
scenario-evidence-review-audit-trail.js
scenario-evidence-handoff-package.js
scenario-evidence-handoff-acceptance.js
```

Gate tests (root, run individually): `test-scenario-evidence-{completeness,review-queue,repair-plan,manual-fix,review-session,review-closeout,review-audit-trail,handoff-package,handoff-acceptance}-batch-1.js`.
Boundary note: the whole stack is browser-local (localStorage + in-memory fallback) — no backend routes, no scenario/world-state mutation; package/acceptance import restores review-session UI state only.

### Runtime and release baselines

Canonical offline runtime command:

```bash
docker compose --env-file UI_MOdified/Offline_Deployment/.env.offline -f UI_MOdified/Offline_Deployment/docker-compose.offline.yml up -d
```

Known-good demo URL for the LAN runtime:

```text
http://172.16.29.157:8640
```

Remote/public access remains a network/admin-side issue, not an RMOOZ app-code issue:

```text
localhost:8640 works
172.16.29.157:8640 works on LAN
155.140.70.51:8640 previously timed out
```

Current relevant tags:

```text
scenario-evidence-v9 -> evidence handoff acceptance workflow (Batch 9)
cmo-evidence-rehome-v1 -> 764d260f Scenario Evidence drawer split from Unit Status
scenario-evidence-v8 -> 941aa275 evidence handoff package
cmo-evidence-v13 -> 9dc07bdb printable evidence report layout
cmo-evidence-rc1 -> 4314f16c evidence release candidate gate
cmo-evidence-v14 -> 6f310c00 evidence actionability commander pack
cmo-evidence-readability-v1 -> 05e3fa58 evidence readability CSS
```

### Audit findings

- Main/offline static parity is intact for **all 32 shared `shell/*.js` modules** (byte-identical, enforced by the new parity gate in `test-app-inventory-audit-1.js`). D6 (stale `scenario-import-wizard.js`) is **RESOLVED**.
- Test-env convention (2026-07-02): browser-global stubs in root gate tests must use `Object.defineProperty(global, '<name>', { value: ..., configurable: true })` — plain `global.navigator = ...` throws on Node 22 (getter-only global).
- Script ordering keeps labels/timeline/contact/engagement/decision-chain before recommendations, matrix, quality, reports, overlays, export, and `unit-status-panel.js`.
- The old live DOCX staging route/UI is not present in active client/offline/server app code. Historical docs/tests may still mention `stage-doc` and Red/Blue DOCX as archived integration history.
- The offline runtime command must include `--env-file`; plain compose is not the release/demo command.
- Public `155.140.70.51:8640` reachability remains `NET-8640-REMOTE-1` and needs firewall/NAT/admin work outside the app.

**Last audited:** `3b192475` · 2026-06-16 · by /audit-app (**full sweep — 144 commits since `e42e2d5`**; `3b192475` landed from a concurrent agent mid-audit — gate-card diagnostics, folded in). The body was already kept current per-feature through 2026-06-16 — the entire **Free Fight AI Commander** subsystem (rows 98–113: FREEDOM-A/B, MCP tool-contract, repair loop, candidate pre-filter, single execution gate, LLM runtime-config resolver, local-only security lock, demo pacing) carries accurate rows; this pass **re-stamps the marker**, fills server-module census gaps, and runs a live smoke test. **Smoke test PASS** against the already-running dev server (:8000): `app.html`/`home.html` 200, `/api/ai/scenarios`·`/api/wargame-sim/status`·`/api/ai/models`·`/api/wargame-sim/free-fight/plan-coas/health` all 200, `auth/me` 401 (expected on the real server; verify stub returns 200). **Free Fight security posture intact** (`local_only:true`, `allow_sim_run:true`, `ai_execution_enabled:true`). **Runtime finding — NOT drift:** the operator-selected local model **`qwen3.6-plus-free` is not pulled in Ollama** → `model_available:false` → Free Fight correctly falls back to `deterministic_diverse_coa` (this is the source of the `coa=2 valid=0 [source=deterministic_diverse_coa]` console log) — exactly the no-silent-swap behaviour designed in **RMOOZ-LOCAL-MODEL-SELECTOR-A** (run `ollama pull <model>` or pick an installed model to exercise the LLM). ⚠️ **SECURITY (surfaced + resolved mid-audit; one OPEN follow-up):** `server/ai/ai-config.js` briefly held **foreign WIP** (a hardcoded Zen `sk-…` key in `zen.apiKey` + `aiProvider: ollama→opencode.ai` cloud switch + cloud default models); the concurrent commit `3b192475` excluded it and a concurrent agent **reverted it to HEAD during this audit** — working tree re-verified **CLEAN** (`git diff` empty, no `sk-` in the file, `aiProvider:'ollama'`, `defaultModel:'qwen2.5:7b'`). **STILL OPEN (owner): ROTATE the exposed Zen key** (treat as compromised — it was present in the shared tree). `[[feedback_free_fight_local_only_security]]`, `[[feedback_shared_working_tree_concurrency]]`. **This pass added:** §C census for the **RMOZ-INTEL-CAPABILITY-TERRAIN-ZONE-A** intel foundation + the Free Fight server modules (`free-fight-coa-planner` 1864 L, `rmooz-ai-tool-contract` 1233 L, analyst/decision/plan/action-engine/situation-triggers, `candidate-prefilter`, `tactical-action-library`/`tactical-terrain-context`, `commander-brief`/`coalition-posture-engine`, `scenario-intel`/`platform-capability-catalog`/`contact-detection-engine`/`sovereign-zone-engine`/`roe-escalation-engine`, `llm-runtime-config`) + AI-GLOBAL-REFACTOR helpers (`ai-json`/`ai-guardrails`); §A rows for the Free Fight **client** display modules (`free-fight-demo.js` ~3.4k L, `free-fight-ai.js`, `ai-model-hud.js`, `symbol-identity`/`unit-intel-normalizer`/`sidc-preview`/`demo-scenario-preview`/`domain-movement`); §E `/api/wargame-sim/free-fight/*` route group; §F refresh (≈81 test files; new free-fight/intel/identity/model suites live under `scripts/`). **Fixed stale Duplications #7** (the Middle East loader + `platforms.json` it cited were DELETED at D5, 2026-06-09 — confirmed gone) and added a geo-math copy item. **`wargame 6/` dead-dup dir still present** (#6, delete candidate). ⚠️ **Audit-method note:** judge wiring by the FULL require graph — many `server/ai/*` modules are wired via `wargame-sim-bridge.js`/`terrain-api.js`/`scenario-intel.js`/`scenario-validator.js`, NOT `web-server.js` (a sweep grepping only `web-server.js` false-flags them dead). **Drift D1–D5 all remain RESOLVED.** **Prior — `e42e2d5` · 2026-06-12 · by /audit-app (scoped: SYMBOL-DB-B / Object Status Card / DOC-UNDERSTANDING-1 G-3* coverage).** Verified all recently-added `shell/`+`server/ai/` modules carry inventory rows (no un-inventoried features). Tests re-run green: **test-symbol-db-b 37/37 · test-base-status-panel-a 17/17 · test-placement-candidates-panel-1 18/18 · test-step1-unified-bases-map-anchors (STEP1-C) 6/6.** **Catalog single-source confirmed** — `CAPABILITY_CATALOG`/`classifyKind` live ONLY in `world-state-db.js`; SYMBOL-DB-B sources systems from it, never fabricated (no-invent invariant tested). proposed_units enrichment stays **review-only** (`needs_review:true` always); SYMBOL-DB-B diff is purely additive — **no final units / placement / tasking / COA execution / movement / world-state mutation** introduced. session-memory mirror in sync (15 local files mirrored; `MEMORY.md` is the union-index superset). **⚠️ NEW finding:** the SYMBOL-DB-A *category ladder* is duplicated in `symbol-db.js` (canonical) + `base-status-panel.js` (defensive inline fallback) — see Duplications #intentional-mirror. **⚠️ Divergence:** local `main` `31914b6` holds an UN-PUSHED *"unify selected object panel for step1 base anchors"* commit that re-applies the owner-reverted unification (`86af86e`→`083afb1`); must be reconciled before the sync pushes it or it re-introduces reverted work. *(Prior — `d8efac0` · 2026-06-05 · Wave 7 — TestingAI→RMOOZ integration + Phase 6 state overlay + Middle East catalog.)* Smoke test PASS (server boots; `app.html`/`home.html`/`/api/ai/scenarios`/**`/api/wargame-sim/status`** all 200; auth/me 401 as expected on the real server — verify stub returns 200). Static tests re-run: FAST-INT-2 21/21 · FAST-DOC-1 30/30 · FAST-INT-3 24/24 · phase-6f-a applied-state 50/50 — all green; **`test-db-1-a-middle-east-catalog.js` 29 FAILING (new Drift D5).** New modules added: integration (`wargame-geojson-import.js`, `wargame-sim-import.js`, server `wargame-sim-bridge.js`), Phase 6 (`applied-state.js`, `delta-extractor.js`, `unit-status-panel.js`), Middle East DB (`middle-east-platform-loader.js` 🔴 unwired + `data/db/middle-east/platforms.json`). ⚠️ **app.html line refs in older rows below are now shifted (+577 lines since f43fd97)** — new rows cite fresh lines; treat older inline `app.html:NNNN` refs as approximate until next full re-stamp. See `SESSION_SUMMARY_2026-06-05.md`.
**Prior — Wave 6-GEN:** `f43fd97` · 2026-06-05 · by /audit-app (Wave 6-GEN — Phases 4A–5D: objectives hardening, Coastal Shield unit seed, BLUE base parity, coverage-summary panel (Phase 5C), DB-Lite Soviet platform expansion (Phase 5D-1); 6 new shell/wargame rows added; D4 ✅ confirmed resolved). See `docs/app-inventory-refresh-after-ui-unit-and-db.md`. ⚠️ **MERGE 2026-06-05:** this line (Phases 4A–5D) was reconciled with the parallel Command-Launch line below; the hub-launch redesign (home.js + native-scenario-loader.js) from that line WON the merge, superseding this session's hub point-fix. **Re-audit due.**
**Wave 6-HUB (merged in):** `9f56820` · 2026-06-04 · by /audit-app (Command Launch Wiring live; L3-A/L3-B/L3.5-A Why-Not + Feasible Alternatives wired; DOCTRINE-A committed; home.html/home.js hub registered; 5 new test files; all smoke-test routes 200).
**Wave 5 (prior):** `90dc412` · 2026-06-03 · by /audit-app (Wave 5 — Evidence chain OBJ-A/B/C + READINESS-A + WS-ENG1-A; tests re-run, D4 stale-test + `wargame 6/` stray flagged). _History below:_ `e75ff65` · 2026-05-31 · two same-day delta refreshes over the `1d4fa64` deep-dig (+16 commits, audited during an active parallel coding session). **Wave 1** (`f31548c`): AN1 attrition visuals, P0 authoring foundation, animation-readiness audit, stray-`wargame 5/` cleanup. **Wave 2** (`11e17b1`→`22d576e`, +9 commits): the **W3 presentation suite** — echelon roll-up, event pins, movement trails, engagement legend, symbol scaling + formation hover-peek, SIDC family-symbol resolver (SYM2), selected-unit operational readout (P5b), and engagement **mission graphics** (MG1). Code audited at `22d576e`; `855301c`+`e75ff65` on top are docs-only (this inventory + CMO docs) with no code drift. Drift D1/D2/D3 re-verified, line refs refreshed. ⚠️ Working tree has active uncommitted code WIP (`app.js`, `adjudicator-map.js`) — re-audit when it lands.
**Wave 3** (`22d576e`→`e0cf324`, +41 commits, audited 2026-06-02 by /audit-app): the **structural CMO backbone landed and is wired.** (1) **World State Engine** — WS1 projection (`shell/world-state.js`), WS3 transition (`shell/world-state-transition.js`), DB-Lite catalog DB1 (`shell/world-state-db.js`), detection DET1 (`shell/detection.js`), engagement ENG1 (`shell/engagement.js`), + server seam `sim/world-state-engine.js`; all pure/framework-free, tests **WS1 25 / DB1 15 / DET1 15 / ENG1 17 / WS3 15, all green**. (2) **MOVE1** continuous movement + playback clock (`wargame/movement-playback.js` — units glide, no teleport). (3) **3D Cesium globe** (`wargame/cesium-view.js`, lazy-loaded) + **Libya DEM terrain** (`client/dem-layer.js` 2D + Cesium 3D provider; server `dem-service.js`, `/api/dem/*`). (4) **Edit Mode slice 1** (`shell/scenario-edit-mode.js` — metadata/sides/posture editable, in-memory working copy; P0 authoring schema now consumed). (5) **Live read-only overlays** wiring the engines onto the map (4b79cf0): coverage/threat rings (41/41), DET1 detection contacts (30/30), ENG1 firing solutions (28/28), 3D parity. (6) **D3 unlocked** — `/api/sim/commit` LIVE + journals; new `/api/sim/decide` (WS3) and `/api/units/:id/place` endpoints. Smoke test 2026-06-02: server boots, `/api/ai/scenarios`+scenario load+`/api/dem/info` all 200.
**Wave 4 (continued 2026-06-03)** — **First reactive World State chain stable**: WS2 (objective status), WS2.5 (DERIVATIONS registry), WS4 (balance computed), WS-BLS-A/A2/B (ownership inversions). **207 tests all green**. BLS-B adds control-based rule (STAGED/SECURED/DENIED/CONTESTED) as temporary, explainable model (may be replaced by MTH1 richer formula). Map/HUD agreement confirmed via fallback pattern `ws.derived.X || state.X`. No mutation, no side effects. **Ready for next phase** (DOC1 doctrine or further roadmap).
**Wave 4** (`e0cf324`→`0efdf17`, +18 commits, audited 2026-06-02 by /audit-app): **Edit Mode Slice 2 completed + polished.** Slices 2A–2E (Geography / Forces / Stepped layout + New Scenario / Forces-at-scale tree / Map-as-editor) were already captured in the gap entry below; this wave adds the **UI-polish slices 2D-1J/K/M/N/O**: an **"Editing: {name}" indicator chip** + **saved-state badge** (unsaved / in-memory / on-disk / on-server), a **"Start from existing" scenario picker** in the New-Scenario form, the editor now renders as a **fixed-position overlay panel** that **expands over the map** when Edit Mode is ON, and **collapses to a banner during Pick-on-map**. No new boundary surface: still in-memory working-copy + `POST /api/scenarios` (durable scenario-file write, separate from the journal); **no `window.units`/`map`/`lines` mutation** (verified). Most of the +18 commits are docs (full 284-caption CMO set + exhaustive functional-rule consolidation) — **no app-code drift**. Smoke test 2026-06-02 (server already up on :8000): `/api/ai/scenarios` 200 (active=wargame3; dp-test-001/2/3 + wargame3), `/api/ai/scenario/wargame3` 200 (1.0 MB), `/api/dem/info` 200, `app.html` 200 (405 KB via verify stub). D1/D2/D3 unchanged (D3 stays resolved-unlocked); no new drift.
**Wave 5** (`0efdf17`→`90dc412`, +46 commits ahead of `origin/main`, audited 2026-06-03 by /audit-app): the **Evidence Architecture chain** landed on `main` via merges from the `map2` remote (branches `gracious-ellis`/`magical-lewin`/`nervous-grothendieck`). New pattern **source → ledger → visibility → operator**: **OBJ-A** objective-evidence ledger (`world-state.js` `computeObjectiveEvidence`, registered in `DERIVATIONS` *before* status), **OBJ-B** objective-status now reads the ledger, **WS-ENG1-A** engagement *outcome* ownership moved into World State (storage only — damage application unchanged), **WS-DET1-A**, **READINESS-A** 6 readiness evidence types (strength/availability/ammo/supply/readiness-state/casualty-rate; sources `ws.units[].supply|readiness`), and **OBJ-C** the new read-only **Objective Evidence Panel** (`shell/objective-evidence-panel.js`, 297 L, loaded `app.html:4822`, element `#objective-evidence-panel:4036`). **Verified by re-running tests 2026-06-03:** OBJ-B 29/29, READINESS-A 48/48, OBJ-C 51/51, WS-ENG1-A 13/13 green — **but `test-obj-a.js` is 30/3 (NOT all-green as the merge docs claim)**: 3 failures are a stale whitelist (see Drift **D4**), not a functional regression. Also picked up: AEGOV light-theme `style.css` (+250) and a stray dead **`wargame 6/`** dir (see Duplications #6). Self-reporting docs in repo root (`MERGE-COMPLETE-2026-06-03.md`, `OBJ-*-*.md`, `READINESS-*.md`, `docs/OBJ-C-COMPLETION-2026-06-03.md`); **DOCTRINE-A** is planned/approved-next (not built), **OBJ-D** (objectives *consume* doctrine) deferred. ⚠️ This work is **on `main`/`map2` only — not yet pushed to `origin`**.
**Branch at audit:** `main` (HEAD `90dc412`, a merge of `map2/claude/gracious-ellis-077026`). Prior audit branch was `claude/magical-lewin-e1ab3a`.

**2026-06-13 — Integration landed on `main` (post-audit; `AUDIT_SHA` line 1 NOT bumped — run `/audit-app` to fully re-stamp):** the DOC-UNDERSTANDING-1 doc-review slice is now on `main` @ **`52dfc6e`** (NOT pushed; `main` is +12 vs `origin/main`). Path: branch `doc-review-persist-fix-a` rebased onto `af30d3c` → **merged `d92549b`** into `integration/doc-review-persist-plus-json-loss` → **UTF8 mojibake fix `52dfc6e`** → `main` **fast-forwarded** `af30d3c→52dfc6e`. Carries: review-anchor persistence + base-id grouping + draft provenance (**FIX-A**); import **Red+Blue DOCX gating** + **Objective X sync** + Unit Status `displayUnitName` fallback (**FIX-B**); **STEP1 base-type symbols** (air/naval/land/facility review anchors); **JSON import loss fix**; **Arabic-RTL fixes**; **UTF8 mojibake fix** — reverses the cp1252 double-encoding introduced at `13cceef` in `doc-understanding-review.js`+`home.js` (guardrail `[[feedback_arabic_utf8_guardrail]]`, regression `scripts/test-utf8-mojibake-arabic-a.js`). Files: client `home.js`, `i18n.js`, `shell/{base-status-panel,demo-movement,doc-understanding-review,free-fight-demo,placement-candidates-panel,scenario-import-wizard,scenario-workspace,unit-status-panel}.js`; server `ai/{brief-to-scenario,multi-country-orbat}.js`. Tests green: **utf8-mojibake 17/0 · arabic-rtl 23/0 · base-placement 16/0 · objective-unitstatus 27/0 · step1-base-type 26/0 · base-status 38/0 · json-loss 8/0**. Integration branch **deleted** (fully merged); `doc-review-persist-fix-a` @ `d3ca76f` **kept** (worktree `MAP_2_fixb`; post-rebase tip, not an ancestor of main); `rescue-after-accidental-pull` @ `5f55f5f` kept pending owner OK. 5 pre-existing test failures (`fast-int-3`, `step1-unified-bases-map-anchors`, `free-fight-demo-a`, `multi-country-demo-a`, `doc-review-step1-proposed-units`) proven byte-identical at HEAD — unrelated to this work. See `[[project_doc_review_persistence_demo_cleanup]]`.

### Legend
| Icon | Meaning |
|---|---|
| ✅ | done & wired — initialized in `app.js`/server and used with live data |
| 🟡 | partial — works but has deferred/incomplete parts or mock data |
| 🔴 | stub/missing — placeholder, drafted-but-not-loaded, or dead |
| ⏸️ | deferred — intentionally parked (links to a memory note for the "why") |
| ♻️ | has duplication — logic/DOM repeated elsewhere; cleanup candidate |
| ⚠️ | **drift** — code disagrees with a documented decision/memory; needs owner confirmation |

---

## ⚠️ DRIFT — code vs. documented decisions (confirm before relying on memory)

These were found during the 2026-05-30 deep-dig. The memory notes were true when written but the code has moved. **Do not silently "fix" either side — confirm with the owner which is now correct.**

> **Update 2026-05-30:** D1 & D2 **reconciled** — verified against code, memory notes updated to RESOLVED. **D3 remains OPEN** pending an owner ruling (it's a locked rule).
> **Re-verified `11e17b1` 2026-05-31:** D1/D2 still wired (line refs refreshed below); **D3 unchanged — still OPEN**. The P0 authoring foundation (`aec386a`) and the entire Wave-2 W3 presentation suite are client-side render/data only — they stay *inside* the locked boundary and do not touch D3.
> **D3 RESOLVED 2026-06-01 — owner ruling "full unlock — UI mutates" (minimal-coherent scope).** Operator commit path is now LIVE: `ai-proposal-commit-bridge.js` POSTs `/api/sim/commit` (ACCEPT/REJECT; Hold defers) → durable `data/journal/<run>.jsonl` with `operator_id`. `journal-contract.js` `ALLOWED_MODES` now includes REAL; `boundary-audit-panel.js` re-pointed to the open posture (self-test + violation harness green). Still closed: client scenario mutation, export/download guards, the **separate** `scenario-workspace` live-decision flow. Verified E2E in-browser. `[[feedback_ai_sim_boundary_rules]]`.

| # | Topic | Memory says | Code now does | Where | Action |
|---|---|---|---|---|---|
| D1 | Scenario marker selection | markers **don't** emit `rmooz:unit-selected` (deferred until unified selection model) — `[[project_scenario_marker_selection_deferred]]` | Red & Blue markers **do** dispatch `rmooz:unit-selected` on click | `wargame/adjudicator-map.js:1937` (Red), `:2006` (Blue) | ✅ **RECONCILED 2026-05-30** — confirmed wired (consumed by `unit-panel.js:408`); memory updated to RESOLVED. |
| D2 | BLS drawn on map | per-step `applyState` on step nav still **deferred** (only load half wired in PR-288M) — `[[project_bls_not_drawn_on_map_deferred]]` | per-step `applyState` **fully wired**: `bls_status` colors semicircles + breach badge on STAGED→contested; blue-destroyed choreography; red-degraded updates | `wargame/adjudicator-map.js` `applyState()` `bls_status` loop ~`:4608` | ✅ **RECONCILED 2026-05-30** — load (PR-288M) + per-step (`goToStep`→`applyStepProgress`, P4 `5f0b272`) both wired; memory updated to RESOLVED. |
| D3 | AI/sim commit boundary (**was locked**) | _(history)_ "commit bridge dry-run only; no `/api/sim/commit`, no journal file" — `[[feedback_ai_sim_boundary_rules]]` | **Client commit bridge now POSTs `/api/sim/commit`** (ACCEPT/REJECT; Hold defers) → durable `data/journal/<run>.jsonl` with `operator_id`. Server path unchanged (also used by MC + legacy shim). | client `shell/ai-proposal-commit-bridge.js`; server `web-server.js:749`, `sim/journal.js:146` | ✅ **RESOLVED 2026-06-01 — owner ruling "full unlock — UI mutates"** (minimal-coherent). Verified E2E. Client scenario mutation + export/download + the separate `scenario-workspace` live flow stay closed. |
| D4 | OBJ-A test vs merged READINESS-A | merge docs claim `test-obj-a.js` "33 assertions, all passing" | _(was 30/3)_ now **33 passed / 0 failed**. | `test-obj-a.js` traceability block | ✅ **RESOLVED 2026-06-03 (DOCTRINE-A).** OBJ-A's frozen whitelist was widened to the UNION of all contributors (combat + readiness `ws.units[].supply\|readiness` + the 9 doctrine sources/types), the value-type check now allows booleans+null, and source-matching is prefix-based (handles `(default — …)` suffixes). Authorized by `DOCTRINE-A-SPECIFICATION.md` §9. Re-verified 33/33. |
| D5 | **Middle East platform catalog (db-1-a/b) — unwired, buggy, test-drifted, duplicates DB1** | db-1-a "minimal seed" + db-1-b "expansion": a Middle East platform catalog feeding the Commander Unit Status Panel | **(a)** `middle-east-platform-loader.js` **deleted** — was never wired in app.html; **(b)** `platforms.json` (19 entries) **deleted** — all useful data folded into `world-state-db.js`; **(c)** `unit-status-panel.js` `getDataSource()` updated to use DB-Lite (`AppWorldStateDB.capabilityFor`) instead of the dead `AppMiddleEastPlatform` reference; **(d)** DB1 is now the single source of truth — 29 entries total (11 prior + 18 new ME-derived). | `shell/world-state-db.js` (DB_VERSION 1.1.0-d5, 29 entries: f16c, mig29, f15e, mirage2000, gripen, tornado, awacs, patriot, tor_aads, mistral, s1_aaa, meko, corvette, patrol_boat, infantry_bn, armor_company, mlrs, logistics + 11 original); `shell/unit-status-panel.js` `getDataSource()` | ✅ **RESOLVED 2026-06-09 — owner ruling: fold useful ME data into DB1, delete parallel loader.** 18 ME platform entries merged with sensor labels + doctrine tags + source provenance. `platform_id`/`platform` field on units triggers named catalog entry. Keyword shortcuts added to `classifyKind()` for unambiguous platform name strings. `s300-sam` not duplicated (already `sam_s300`). test-pr-288L 83/83 ✅ · test-pr-288M 45/45 ✅. `[[project_middle_east_catalog_drift]]`. |
| D6 | **Offline copy of `scenario-import-wizard.js` was stale** | main/offline app shells ship byte-identical mirrors of shared shell modules (parity gates enforce this for the CMO/Scenario-QA lists) | _(was ~407 lines behind main)_ offline copy now **byte-identical to main**, and a new parity gate compares **every** shared `shell/*.js` (not just the CMO/Scenario-QA lists) so no shared module can drift silently again. | main `shell/scenario-import-wizard.js` == `Offline_Deployment/offline_app/client/shell/scenario-import-wizard.js`; gate in `test-app-inventory-audit-1.js` ("every shared main/offline shell module is byte-identical") | ✅ **RESOLVED 2026-07-02 — owner ruling "sync main→offline, not intentional divergence" (OFFLINE-PARITY-D6).** All 32 shared shell modules verified byte-identical; `test-app-inventory-audit-1.js` 9/9. Offline image rebuild is the operator's runtime step (Docker not run in this session). |

---

## A. Client shell — operational modules (`UI_MOdified/client/shell/`)

| Module / Feature | Status | Key file | Notes |
|---|---|---|---|
| **AI Free Fight operating flow — Slice 1** | ✅ | `shell/scenario-package-contract.js` · `shell/doc-understanding-review.js` · `shell/scenario-import-wizard.js` | **NEW 2026-06-21 (RMOOZ-AI-FREE-FIGHT-OPERATING-1).** Operator entry flow over the (unchanged) engine. (1) NEW canonical resolved-scenario contract — `window.RmoozScenarioPackage.build/summarize` → `units[]`/`objectives[]` with `coord_status` (exact\|candidate\|missing) + confidence + source + `resolver_report`; accepts a live scenario OR an analyze+placement payload; pure, no mutation; test `scripts/test-scenario-package-contract-a.js` 38/38. (2) Review panel (`doc-understanding-review.js`) gained the explicit **Scenario-mode fork** — `mode-noai` ("Scenario without AI" → delegates to the existing `generate` handler = deterministic load) + `mode-ai` ("Scenario with AI — Free Fight" → `RmoozFreeFightDemo.mount` = the SCC engine) + an honest resolver summary; the legacy/demo buttons (generate/preview/demo-movement/free-fight/debug) were collapsed under `<details data-el="more-actions">` (every test-asserted token retained). (3) Wizard (`scenario-import-wizard.js`) promotes the single JSON scenario-package import to PRIMARY and collapses the Red/Blue DOCX grid into `.wg-wz-docx-advanced`; `home.js`/`home.html` tile relabeled (no "Red/Blue DOCX"); `wargame-sim-import.js` marked legacy. **REUSES** the existing `/analyze`+`/placement` resolver chain + the Scenario Control Center engine — no engine rebuild. Live-verified (auth proxy :8011, `scripts/verify-auth-proxy.js`): home tile, wizard reframe, fork buttons, SCC mount over a real scenario. NEXT: Slice 2 four-agent orchestrator + live RED LLM · Slice 3 resolver depth + accept-coord-on-map · Slice 4 movement realism. `[[project_ai_free_fight_operating_slice1]]`. |
| Scenario Workspace | ✅ | `shell/scenario-workspace.js` | Large read-only operator overview; reads `window.RmoozScenario` (never writes). Paints name/status/phase, phase timeline, step-aware unit composition. Proposal service NOT_CONNECTED by design. **Wave 7 (+58 L, Phase 6C-A):** added live read-only readiness/supply display (CSS + text only, no unit-object mutation). |
| Event Log | ✅ | `shell/event-log.js` | Closed-set ops ledger (SYSTEM/OPERATOR/UI only); in-memory, max 100 rows. **Must stay tabular, not chat** — `[[feedback_event_log_not_chat]]`. 15 modules call `AppShellEventLog.append()`. |
| AI Proposal — Panel | ✅ | `shell/ai-proposal-panel.js` (`?v=3`) | Right-side review UI; Accept/Reject/Hold. **Wave 7 (+114 L, Phase 6E-A):** renders a readiness/supply **delta summary** (via `AppDeltaExtractor`, reading `RmoozScenario.scenario` read-only) and on Accept logs `STATE_DELTA` rows to the Event Log; the decision itself routes through `AppShellAIProposalCommitBridge.commitDecision()` (server commit, **not** local scenario mutation). |
| AI Proposal — Bridge | ✅ | `shell/ai-proposal-bridge.js` | Calls `/api/sim/propose` with `mockMode:true`; user opt-in toggle. |
| AI Proposal — Contract | ✅ | `shell/ai-proposal-contract.js` | Pure validator/normalizer; closed-set enums; strips nested objects. |
| AI Proposal — Inbox | ✅ | `shell/ai-proposal-inbox.js` | In-memory store (max 50); no persistence. |
| AI Proposal — Commit Bridge | ✅ | `shell/ai-proposal-commit-bridge.js` | **LIVE commit** (UNLOCKED 2026-06-01): ACCEPT/REJECT → `POST /api/sim/commit` + durable journal w/ `operator_id`; HOLD defers. `getState().mode==='live'`. D3 **resolved**. |
| AI Proposal — Decision Journal | ✅ | `shell/ai-proposal-decision-journal.js` | In-memory mirror (dies on reload); now records real commits (`committed:true` / `#seq` / "Live"); durable record is the server journal. Max 100. |
| Boundary Audit Panel | ✅ | `shell/boundary-audit-panel.js` | Read-only mirror of 9 safety boundaries; reads 13 `AppShell*` bridges. Highest coupling by design. |
| Journal Contract | 🟡 | `shell/journal-contract.js` | Pure per-entry validator; `DRY_RUN` mode only (REAL rejected). No persistence. |
| Journal Draft Preview | 🟡 | `shell/journal-draft-preview.js` | Read-only render of what *would* be written. No file write. |
| Journal Export Contract | 🟡 | `shell/journal-export-contract.js` | Shape of future export package; no Blob/download/fetch. |
| Journal Export Preview | 🟡 | `shell/journal-export-preview.js` | Read-only preview of export package; download deferred. |
| Journal Download Guard | ⏸️ | `shell/journal-download-guard.js` | Declares "download forbidden"; no download fn exists. Design contract only. |
| Safety Regression Badge | 🟡 | `shell/safety-regression-badge.js` | Read-only mirror of safety summary + popover; never runs tests. |
| Scenario Dry-Run Fixtures | 🟡 | `shell/scenario-dry-run-fixtures.js` | Static data only ("AMBER RIDGE"); consumed by a builder not yet implemented. |
| Timeline / Transport | ⏸️ | `shell/timeline.js` | UI scaffolding; buttons flip local CSS only. No event dispatch / sim hook / map mutation. |
| Canonical Scenario Runner | ✅ | `shell/scenario-runner.js` | **RUNFIX-1:** single canonical PREVIEW playback engine — `window.AppScenarioRunner`; `runScenarioCanonical`, `play`/`pause`/`toggle`/`seek`/`stepBy`/`setSpeed`, `registerPreviewRenderer`/`registerLiveRunner`. One `setInterval` timer; `_bindTransport` owns `rmooz:timeline-ui-action` when present (workspace defers to it). Loaded before movement-playback/timeline/workspace (`app.html:4468`). |
| Unit Panel (selected unit) | ✅ | `shell/unit-panel.js` | Reads `rmooz:unit-selected`; shows MGRS/echelon/side. **P5b** adds marker-derived readouts — symbol profile, current-step status, capability (`:182`/`:369`; tests 19/19, `test-p5b-selected-unit-readout.js`, `11e17b1`). Combat/C2 sim sections still placeholders pending a sim-data feed. |
| Side Picker (BLUE/RED/GOD) | ✅ | `shell/side-picker.js` | Persists to localStorage; broadcasts `rmooz:view-side-changed`. |
| Classification Bar | ✅ | `shell/classification-bar.js` | i18n text into top/bottom bars. |
| Clock (Zulu DTG + local) | ✅ | `shell/clock.js` | `AppShellClock.formatZuluDtg()` reused by 7+ modules. |
| Coordinate Readout | ✅ | `shell/coord-readout.js` | Cursor MGRS + bearing/range in footer; degrades if MGRS absent. |
| Scenario Catalog Contract | 🔴 | `shell/scen-catalog-contract.js` | Parses CMO `.scen` wrappers (10 safe fields) but **not loaded in app.html** — drafted, unwired. Deferred — `[[project_external_scenario_catalog_deferred]]`. |
| Scenario Edit Mode (Authoring) | ✅ | `shell/scenario-edit-mode.js` (~2.2k L, `?v=8`) | **Wave 7 (+243 L):** Step 1 Metadata hardening (Name sanitiser → lowercase-dash, `scenario_id` auto-sync, Label-required validation, `model_version` default `authored-v1`), Step 3 Sides hardening (every side needs `name_en`), pointer-events fixes for edit controls, and readiness(`ready`/`limited`/`not_ready`)+supply(0..1, dflt 0.8) selects on RED/BLUE unit editors. `saveDraft()` still gates on hard rules + `draftIsSafe()`; **boundary unchanged — in-memory `_draft` clone + `POST /api/scenarios` only, no `window.units`/`map`/`lines` mutation.** **Wired** (`window.AppEditMode`, mounted in app.html `:5125`, `#sw-editmode-toggle`). Edits an **in-memory working-copy draft** of the active scenario: metadata/sides/posture, geography (AO/obj+carver 0..60/pipeline/throughput/BLS), forces (red/blue OOB via tree+detail+search at wargame3 scale), time (`phase_table`), per-step briefing, New Scenario + "Start from existing" picker, stepped CMO build-order rail, **map-as-editor** (click-to-draw AO/pipeline/obj/BLS/unit coords). **Boundary-safe:** never mutates `window.units`/`map`/`lines`; "Save draft" writes only `window.RmoozScenario.scenario` for live preview; durable save = `POST /api/scenarios` (separate from the journal). Save-As-JSON = Blob download (boundary-safe). Doctrine/Weather/Missions/Events steps are 🟡 placeholder cards (engine gaps). Tests: slice2a 50/50, 2b 46/46, 2c 50/50, 2d 43/43, 2e 30/30 + server `test-api-scenarios-post.js` 17/17. Consumes the P0 authoring schema (row below) — `[[project_workspace_editable_owner_ruling]]`. ⚠️ tiny doc-drift: the file header comment still says "Export = copy-to-clipboard" though Save-As-JSON now uses Blob download. |
| Objective Evidence Panel (OBJ-C) | ✅ | `shell/objective-evidence-panel.js` (297 L) | **NEW 2026-06-03.** Read-only display layer for the World State **objective-evidence ledger** (`ws.derived.objective_evidence`). Pure passthrough — groups evidence into 6 categories (Combat / Readiness / Control / Contacts / Doctrine / System) with confidence dots (●●●/●●/●) + source attribution. **0 inputs, 0 buttons, no mutation.** Loaded `app.html:4822`; element `#objective-evidence-panel:4036` (z-index 1050, above the 1040 sidebar — fixed from a buried z-10 bug). Closes the "evidence stored but invisible" gap from `EVIDENCE-CONSUMPTION-AUDIT.md`. Tests `test-obj-c.js` **51/51 green** (re-verified 2026-06-03). Source layers: OBJ-A (ledger), OBJ-B (status reads it), READINESS-A (6 readiness types), **DOCTRINE-A (9 doctrine types — Doctrine group now populated)** — all in `world-state.js` `DERIVATIONS`. ⚠️ Value-audit caveat (self-reported): only exercised on a **DORMANT** objective; THREATENED/CAPTURED/CONTESTED still unverified. |
| Why-Not evaluator (L3-A) + Feasible Alternatives (L3.5-A) | ✅ | `shell/action-feasibility.js` (329 L) | **NEW 2026-06-03.** `evaluateAction(ws, action)` → `{verdict, blockers[], risks[], evidenceGaps[]}` for ENGAGE and ATTACK_OBJECTIVE. Pure/no-mutation; locked contract: no UI, no simulation, no scoring/recommendation wording. Also exports `generateFeasibleAlternatives(ws, action, finding)` (L3.5-A) — inverts each blocker/risk into a "Feasible Alternative" option lookup table (no probability, no prediction). Wired `app.html:4819`. Contract: `L3-A-SPECIFICATION.md`. Tests: `test-l3a-why-not.js` (80/80) + `test-l35a-alternatives.js` (29/29). ⚠️ `generateFeasibleAlternatives` logic-only — not yet displayed in UI (L3.5-B display slice pending). |
| Why-Not panel (L3-B) | ✅ | `shell/why-not-panel.js` (128 L) | **NEW 2026-06-03.** Read-only display of `evaluateAction()` verdict in the Scenario Workspace. Derives current action from the primary objective (no fabrication). Listens to `rmooz:playback-tick` + `rmooz:scenario-visibility-changed`. Strictly read-only — no mutation, no inputs. Wired `app.html:4844`. |
| Native scenario loader (PR-LAUNCH) | ✅ | `shell/native-scenario-loader.js` (~590 L, `?v=3`) | **NEW 2026-06-03, expanded 2026-06-04, +392 L Wave 7.** Consumes `?launch=` intent from home.html. **Wave 7: now 7 intents** — the 5 below **plus `import-geojson` / `import-docx`** which open the WarGamingGEN import card in a top-level modal popup (`openImportCardModal`, z 100000, auto-closes on `rmooz:wg-import-loaded` to reveal the map). Wired `app.html:5117`. Original **5 intents:** `demo` → fetch `samples/rmooz-native-01.json` → `loadLiveScenarioFromJson`; `load` → `sessionStorage['rmooz.pending-import']` → load → clear; `resume` → `sessionStorage['rmooz.last-json']` → load; `editor` → dismissable in-page notice; `new` → in-page notice. After every successful load: saves `sessionStorage['rmooz.last-json']` + `localStorage['rmooz.last-session']` (feeds Resume). Retry loop waits for workspace (20 × 150 ms). Wired `app.html:4983` (`v=2`). No parallel loader, no backend, no mutation path. `window.AppNativeScenarioLoader`. |
| Workspace consolidation (PR3) | ✅ | `shell/workspace-consolidation.js` (87 L) | **NEW 2026-06-03.** Three functions mounted on app.html: (1) **"← Main Window"** → relative `home.html` navigation (`new URL('home.html', href)` — location-agnostic); (2) **"Export Scenario"** → downloads `window.RmoozScenario.scenario` as a Blob JSON (read-only; shows `#rmooz-export-status` message); (3) **"Developer / Import"** toggle → CSS-reveals the hidden scenario import/debug section. Nothing deleted — full import path remains recoverable. Wired `app.html:4986` (`v=1`). `window.AppWorkspaceConsolidation`. |
| Doctrine evidence contributor (DOCTRINE-A) | ✅ | `shell/world-state.js` `computeDoctrineEvidence()` | **NEW 2026-06-03 (committed).** SOURCE-layer evidence contributor (NOT a `doctrine-engine.js` — by owner ruling, the "engine" name invites ROE/WRA/targeting behavior creep). Called from `computeObjectiveEvidence`; pushes **9 doctrine evidence types** (unit doctrine_tags / echelon / posture; side WCS-ROE / EMCON / engage_ambiguous; inheritance scope; objective priority; compliance summary) into the same flat ledger → renders in OBJ-C's Doctrine group. Two minimal pure projections added: `unit.posture` + top-level `ws.doctrine` (read-only echoes of the scenario; W3 has none → defaults at 0.5 confidence labelled "(default — no scenario doctrine)"). **Evidence only: no behavior, no consumption, no mutation, parity-proven** (objective_status_display byte-identical with/without). Contract: `DOCTRINE-A-SPECIFICATION.md`. Tests `test-doctrine-evidence.js` **78/78**; browser value-audit PASSED (9 doctrine rows render on a CONTESTED objective, ROE/EMCON/posture readable, no console errors). Next = **DOCTRINE-B** (consumer) / **DOCTRINE-C** (WRA/targeting interpretation) — gated, not now. |
| Scenario Authoring Schema (P0) | ✅ | `shell/scenario-authoring-schema.js` | Pure data/schema foundation for **Authoring Mode**: standard template + gap-fill (on copies) + diagnostics + draft-safety guard. NO DOM/fetch/storage/mutation; respects the locked boundary (`liveMutationAllowed`/`aiCommitAllowed=false`). **Now loaded** (`app.html:4943`, before edit-mode) and **consumed by Scenario Edit Mode** (`buildStandardScenarioAuthoringTemplate` for New Scenario; `isScenarioAuthoringDraftSafe` gates Save). `window.AppScenarioAuthoring` (33/33). `[[project_scenario_authoring_foundation]]`, `docs/rmooz-authoring-foundation.md`. (`aec386a`) |
| Action Feasibility (L3-A) | ✅ | `shell/action-feasibility.js` (329 L) | **NEW — found Wave 6 audit.** L3-A "Why-Not" feasibility evaluator. Takes a PROPOSED action → returns `feasible/blocked/risky + why` by consolidating reasons already in World State (no simulation, no new math). **Slice L3-A-1: ENGAGE only** — blockers map 1:1 to ENG1's own `reason` enum (out_of_range / weapons_hold / winchester / no_fire_control_channel / undetected). Pure, no DOM, no mutation, no window export. Loaded `app.html:4819`. Contract: `L3-A-SPECIFICATION.md`. |
| Why-Not Panel (L3-B) | ✅ | `shell/why-not-panel.js` (128 L) | **NEW — found Wave 6 audit.** L3-B read-only UI: renders action feasibility results from `action-feasibility.js` into a panel card. `window.AppShellWhyNotPanel`. Loaded `app.html:4845`. The HTML mount point is `#why-not-panel` (`app.html:983` — *"L3-B-1: Why-Not Review (read-only). Painted by shell/why-not-panel.js"*). |
| Native Scenario Loader | ✅ | `shell/native-scenario-loader.js` (90 L) | **NEW — found Wave 6 audit.** Loads W3 scenario JSON from native/browser sources. `window.AppNativeScenarioLoader`. Loaded `app.html:4984`. |
| Workspace Consolidation | ✅ | `shell/workspace-consolidation.js` (86 L) | **NEW — found Wave 6 audit.** Consolidates multi-source scenario state for workspace display. `window.AppWorkspaceConsolidation`. Loaded `app.html:4987`. |
| Objectives Editor Step | ⏸️ | `shell/objectives-editor-step.js` (297 L) | **NEW — found Wave 6 audit.** Scenario objective editor UI — deferred/incomplete; no window export, no behavioral hookup. Loaded `app.html:4993` but functionally a placeholder. |
| WarGamingGEN GeoJSON Import (FAST-INT-2) | ✅ | `shell/wargame-geojson-import.js` (205 L) | **NEW 2026-06-05 (Wave 7).** Operator picks an external WarGamingGEN `all_phases.geojson`; client computes a **read-only pre-flight summary** (phases / red / blue / objective via `summarizeGeoJson()`), then on explicit click `POST /api/scenario/import` (server's existing `scripts/port-wargame.js` porter, provenance `source=WarGamingGEN`) → fetch back → `AppShellScenarioWorkspace.loadLiveScenarioFromJson()`. **Mutation only on explicit Import click** (the sole write point). Self-mounts a card next to `#sw-live-scenario-import-card`. Loaded `app.html:5107` (`?v=1`). Test `test-fast-int-2-wargame-geojson-import.js` 21/21. |
| WarGamingGEN DOCX Sim Import (FAST-DOC-1/2) | ✅ | `shell/wargame-sim-import.js` (201 L) | **NEW 2026-06-05 (Wave 7).** Upload red/blue `.docx` → `POST /api/wargame-sim/stage-doc` → `/run` (spawns the real WarGamingGEN sim, **gated by `RMOOZ_ALLOW_SIM_RUN=1`**; else shows the manual command) → `/import` (latest dated export via the same porter). State machine waiting→staged→running→outputs→imported; read-only until explicit "Import + load". Self-mounts next to the GeoJSON card. Loaded `app.html:5112` (`?v=1`). Server side = `wargame-sim-bridge.js` (§C). Test `test-fast-doc-1-docx-sim-bridge.js` 30/30; home-launch wiring `test-fast-int-3` 24/24. |
| Applied-State overlay (Phase 6F-A) | ✅ | `shell/applied-state.js` (252 L) | **NEW 2026-06-05 (Wave 7).** Pure read-only **in-memory overlay**: derives a unit's applied readiness/supply from the scenario baseline + accepted `STATE_DELTA` event-log rows. **Boundary-safe — deep-clones every unit (`JSON.parse(JSON.stringify(unit))`, :73) before applying deltas; never touches `window.units`/`map`/`lines`/`RmoozScenario.scenario`.** `window.AppAppliedState` (all pure functions). Consumed by `unit-status-panel.js:370/384`. Loaded `app.html:4958` (`?v=1`). Test `test-phase-6f-a-applied-state.js` 50/50. |
| Readiness/Supply Delta Extractor (Phase 6E-A) | ✅ | `shell/delta-extractor.js` (140 L) | **NEW 2026-06-05 (Wave 7).** Pure comparison utility — diffs a proposal's `projected_state` against the current scenario baseline → readiness/supply delta arrays + formatters. Zero mutation. `window.AppDeltaExtractor`. Consumed by `ai-proposal-panel.js` (delta summary + `STATE_DELTA` event payloads on Accept). Loaded `app.html:4980` (before the panel, `?v=1`). |
| Commander Unit Status Panel (UI-UNIT-1-C) | ✅ | `shell/unit-status-panel.js` | **NEW 2026-06-05 (Wave 7); UI-Unit-1-C 2026-06-09.** Read-only commander panel; listens to `rmooz:unit-selected`, mounts to `#unit-status-panel`. Data sources (in order): `RmoozScenario` baseline → `AppAppliedState` overlay → **`AppWorldStateDB` DB1 enrichment** → `AppEventLog` deltas → milsymbol. **UI-Unit-1-C additions:** `AppWorldStateDB.enrichUnit()` called in `populatePanel` so sensors/weapons/magazines fill from DB1 catalog when absent from scenario (authored fields always win); DB1 platform label appended to role row for named entries; `getDataSource()` now returns `"DB-Lite — F-16C Fighting Falcon"` / `"DB-Lite — air_unit (default)"` / `"Scenario Baseline"` (no more broken ME reference); sensor rows show emcon field; `formatMagStock()` handles DB1 object stock format `{ weapon_class: count }` (was rendering `[object Object]`); source footnote added at bottom of sensor/weapon lists. D5 `AppMiddleEastPlatform` reference removed. **RMOOZ-UNIT-IDENTITY-CONTRACT-A 2026-06-15 (v2):** `displayUnitName()` delegates to the shared `RmoozUnitIdentity` resolver; hero shows the resolver `displayName` (real platform/capability name), the id row shows `canonicalId`, the code row shows `tacticalCode`, and `populateIdentity()` renders an **Identity-source row** (`#usp-identity-source`) + a **"generic — review"** chip when the name is type-derived. `resolvePanelIdentity()` injects the DB-Lite *named* platform to upgrade a generic name (authored identity always wins; DB-Lite generic default never overrides). Tests `test-unit-status-panel-identity-a.js` 10/10. **Unit imagery (2026-06-10):** `_renderRealImage()` (`:585`) renders a locally-cached photo per unit from the **git-tracked main-app** dir `client/assets/units/` (`manifest.json` provenance + `patriot-sam-battery.jpg` / `uss-lake-champlain-cvs39.jpg`); `src` resolved from `world-state-db.js` `image_asset`, with a military filter, lazy-load + graceful SVG-silhouette fallback, and `image_credit` attribution. **This is a main-app feature, NOT offline-only** — only the offline overlay's `assets/fonts/` + `brand-logo.jpeg` stay offline-only (`test-offline-isolation-0.js` §6 corrected 2026-06-11 to allow `client/assets/units/`). **No edit controls, no mutation.** Tests: `test-ui-unit-1-c.js` **174/174 PASS**. |
| ~~Middle East Platform Loader~~ | 🗑️ | ~~`shell/middle-east-platform-loader.js`~~ | **DELETED 2026-06-09 (D5).** Was never wired in app.html; `platforms.json` also deleted. All 18 ME platform entries folded into DB1 (`world-state-db.js`). See D5 row. |
| Platform Identity Enrichment (RMOOZ-PLATFORM-IDENTITY-ENRICHMENT-A) | ✅ | `client/shared/platform-identity-enrichment.js` (~200 L) | **NEW 2026-06-15.** Maps a thin unit (role/type only) to the best AVAILABLE platform display, with provenance. Precedence: **1** explicit scenario platform/name → `authored`; **2** DB-Lite exact (as-is) → `db_lite_exact`; **3** imported document equipment → `document_extracted`; **4** caller unit-catalog mapping → `catalog`; **5** generic capability fallback → `generic_fallback` (flagged GENERIC — REVIEW). A public platform catalog + `matchPlatformMention()` expands free-text mentions: F-16 → "F-16 Fighting Falcon", Tomcat/F-14 → "F-14 Tomcat", S-300 → "S-300 SAM Battery"; bare role ("fires"/"fighter") → generic, NEVER an invented exact platform. Pure, dual-mode; owns `CAPABILITY_LABELS`. The resolver (`unit-identity-resolver.js`) consumes it → adds `platform_provenance`/`platform_confidence`; the server AI path enriches by delegation (analyst/OOB → resolver). Panel shows the provenance row ("scenario platform"/"DB-Lite platform"/"document equipment"/"unit catalog"/"capability — generic review"). uid/internalKey + canonicalId unchanged. Loaded `app.html` before the resolver. Tests: platform-identity-enrichment 9. `[[project_unit_identity_contract]]`. Source @28037c1, tests/docs @next. |
| Tactical Terrain Context (GIS terrain-aware tactics) | ✅ | `server/ai/tactical-terrain-context.js` (~190 L) | **NEW 2026-06-15.** Assembles the 8 GIS factors for the tactical actions with HONEST provenance: borders/sovereign zones (sovereign-zone-engine + country label), movement corridor (enemy→objective axis — no road net exists), choke point (corridor; DEM relief-pinch when covered), high ground/observation (real DEM peak when covered else inferred vantage), coastline/desert/urban (terrain text hint), threat rings (situation thresholds), route cost (terrain-class ease), own border/fallback. Optional sync `elevationAt()` = `dem-service.getElevation` — **real elevation is LIBYA-ONLY**; off-coverage → inferred, marked in `provenance` (gis_dem/inferred_*). `computeActionGeometry` (tactical-action-library) consumes it (gated): recon→high ground + stand off warning ring + own zone; delay→choke; flank→avoid choke; defend→defensible high ground; withdraw→toward own border. Threaded by `free-fight-coa-planner.js` into buildDiverseCoas + LLM prompt `terrain_zone_context.gis` + `plan.terrain_context`. **2026-06-15 degeneracy fix (MOVEMENT-EXECUTION-AUDIT-A):** when `nearestEnemy` has no distinct value — e.g. the RED attacker whose only reference IS the objective (`enemy≈obj`) — the enemy→objective axis was zero-length, so corridor/choke/high-ground all COLLAPSED onto the objective and recon/delay/screen/defend became indistinguishable from attack. Now falls back to the own-force approach axis (`ownCenter→objective`); provenance tags `inferred_approach_axis`/`inferred_approach_corridor` mark it. Test: tactical-terrain-context-a 15 + movement-execution-a. `[[project_ai_commander_freedom]]`, `[[project_movement_execution_audit]]`. @4d35e55 |
| Tactical Action Library (RMOOZ-AI-COMMANDER-FREEDOM-A) | ✅ | `server/ai/tactical-action-library.js` (~260 L) | **NEW 2026-06-15.** The doctrine-free tactical vocabulary: 16 actions (recon/probe/screen/delay/defend/withdraw/flank/deceive/feint/attack/hold/reposition/avoid_contact/observe/support/reserve), each with DISTINCT movement geometry (recon stands off outside the threat zone, flank uses a different axis, withdraw increases distance, delay shapes the corridor, deceive moves off the real objective). Pure, planar; every move capped to MAX_STEP_DEG 0.10° (below the 0.15° teleport guard). 3 COA archetypes (cautious_recon / maneuver_deception / direct_action). `computeActionGeometry(action,unit,ctx)`. Consumed by the planner's `buildDiverseCoas`. **Validator is now structure/physics ONLY** (`rmooz-ai-tool-contract.js` — removed ground_unit_air_intercept / aircraft_as_infantry / impossible_domain_role / repeated_coa_family; kept invented-id/teleport/bounds/kill/naval-land-move/malformed). AI Commander Mode (controlled/free/high_variation) in `free-fight-coa-planner.js` + `free-fight-demo.js`. **FREEDOM-B (@d4971cd):** app default = High Variation (controlled = explicit option); de-biased prompts (no posture/intercept/warn/repeat wording — structure/physics only); free prompt lists the actions + "Do not force intercept/defend/attack"; "Current AI Mode" indicator + event-log mode·seed·family·lead·why; "Generate 5 different COAs" button (seeds 0–4). Tests: tactical-action-library 14, ai-commander-freedom 16. `[[project_ai_commander_freedom]]`, `[[feedback_validator_structure_physics_only]]`. @8ff6fb8 |
| COA Planner Performance (RMOOZ-AI-COA-PERFORMANCE-A) | ✅ | `server/ai/free-fight-coa-planner.js` · `server/ai/rmooz-ai-tool-contract.js` · `server/wargame-sim-bridge.js` · `client/shell/free-fight-demo.js` | **NEW 2026-06-15.** Profiled + optimized the FREEDOM-A/B + GIS terrain pipeline **without removing any tactical freedom or terrain behavior** (verified: 3 archetypes + terrain_basis/deciding_factor intact). (1) **Timing instrumentation** — `planCoas` returns `plan.debug_timing` (`total_ms` + `situation/build_scenario_intel/tactical_terrain_context/analyze_unit_capabilities/get_capability_intel_tool/build_commander_prompt_pack/build_diverse_coas/validation/commander_brief` ms) and logs a one-line `[free-fight/plan-coas] total=…` breakdown. (2) **Dedup** — `analyzeUnitCapabilities` ran TWICE (planner + tool-pack's `getCapabilityIntelTool`); planner now builds profiles ONCE and hands them to the pack via `opts._precomputed_profiles` → `getCapabilityIntelTool` reuses them (`profile_reused:true`, no 2nd analyst/LLM call). (3) **Request-scoped reuse** — `planCoas` refactored into `_buildPlanningContext` (heavy: situation/intel/terrain/caps/tool-pack, once) + `_assemblePlan(P,seed)` (cheap per-seed); no global cache (avoids stale-scenario bugs). (4) **`ai_depth`** opt — `fast` (heuristic caps, NO LLM analyst/commander, terrain summary only = no DEM sampling via `elevationFn=null` thread through buildDiverseCoas/buildCoasForSide) · `normal` (prior behavior) · `deep` (full LLM + DEM). (5) **Generate-5** = new `planCoaVariations` (route branches on `opts.variation_seeds`): builds ONE shared context, varies only seed/`buildDiverseCoas`, NO LLM per seed unless `deep`. (6) **UI** — "Planning…" text, `⏱ Stage timings` block (AI total/LLM/capability/terrain/COA build), Depth selector (fast/normal/deep), Generate-5 timing+"one shared context, not 5×" line. (7) **Timeout/fallback** — slow/unavailable LLM → deterministic floor + honest `fallback_message` "LLM slow/unavailable — used fast tactical planner." **E2E verified over real `:8000`:** single high_variation plan + Generate-5 (analyst once for 5 seeds, varied families/leads). Tests: **coa-performance-a 9/9**; baselines green (ai-commander-freedom 16 · tactical-terrain-context 15 · tactical-action-library 14 · rmooz-ai-tool-contract 41 · freefight-tool-contract-coa 32 · freefight-llm-capability-analyst 89). `[[project_ai_commander_freedom]]`, `[[feedback_validator_structure_physics_only]]`. |
| Movement Execution Audit (RMOOZ-AI-MOVEMENT-EXECUTION-AUDIT-A) | ✅ | `server/ai/tactical-terrain-context.js` · `server/ai/free-fight-coa-planner.js` · `client/shell/free-fight-demo.js` | **NEW 2026-06-15.** Audited the full chain LLM/COA output → normalize → select → apply → animation → marker, to answer the owner report "changing AI mode gives the same decision/approach/movement." **Findings:** the apply layer (server `applyCoaPlan` AND client `_resolveCoaMoves`/`_writeMoveFrame`) DOES consume `act.target` (action-specific), and `normalizeCoaAction` preserves the lower-case tactical actions (NOT converted to MOVE_TOWARD_OBJECTIVE). **Root-cause bug found + fixed:** for the RED attacker the terrain axis was degenerate (`enemy≈objective`) so recon/delay/screen/defend/support/probe ALL targeted the objective → identical bearing to attack (the "same approach"). Fixed in tactical-terrain-context (approach-axis fallback) → recon (−141°) now diverges from attack (−123°) through the real planner. **Instrumentation added:** `execution_mode` stamped on every action (`recon_standoff_target`/`flank_offaxis_target`/`delay_choke_target`/`withdraw_fallback_target`/`deceive_offaxis_target`/`attack_direct_target`/… via `executionModeFor`), carried into `applyCoaPlan.moved[]` + client move records; **per-unit event-log line** `EXECUTED: <unit> <action> from <lat,lon> to <lat,lon> via <execution_mode>` (`_logExecutedMoves`, both manual + loop apply); **debug overlay** (`🔬 Movement execution debug` — plan_source/llm_called/llm_status/fallback_reason/commander_mode/ai_depth/variation_seed/family/lead action/target coord/actual final coord); **plain LLM-usage banner** — deterministic → "LLM not used", fast → "Fast tactical planner, no LLM", llm → "LLM-planned". applyCoaPlan now also skips lower-case `hold` (not only `HOLD_POSITION`) and uses the recon short step for `recon`/`observe`. **NOTE: the RMOOZ-AI-COA-PERFORMANCE-A work did NOT address this — this is the separate correctness fix.** Tests: **movement-execution-a 11/11**; baselines green. `[[project_movement_execution_audit]]`, `[[project_ai_commander_freedom]]`. |
| Generate-AI-Attack-Plan AI-only gate (RMOOZ-AI-ATTACK-PLAN-AI-ONLY-A) | ✅ | `client/shell/free-fight-demo.js` · `server/ai/free-fight-coa-planner.js` | **NEW 2026-06-15.** Owner ruling: the **manual** "Generate AI Attack Plan" page must present **ONLY real LLM results** — never deterministic/fallback dressed as AI (the prior cause of "the numbers never change"). Strict client gate `_isRealLlmPlan` (in `renderCoaPlanHtml`, scoped to plans tagged `_requestedVia:'manual_generate'`): a plan renders cards/scores ONLY when `llm_called===true && plan_source==='llm' && llm_status∈{ok,success} && !fallback_reason && provider_used && model_used && ai_depth!=='fast'`. Otherwise it renders NOTHING but `_aiOnlyGateHtml` — **"No AI result generated. / LLM was not used. / Reason: <fallback_reason|timeout|unavailable|fast mode|deterministic fallback>"** + diagnostics (plan_source/llm_called/llm_status/fallback_reason/provider_used/model_used/ai_depth/commander_mode). `_generateCoaPlan` clears `_coaPlan=null` before the request (no stale numbers). Server: a validated LLM plan now reports `llm_status:'ok'` (was null on success) so the gate reads cleanly. **Scope: manual button ONLY** — the continuous loop + Generate-5 (which legitimately animate deterministic COAs) are untouched (verified: an untagged deterministic plan still renders). Fallback planner still exists internally for safety; it is just never presented as AI on this page. **MCP-PROMPT-A (2026-06-15):** the manual button now ROUTES THROUGH the MCP/tool-contract commander prompt as the single source of truth — `rmooz-ai-tool-contract.js` `composeCommanderPrompt()` owns the commander instruction (`SYSTEM_CONTRACT` + `MCP_COMMANDER_INSTRUCTIONS`: think-like-a-commander / don't-move-all-units / select-relevant / prefer-nearby-ready-supplied / reason-from-country-zone-terrain-rings-corridor-choke-objective / choose-freely-don't-force-full-attack / don't-move-all-countries / explain why_unit + non_selected_units / JSON-only). Planner `_assemblePlan` composes it ONCE from the tool pack and `_callLlm` sends it VERBATIM (the locally-built prompt is now only a fallback for legacy callers/tests); `non_selected_units` preserved through `normalizeCoa`. The button **forces `useLlm=true` + `ai_depth` off fast**; a validated LLM plan reports `llm_status:'ok'`; `plan.llm_enabled` + `plan.mcp_prompt` (+version) ride on every plan. UI: LLM-disabled → "Local LLM is disabled. Enable RMOOZ_ALLOW_SIM_RUN=1 and select a local model…"; diagnostics add **LLM enabled + MCP prompt pack version**; new **"View MCP Prompt"** toggle shows the exact system + user prompt (commander instructions, tools_context summary, allowed units, objective, terrain/zone) — proof the AI is instructed via MCP. Tests: **ai-attack-plan-ai-only-a 9/9 · ai-attack-plan-mcp-prompt-a 10/10**. `[[project_attack_plan_ai_only]]`, `[[project_attack_plan_mcp_prompt]]`, `[[project_movement_execution_audit]]`. |
| AI Commander Free Fight loop = AI-only (RMOOZ-AI-FREE-FIGHT-AI-ONLY-A) | ✅ | `client/shell/free-fight-demo.js` | **NEW 2026-06-15.** Owner ruling: the **"AI Commander Free Fight"** loop card must move units ONLY via a real local LLM — **no LLM = no movement** (no deterministic / fallback / fast / hardcoded planner animation in this card). Pre-start gate (`startLoop`/`stepOnce` via `_freeFightAiReady`): blocks unless LLM toggle on + `ai_depth`≠fast + (route health, if probed) `allow_sim_run` + `model_available` → shows **"AI Free Fight requires a local LLM. Enable RMOOZ_ALLOW_SIM_RUN=1 and select a local model."** Per-turn gate (`runNextTurn`): a turn applies ONLY when `_isRealLlmPlan(plan)` (llm_called + plan_source==='llm' + llm_status ok + no fallback + provider/model + not fast); otherwise the turn is **skipped (no `_runTurnCore`, no movement, no animation)**, the loop pauses, and it logs **"AI turn skipped — LLM not used."** The deterministic builder/`_runTurnCore` remain for the loop-MECHANICS test suites (sanctioned via `_setAiOnlyGateForTest(false)` — `continuous-commander-loop` + `coa-route-json-guard`), but NEVER drive the live card. Honest panel labeling carried over (deterministic ⇒ "Deterministic tactical planner — LLM not used", not "AI Commander Reasoning"). Tests: **ai-free-fight-ai-only-a 7/7** (toggle-off / fast / route-disabled blocked; deterministic turn skipped w/ no movement; real LLM moves units; step-once blocked). Loop-mechanics suites updated + green (commander-loop 42, route-guard 49). `[[project_free_fight_ai_only]]`, `[[project_attack_plan_ai_only]]`. |
| AI Free Fight REAL-LLM E2E acceptance (RMOOZ-AI-FREE-FIGHT-REAL-AI-TEST-A) | ✅ | `scripts/verify-ai-free-fight-real-llm-e2e.js` · `server/ai/{free-fight-coa-planner,rmooz-ai-tool-contract}.js` · `client/shell/free-fight-demo.js` | **NEW 2026-06-15.** Owner ruling: this card's acceptance must prove the **real configured local LLM** produced the plan AND the real app loop used it to move units — NOT unit tests / mocks / deterministic / fast / test-bypass / route-only. `scripts/verify-ai-free-fight-real-llm-e2e.js` spawns the real server with `RMOOZ_ALLOW_SIM_RUN=1` (+ ollama/qwen3-coder), verifies preconditions (route health `allow_sim_run`+`model_available` + provider/model + the provider reachable with the model loaded via `/api/tags`), then drives the **REAL client loop** (`_startLoopForTest`, **AI-only gate ON — no bypass**, LLM toggle on, Normal depth, `capture_raw_llm`) against the live server + real model, and asserts `plan_source==='llm'` · `llm_called` · `llm_status==='ok'` · provider+model present · no `fallback_reason` · **raw LLM response captured** · MCP prompt captured · ≥1 unit moved · EXECUTED line present · no "AI turn skipped". **Fails closed** (non-zero) on any missing precondition/proof — mocks/deterministic/fast/bypass cannot pass it. Supporting changes: `_callLlm` returns `raw_response` (+ plan `llm_raw_response` gated by `opts.capture_raw_llm`); **LLM destination targets are clamped to one tactical bound** (`_clampLlmTargets`, ≤0.10° — preserves the model's direction, passes the validator teleport guard, mirrors the apply step); the MCP prompt now **requires 2-3 COAs** (the model was returning one focused plan, tripping the ≥2 floor). **Ran GREEN here** with qwen3-coder:latest: LLM picked COA-2 (flank R-3 / feint R-4), **explained non_selected_units** (R-1 too close to corridor, R-2 covers the choke), EXECUTED `R-3 flank … via flank_offaxis_target`, 2 units moved. `[[project_free_fight_real_ai_test]]`, `[[project_free_fight_ai_only]]`, `[[project_attack_plan_mcp_prompt]]`. |
| Single AI execution gate (RMOOZ-AI-EXECUTION-SINGLE-GATE-A) | ✅ | `server/ai/{free-fight-coa-planner,free-fight-llm-capability-analyst,free-fight-llm-decision,free-fight-llm-plan}.js` · `server/wargame-sim-bridge.js` · `client/shell/free-fight-demo.js` | **NEW 2026-06-15.** Owner ruling: the confusing `RMOOZ_FREE_FIGHT_LLM` flag is **removed** — `RMOOZ_ALLOW_SIM_RUN` is the SINGLE top-level permission gate for AI Free Fight + Generate AI Attack Plan + the real-LLM E2E + route health + UI diagnostics + server planner permission checks. **Zero active runtime references to the old flag** (server/client/scripts/tests); the config env vars were renamed `RMOOZ_FREE_FIGHT_{PROVIDER,MODEL,TIMEOUT_MS}` (dropped the `_LLM` infix). Three states: (1) `RMOOZ_ALLOW_SIM_RUN!=='1'` → blocked, no movement, **"AI execution is disabled. Enable RMOOZ_ALLOW_SIM_RUN=1."**; (2) allowed but no local model → blocked, **"AI execution is allowed, but no local LLM/model is available. Select/configure a local model."**; (3) allowed + model available → only `plan_source='llm'`+`llm_called`+`llm_status='ok'` moves units / renders AI cards. **Route health** (`/plan-coas/health`) now returns `allow_sim_run` · `ai_execution_enabled` · `provider` · `model` · `model_available` (live provider probe `probeModelAvailable` → ollama `/api/tags`, async) · `reason_if_blocked` (+`llm_enabled` back-compat alias). Client `_freeFightAiReady` gates on route-health `allow_sim_run`+`model_available`+depth (NOT a separate toggle; the loop forces `useLlm:true`). Deterministic/fast/fallback planners remain for tests/other modes but never present as AI here. **Real-LLM E2E re-run GREEN** with `RMOOZ_ALLOW_SIM_RUN=1` (qwen3-coder, 3 units moved). Tests: ai-free-fight-ai-only-a 7 · ai-attack-plan-ai-only-a 11 · ai-attack-plan-mcp-prompt-a 10 · route-guard 49 (+ all baselines). `[[project_ai_execution_single_gate]]`, `[[project_free_fight_real_ai_test]]`. |
| Free-fight LLM timeout/retry + model single-source (RMOOZ-AI-COA-TIMEOUT-RETRY-A · RMOOZ-AI-FREE-FIGHT-MODEL-SOT-A) | ✅ | `server/ai/{free-fight-coa-planner,free-fight-llm-capability-analyst,free-fight-llm-decision,free-fight-llm-plan,ai-config}.js` · `client/shell/free-fight-demo.js` · `scripts/verify-ai-free-fight-real-llm-e2e.js` | **NEW 2026-06-16.** Operator hit *"AI execution allowed, but no local LLM/model available — `llm_returned_fewer_than_2_valid_coas (0)`"* on a healthy Ollama. **Two root causes, both fixed.** (1) **Timeout too tight** — the per-LLM-call timeout defaulted to **45s** (30s in `free-fight-llm-plan`), but the COA call asks for up to 2500 tokens of JSON which needs ~40–70s warm (longer cold) on a 7B model here; it timed out → planner silently fell back to deterministic COAs → misleading "no model" error. Default bumped to **120s** across all 4 free-fight LLM modules (`RMOOZ_FREE_FIGHT_TIMEOUT_MS` still overrides; repo e2e uses 180s). `_callLlm` now **retries** the *fast* failure modes (bad JSON / <2 valid COAs) `RMOOZ_FREE_FIGHT_ATTEMPTS` times (default 2) but returns immediately on timeout/unavailable (no stacked waits). `_assemblePlan` `fallback_message` now distinguishes *timed out* vs *unavailable* vs *unusable plan after retries* — no longer implies the model is missing. (2) **Model default drift (resolved)** — `resolveLocalModel()` in the 3 server modules hardcoded `qwen3-coder:latest` (NOT installed by default; diverged from `ai-config.js`'s `qwen2.5:7b`) — this is what produced the original *"model 'qwen3-coder:latest' not found"* 404. All three now fall back to **`ai-config.defaultModel`** (single committed source; free-fight env vars still win). Client placeholder + e2e default + tests repointed to the ai-config default (tests assert against `AI_CFG.defaultModel`, not a hardcoded name). **Verified:** live `/plan-coas` HTTP route → `plan_source:'llm'`, `llm_status:'ok'`, no fallback, 3 COAs, `model_used:qwen2.5:7b`. Tests: **freefight-local-llm-only 66/0 · freefight-coa-route-json-guard 49/0**. `[[project_free_fight_llm_runtime_config]]`, `[[project_ai_execution_single_gate]]`. |
| AI Commander repair loop + planning trace (RMOOZ-AI-COMMANDER-REPAIR-LOOP-A) | ✅ | `server/ai/{free-fight-coa-planner,rmooz-ai-tool-contract}.js` · `client/shell/free-fight-demo.js` · `scripts/verify-ai-free-fight-real-llm-e2e.js` | **NEW 2026-06-16.** Demo direction (owner): the LLM visibly drafts COAs (commander brain), RMOOZ validates + STABILIZES them (staff officer) — not a deterministic system the LLM decorates. **(1) Repair loop** — when the LLM COA is unusable (validator rejection OR `_callLlm` "<2 valid COAs / invalid JSON" = the model used unit/action refs outside the allowed list), `_assemblePlan` sends the violations BACK to the model via new `CONTRACT.composeRepairPrompt()` (carries violations + allowed_unit_ids + allowed actions + the COA schema), re-normalizes + re-validates, bounded by `RMOOZ_FREE_FIGHT_REPAIR_ATTEMPTS` (default 1). Success → `plan_source:'llm'`, `repaired:true`, `repair_attempts`, `repaired_violations` (+ `tool_contract.repaired`); still invalid → Staff-Safe floor, violations preserved. **(2) `planning_trace`** on every plan — REAL data only (NO readiness/supply): `input_understood` (units analyzed, role buckets maneuver/fires/air_defense/recon/support from the capability catalog via `_capBucket`, enemy assessment by side, objectives, terrain class + honest provenance, alert/ROE), `reasoning` (per-COA why + why-not), `validation` (unit-ids-valid/actions-matched/kill-blocked/within-bounds/repaired+count/valid_coa_count), `mode` (ai_commander|staff_safe + provider/model). **(3) Two modes** — `opts.planning_mode`: `commander` (default, LLM-first+repair) · `staff_safe` (skips the LLM → deterministic; the manual toggle AND the auto-fallback). **(4) UI** (`free-fight-demo.js`): the demo-facing **AI Planning Trace** panel (Input understood → AI reasoning → Validation ✓, "✓ repaired N invalid references", green AI-Commander / amber Staff-Safe badge) renders before the COA cards; a **live in-flight** "🧠 AI Commander reasoning… Ns" elapsed ticker (honest in-progress — SSE step-streaming deferred); an **AI Commander \| Staff-Safe** toggle wires `planning_mode`. **AI-only gate relaxed (updates RMOOZ-AI-ATTACK-PLAN-AI-ONLY-A, row above):** Staff-Safe deterministic COAs are now SHOWN clearly badged (not hidden) — honesty preserved by the explicit Staff-Safe label + the why-not-AI note; only a true no-plan failure still hits the gate. Local-only + `RMOOZ_ALLOW_SIM_RUN` unchanged. Owner decisions: keep qwen2.5:7b + live trace · only real data · Commander default w/ auto+manual Staff-Safe · focused slice (battle-package enrichment / reasoning-JSON split / SSE / per-axis terrain / readiness deferred). Tests: **free-fight-repair-loop-a 7/7**; baselines green (free-fight-ai-a 9 · card-visibility 7 · ai-model-selector 16). `[[project_ai_commander_repair_loop]]`, `[[project_attack_plan_ai_only]]`. |
| AI Free Fight candidate pre-filter (RMOOZ-AI-FREE-FIGHT-CANDIDATE-PREFILTER-A) | ✅ | `server/ai/candidate-prefilter.js` (NEW) · `server/ai/{free-fight-coa-planner,rmooz-ai-tool-contract}.js` · `client/shell/free-fight-demo.js` · `scripts/{test-free-fight-candidate-prefilter-a,verify-ai-free-fight-prefilter-e2e}.js` | **NEW 2026-06-16.** Reduce the LLM problem size BEFORE the MCP prompt so a small model gets a small, clean problem (the app logic shrinks the problem — NOT "just use a bigger model"). `selectCandidates(allUnits, obj, ctx)` scores the active-side force by distance-to-objective · objective country/zone (`terrainCtx.owner_country`) · role/capability (capability catalog) · per-domain reachability heuristic (interim `REACH_DEG`) · threat proximity; readiness/supply/already-tasked scored ONLY when present (never invented — the ORBAT/demo units don't carry them). Keeps the top **10–25** (clamp, default 20; all when total ≤ target or `opts.send_all_units`) + a grouped `non_candidate_summary` (out_of_reach / far / different_country / low_capability …). The planner overrides `allowedUnitIds` = candidates → drives BOTH `normalizeCoa` AND `validateCommanderCoaTool`, so **the guardrail is free: a non-candidate the model invents is rejected (no deterministic move in AI Free Fight)**. `composeCommanderPrompt` restricts `force_pool`/`allowed_unit_ids` to candidates, adds `selected_candidates` (detailed) + `non_candidate_summary` + "choose ONLY from selected_candidates", and filters the REST of the prompt to candidates too — `capability_intel` per-mission lists + a compacted `terrain_zone_context` (contact_picture/coalition → summaries) — so a 300+ force never bloats the prompt. Result carries `candidate_prefilter`; `planning_trace.input_understood.candidates` + the AI Planning Trace UI show "Candidate units sent to AI: X / Y · excluded Z" + top reasons. Tests: **candidate-prefilter-a 7/7** (300-unit scoring, send-all bypass, normalizeCoa guardrail, planCoas prompt=candidates-only + no terrain leak, result/trace, UI render); **Qatar + hundreds-of-units real-LLM E2E** `verify-ai-free-fight-prefilter-e2e.js` (10–25 sent, plan_source=llm, only candidates moved). Deferred: real route-cost terrain relevance, a real range model (`REACH_DEG` is interim), per-unit readiness/supply data. `[[project_ai_free_fight_candidate_prefilter]]`, `[[project_ai_commander_repair_loop]]`. |
| AI Commander demo pacing (RMOOZ-AI-COMMANDER-DEMO-PACING-C) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `scripts/test-free-fight-demo-pacing-c.js` | **NEW 2026-06-16.** Demo-pacing only — **NO battle/scoring/DB-Lite/terrain/readiness/supply logic changed** (the server already skips BOTH LLMs at `staff_safe`+`fast`; only client request-shaping + labels changed). Manual Staff-Safe is now FAST + honest: `_generateCoaPlan` shapes the request by mode — **staff_safe → `{useLlm:false, ai_depth:'fast'}`** (skips both the COA-generation LLM AND the capability-analyst LLM → deterministic/DB-Lite COAs, near-instant) · **commander → `{useLlm:true}`** and never sends `fast` (bumps `fast`→`normal` so AI Commander always reasons). Mode description carries the **timing labels** "⚡ Fast deterministic planning — AI explanation optional." (Staff-Safe) / "Full local AI planning — may take 2–5 minutes." (AI Commander); the Generate button reads "⚡ Generate Staff-Safe Plan (fast)" in Staff-Safe. The timeout / auto-fallback stays VISIBLY badged (Staff-Safe Mode + "timed out — used Staff-Safe planner"), never silent. `?v=pacing-c`. Tests: **free-fight-demo-pacing-c 5/5** (server `staff_safe`+`fast` → 0 LLM calls + deterministic + trace mode `staff_safe` · commander → LLM called · client request-shaping per mode via DOM-harness + stubbed `fetch` · timing labels + button text · timeout fallback still labelled); baselines green (repair-loop 7 · candidate-prefilter 7). `[[project_ai_commander_repair_loop]]`, `[[project_ai_free_fight_candidate_prefilter]]`. |
| Free Fight local-only security lock (RMOOZ-AI-FREE-FIGHT-SECURITY-CLEANUP-A) | ✅ | `scripts/test-free-fight-zen-blocked-a.js` (NEW) · guards `server/ai/{free-fight-coa-planner,free-fight-llm-capability-analyst}.js` + `.env.example` | **NEW 2026-06-16 (security cleanup).** A foreign WIP change (uncommitted in the shared working tree — **NEVER in git history; HEAD was always clean**) tried to (a) unblock the `zen` cloud provider for Free Fight and (b) put a real `OPENCODE_ZEN_API_KEY` into the tracked `.env.example`. Both reverted to HEAD. Free Fight stays **local-only**: `REMOTE_PROVIDERS_BLOCKED = ['claude','zen','openai','auto']` in BOTH the COA planner AND the capability analyst; a blocked provider short-circuits `_callLlm`/analyst to `{llm_status:'remote_blocked', fallback_reason:'remote_provider_not_allowed_for_free_fight'}` with **ZERO `aiProvider.generate` calls** (no cloud); `routeHealth()` reports `provider_blocked` and never names zen as the active provider (reports `ollama`). New regression **`test-free-fight-zen-blocked-a 8/8`**: `isRemoteProvider`; `routeHealth`-zen; `_callLlm`-zen no-cloud-call; `planCoas`-zen end-to-end no-cloud + `deterministic_coa_fallback`; `.env.example` has no `sk-*` secret + every `*_API_KEY/_TOKEN/_SECRET` blank + `OPENCODE_ZEN_API_KEY` blank. The existing `test-freefight-local-llm-only-a` §3/§11/§12 already assert zen/remote rejection. **OPEN — the exposed Zen key MUST be rotated** (it was present in the working tree). `zen` stays blocked for Free Fight unless the owner explicitly approves a future cloud mode. `[[feedback_free_fight_local_only_security]]`, `[[feedback_shared_working_tree_concurrency]]`. |
| ai-config committed-secret guard (RMOOZ-SECURITY-AI-CONFIG-GUARD-F) | ✅ | `scripts/test-ai-config-guard-f.js` (NEW) · `docs/ai-config-secret-guard.md` (NEW) · guards `server/ai/ai-config.js` | **NEW 2026-06-16 (committed safety net — NO AI behavior change; `ai-config.js` untouched).** A static guard so the committed `ai-config.js` can never accidentally carry a real cloud secret or a cloud-default flip (directly addresses the foreign-WIP risk from row above — the hardcoded Zen `sk-…` + `aiProvider: ollama→opencode.ai` cloud switch). The test reads the source, evaluates **ONLY the committed `defaults` object literal** (brace-matched + `new Function` — NOT the env/overlay-merged export, which CI/operator env legitimately overrides), and **fails closed** if: a real **`sk-…` key** appears anywhere in the file (`/sk-[A-Za-z0-9._-]{20,}/`); **any `apiKey`** in defaults is non-empty (recursive sweep — top-level + `claude`/`zen`/`openrouter`); top-level **`url` ≠ `http://localhost:11434`**; default **`apiStyle` ≠ `ollama`**; default **`aiProvider` ≠ `ollama`** (no cloud default — serves the goal). Comment placeholders **`<api-key>` / `<claude-api-key>` are ALLOWED** (not real keys, not values). Every violation message names the fix: **use env vars (RMOOZ_*/ANTHROPIC/OPENCODE_ZEN/OPENROUTER) or the gitignored `ai-secrets.local.js`, NEVER committed defaults** (req #5). Includes **guard-the-guard** negative tests (planted `sk-ant-…` key, `apiStyle:'openai'`, cloud `url`, `aiProvider:'zen'`, non-empty zen key) that each must fail-closed. Tests: **ai-config-guard-f 15/15**. `[[feedback_free_fight_local_only_security]]`, `[[feedback_shared_working_tree_concurrency]]`. |
| Cross-platform run scripts (RMOOZ-CROSS-PLATFORM-RUN-SCRIPTS-G) | ✅ | `scripts/run-rmooz-app.js` (NEW) · `package.json` (`serve`/`web`) · `scripts/test-cross-platform-run-scripts-g.js` (NEW) | **NEW 2026-06-16 (run-tooling only — NO combat/COA/DB-Lite/terrain/readiness/planner/ai-config change).** The `serve`/`web` npm scripts used Windows-cmd-only `set RMOOZ_ALLOW_SIM_RUN=1&& …` (a no-op in `sh`/`bash` → the AI gate was OFF on macOS/Linux → "AI execution is disabled") plus a foreign user's hardcoded `C:\Users\ADMIN\…` TestingAI/Python paths. **Fix:** a **zero-dependency Node launcher** `scripts/run-rmooz-app.js` — `applyRunDefaults(env)` sets, only when unset, `RMOOZ_ALLOW_SIM_RUN=1` (default-on; an explicit `=0` for a locked-down deploy is preserved), `RMOOZ_SIM_MODEL=qwen2.5:7b`, `RMOOZ_FREE_FIGHT_TIMEOUT_MS=300000`, **repo-relative** `RMOOZ_TESTINGAI_DIR=<repo>/TestingAI`, and per-OS `RMOOZ_PYTHON` (`python3`/`python`) — then spawns `server/web-server.js` with that env (process.env wins over `.env`, so defaults apply yet stay overridable). **No provider is set → local-only Ollama default preserved (req #6).** `serve`/`web` → `node scripts/run-rmooz-app.js`; `app` unchanged (`concurrently start+serve`). Deliberately NOT `cross-env` (a devDependency would break `npm run serve` under `npm ci --omit=dev`/offline). **Anti-foreign-commit:** a concurrent agent had applied an uncommitted `cross-env` variant (+ `cross-env` devDep + lockfile churn); reverted to HEAD and superseded by the launcher (`[[feedback_shared_working_tree_concurrency]]`). Tests: **cross-platform-run-scripts-g 10/10** — launcher sets gate=1 · explicit value preserved · repo-relative TestingAI + no `C:\Users\ADMIN` + per-OS python · no provider forced · no script has `set RMOOZ_ALLOW_SIM_RUN=1&&` · no script has `C:\Users\ADMIN` · serve/web route through the launcher · **LIVE health via the launcher → `allow_sim_run:true`, `configured_provider:ollama`, `provider_blocked:false`**. Caveat: `electron` script still has cmd `set ELECTRON_RUN_AS_NODE=&&` (desktop build, out of scope — clears a var, harmless on `sh`). `[[project_ai_execution_single_gate]]`, `[[feedback_shared_working_tree_concurrency]]`. |
| Canonical LLM runtime-config resolver (RMOOZ-LLM-RUNTIME-CONFIG-A) | ✅ | `server/ai/llm-runtime-config.js` (NEW) · refactors `server/ai/{model-selection,free-fight-coa-planner,free-fight-llm-capability-analyst,free-fight-llm-decision,free-fight-llm-plan}.js` | **NEW 2026-06-16.** ONE resolver owns provider/model/timeout/repair/draft/keep-alive — no feature module reads those env directly (kills the drift across the 5 free-fight LLM modules + model-selector). Canonical `RMOOZ_LLM_{PROVIDER,MODEL,TIMEOUT_MS,REPAIR_ATTEMPTS,ATTEMPTS,KEEP_ALIVE}` + **task overrides** `RMOOZ_LLM_MODEL_<TASK>` / `RMOOZ_LLM_TIMEOUT_MS_<TASK>` (COA_PLANNER/CAPABILITY_ANALYST/DECISION/PLAN). **Legacy aliases kept (req #5, marked legacy):** `RMOOZ_FREE_FIGHT_{MODEL,PROVIDER,TIMEOUT_MS,REPAIR_ATTEMPTS,ATTEMPTS}`, `RMOOZ_AI_{MODEL,TIMEOUT_MS}`, `RMOOZ_OLLAMA_{MODEL,TIMEOUT_MS}`. **Model precedence:** task override → operator UI selection (`model-selection`, lazy-required → no load cycle) → canonical env → legacy → `ai-config` committed default. `getProvider()` returns the RAW provider so the feature modules' `isRemoteProvider` still blocks `zen` (**local-only intact**, security-lock row above). `model-selection.js` now owns ONLY the runtime UI pick + delegates its env default to the resolver (display `getProvider()` still clamps remote→ollama). **Behavior-neutral (req #7):** defaults unchanged (timeout **120000**, draft **2**, repair **1**); `ai-config.js` stays the ollama/adjudicator base (documented home of `RMOOZ_OLLAMA_*`, exempt). Tests: **llm-runtime-config-a 13/13** (default/legacy/task-override/timeout/repair/draft + planner & analyst use resolver output + **GREP-PROOF**: zero direct model/provider/timeout/repair env reads in the 5 feature modules); regressions green (model-selector 16 · repair-loop 7 · prefilter 7 · zen-blocked 8 · pacing-c 5 · capability-analyst 89 · coa-planner 71 · **timeout-polish 23** — repointed to the resolver, also clearing 2 stale 45s assertions). `[[project_llm_runtime_config_resolver]]`, `[[project_free_fight_llm_runtime_config]]`, `[[project_local_model_selector]]`. |
| Free Fight AI gate status card (RMOOZ-FREE-FIGHT-AI-GATE-CARD-D) | ✅ | `client/shell/free-fight-demo.js` · `client/shell/ai-model-hud.js` · `server/ai/free-fight-coa-planner.js` (routeHealth) · `server/web-server.js` (/api/ai/models) · `scripts/test-free-fight-ai-gate-card-d.js` | **NEW 2026-06-16 (UI/diagnostics only — NO combat/COA/DB-Lite/terrain/readiness/Staff-Safe logic change).** The Free Fight AI status card now explains the EXACT block + fix, separated by gate. `_aiGateStatusHtml()` (reads `/plan-coas/health`) shows FOUR signals — **execution gate** (RMOOZ_ALLOW_SIM_RUN) · **raw provider** (from llm-runtime-config) · **model availability** · **local-only policy** — then the exact fix for EVERY active block: gate off → "AI execution is disabled. Set RMOOZ_ALLOW_SIM_RUN=1 and restart the server."; remote provider → "Free Fight is local-only. Current provider is &lt;x&gt;. Set RMOOZ_LLM_PROVIDER=ollama or remove remote provider env."; **BOTH shown when both block** (req #6). `_freeFightAiReady`/`_aiBlockReasons` combine all reasons. **Server:** `routeHealth` adds `configured_provider` (RAW — names the blocked provider; masked `provider` stays ollama); `/api/ai/models` adds `configured_provider`+`provider_blocked` so the **global model HUD** (`ai-model-hud.js`) shows "FF blocked (gate/remote provider)" — a selected model never implies Free Fight can run (req #7). **Generate:** AI Commander button warns ("⚠ … (blocked)") + banner when blocked (req #8); **Staff-Safe stays available + unwarned** (deterministic/no-LLM, req #9). `?v=gate-card-d`. Tests: **ai-gate-card-d 7/7** (server configured_provider/provider_blocked · gate-off fix · provider fix names provider · both · ready · Staff-Safe available when commander blocked); regressions green (model-selector 16 · zen-blocked 8 · llm-runtime-config 13 · ai-free-fight-ai-only 7 [msg updated to spec wording] · route-guard 49 · commander-loop 42 · card-visibility 7 · free-fight-ai 9 · pacing-c 5). `[[feedback_free_fight_local_only_security]]`, `[[project_llm_runtime_config_resolver]]`. |
| Free Fight AI operator-proof blocks (RMOOZ-AI-FREE-FIGHT-UX-PROOF-A) | ✅ | `client/shell/free-fight-demo.js` · `scripts/test-free-fight-ux-proof-a.js` · `docs/ai-free-fight-ux-proof-a.md` | **NEW 2026-06-16 (UI/diagnostics only — NO planner/COA/DB-Lite/terrain/readiness/Staff-Safe/model-selector/gate/guardrail change).** Makes the AI Commander Free Fight card understandable WITHOUT server logs by CONSOLIDATING data RMOOZ already produces into four leading operator blocks (the existing planning-trace/COA-cards/movement-debug stay below as detail). **(1) 🛰 AI Readiness** = `_aiGateStatusHtml()` (gate · provider · model-avail · local-only, from gate-card-d) + **Selected model** + post-gen **Plan source** + **LLM status** + verdict "✅ Movement came from the local LLM (plan_source=llm)" vs "⚠ Deterministic — the LLM did not produce this (not 'AI')". **(2) 🎯 AI Candidate Filter** = "sent **X** / total **Y** · excluded **Z**" + top exclusion reasons + "the AI reasoned over only X of Y" proof (from `planning_trace.input_understood.candidates`); shows a before-generation explainer too. **(3) ✅ AI Selected Units** — ~~per selected-COA action: display name(code) · country/loc · action_type · execution_mode · why_unit · target→final coord~~ **REMOVED 2026-06-16 at owner request (RMOOZ-SELECTED-UNITS-REMOVE)** — a long "AI moved N units" roster is noise, not commander insight; the COA cards + AI Planning Trace + map markers already convey what the AI did (`_aiSelectedUnitsHtml` + its test seam + ux-proof §5 deleted; ux-proof now **6/6**). **(4) ⊘ AI Non-Selected Units** = `non_selected_units` in a COLLAPSED `<details>` with per-unit reason. Event-log proof kept (`AI COA Applied … [llm]`, `EXECUTED: <unit> <action> from→to via <execution_mode>`). `?v=ux-proof-a`. Tests: **ux-proof-a 7/7** (candidate count · selected model · plan_source=llm only for real LLM · deterministic NOT labelled AI · selected why_unit+execution_mode · non_selected collapsed · before-gen explainer); regressions green (prefilter 7 · repair-loop 7 · gate-card-d 7 · pacing-c 5 · free-fight-ai 9 · card-visibility 7 · commander-narrative 45). DOM-render proof report `docs/ai-free-fight-ux-proof-a.md` (JPEG screenshot tool broken in this env → authoritative render-text evidence instead; card had a real-browser pass under TRACE-VERIFY-B). Minor intentional overlap: candidate count also appears in the planning-trace (detail). Pre-existing reds NOT mine (attack-plan-ai-only 6/5, coa-ui §15 — repair-loop-A "show Staff-Safe COAs" relaxation drift, confirmed via `git stash` baseline). `[[project_free_fight_ai_gate_card]]`, `[[project_ai_free_fight_candidate_prefilter]]`, `[[project_ai_commander_repair_loop]]`. |
| OpenRouter gated cloud mode (RMOOZ-OPENROUTER-QWEN35-CLOUD-MODE-A) | ✅ | `server/ai/openrouter-client.js` (NEW) · `server/ai/{ai-config,ai-provider,llm-runtime-config,model-selection,free-fight-coa-planner,free-fight-llm-capability-analyst,free-fight-llm-decision}.js` · `server/web-server.js` · `client/shell/{ai-model-hud,free-fight-demo}.js` · `scripts/{test-openrouter-cloud-mode-a,verify-openrouter-qwen-e2e}.js` · `docs/openrouter-cloud-mode.md` | **NEW 2026-06-16.** Owner-approved FIRST cloud egress: **OpenRouter** (OpenAI-compatible) as an EXPLICIT, GATED cloud mode for testing **Qwen3.5 397B A17B** — **default stays local-only** ([[feedback_free_fight_local_only_security]] honored: this is the "owner explicitly approves a cloud mode" event). **Three gates, ALL required:** `RMOOZ_ALLOW_SIM_RUN=1` + **new** `RMOOZ_ALLOW_CLOUD_AI=1` + `OPENROUTER_API_KEY` + provider=openrouter selected. **Guardrail is now cloud-aware** (planner/analyst/decision): `isRemoteProvider('openrouter') === !LLM_CFG.openrouterReady()` (`cloudAllowed()` = `RMOOZ_ALLOW_CLOUD_AI==='1'`; `openrouterReady()` also needs the key) — **`zen`/`claude`/`openai`/`auto` stay blocked UNCONDITIONALLY** (block-list unchanged). Backstop: `ai-provider.resolveProvider('openrouter')` throws when cloud off (covers `free-fight-llm-plan`). `openrouter-client.js` mirrors `zen-client` (`/chat/completions` + `/models`, Bearer + `HTTP-Referer`/`X-Title`, **key masked, never logged**); `ai-config.openrouter` ships **blank key + blank model**. Model selector: `model-selection` persists `{provider, model}` (un-clamps `openrouter` display only when cloud-ready; `selectedProviderRaw()` feeds `LLM_CFG.getProvider()` — **env provider precedence preserved over the runtime file** so the zen env-block tests stay deterministic); `/api/ai/models[?provider=openrouter]` lists the cloud catalog + `cloud_allowed`/`cloud_enabled`/`is_cloud`/`provider_blocked`; `POST /api/ai/model/select {provider,model}`. UI: header HUD **Provider dropdown (Ollama/OpenRouter)** (OpenRouter disabled until `RMOOZ_ALLOW_CLOUD_AI=1`), "☁ Cloud provider — data leaves local machine" / "Cloud AI disabled…" notes; the free-fight gate-card labels openrouter "☁ CLOUD (data leaves machine)" not "local". Candidate pre-filter still runs first (10–25 sent, provider-agnostic). plan_source=llm · provider_used=openrouter · model_used=selected. `.env.example`: blank `RMOOZ_ALLOW_CLOUD_AI=`/`OPENROUTER_API_KEY=`/`RMOOZ_OPENROUTER_MODEL=`/`OPENROUTER_URL=`. `?v=openrouter-a`. Tests: **openrouter-cloud-mode-a 9/9** (blocked-no-gate · blocked-no-key · model-list parse · selector persists · cloud-ready→provider/model/plan_source=llm · prefilter ≤25 before cloud · no key literal + .env blank · ollama default+zen blocked); **zen-blocked 8/8 still green**; regressions green (llm-runtime-config 13 · model-selector 16 · gate-card-d 7 · ux-proof 7 · repair-loop 7 · prefilter 7 · pacing-c 5 · free-fight-ai 9). Owner-run real-cloud E2E `verify-openrouter-qwen-e2e.js` (needs the key — NOT run by the agent). Pre-existing env reds NOT mine (local-llm-only §9 + route-guard — stale `runtime/ai-model-selection.json`; green when moved aside). `[[feedback_free_fight_local_only_security]]`, `[[project_llm_runtime_config_resolver]]`, `[[project_local_model_selector]]`. |
| Operator-friendly AI model flow (RMOOZ-AI-USER-FRIENDLY-MODEL-FLOW-A) | ✅ | `client/shell/free-fight-demo.js` · `scripts/test-ai-user-friendly-model-flow-a.js` (NEW) | **NEW 2026-06-16 (UI/UX only — NO planner/gate/security/model-selection-server change).** Owner: the AI card was "too complicated and too technical" (kept showing env/provider jargon). The **main AI Free Fight card** is now just **AI Model · Status · Select AI Model · Start** — "Choose model → Ready → Start AI Free Fight." **(1)** `AI_NO_MODEL_MSG` changed from "AI execution is allowed, but no local LLM/model is available…" → **"Choose an AI model to start."** **(2)** New `_modelFlowStatus()` collapses route-health + `/api/ai/models` into ONE operator status: `ready`→"Ready" · `needs_model`→"Needs model" (+ "No AI model found. Start Ollama or choose a cloud model." when none installed / "Your saved model is not available. Choose another model." when the saved one is missing but others exist) · `cloud_disabled`→"Cloud disabled" · `gate_off`→"Needs setup" (env-free) · `fast`→"Fast — AI off". **(3)** `_modelFlowHtml()` renders the simple block + one **"Select AI Model"** button (`ff-open-model-picker`) → `_modelPickerHtml()`: **local models first** (one-tap rows labelled "Local model"), **cloud models only when cloud mode is enabled** (labelled "Cloud model ☁" + **"⚠ data leaves this machine"**); a 🖥 Local / ☁ Cloud source switch (Cloud button hidden unless `cloud_enabled`). **NO env-variable names anywhere in the main card.** **(4)** `_maybeAutoSelectModel()` (req #4): saved-available → keep · exactly one installed local model → auto-select it · multiple installed + saved missing → leave for the operator (no silent swap, [[project_local_model_selector]] honored) · cloud never auto-picked. **(5) Advanced diagnostics** = a **collapsed `<details>`** (`_advancedDiagnosticsHtml()`) that now OWNS the technical signals — route probe + `Check route`, the 4-signal `_aiGateStatusHtml` (RMOOZ_ALLOW_SIM_RUN), RMOOZ_ALLOW_CLOUD_AI/cloud-ready, provider/model_available/plan_source, and the **raw model dropdown** (`renderModelSelectorHtml`, moved out of the group-demo header → ONE place to pick a model). **(6) Start is disabled** until `_freeFightAiReady().ok` (same single runtime gate). **Safety unchanged (req #9):** `RMOOZ_ALLOW_SIM_RUN` stays the only execution gate, OpenRouter stays cloud-test-only, candidate pre-filter intact, AI Free Fight still requires `plan_source="llm"`, no deterministic fallback movement. Tests: **ai-user-friendly-model-flow-a 22/22**; updated **ai-free-fight-ai-only-a** (no-model regex → new wording, 7/7) + **coa-route-json-guard §10** (route status now under Advanced, 49/49); regressions green (model-selector 16 · gate-card-d 7 · ux-proof 7 · repair-loop 7 · prefilter 7 · pacing-c 5 · free-fight-ai 9 · card-visibility 7 · openrouter 9 · continuous-loop 42 · coa-ui 29 · card-ux-clarity 43). Pre-existing reds NOT mine (confirmed at HEAD by stash): attack-plan-ai-only 6/5 + mcp-prompt 8/2 (repair-loop Staff-Safe-COA relaxation drift) + free-fight-demo-a 15/1 (phases). `[[project_local_model_selector]]`, `[[project_ai_execution_single_gate]]`, `[[project_free_fight_ai_gate_card]]`. |
| AI model-ready state bugfix (RMOOZ-AI-MODEL-READY-STATE-A) | ✅ | `client/shell/free-fight-demo.js` · `scripts/test-ai-model-ready-state-a.js` (NEW) | **NEW 2026-06-16 (bugfix — no new UI, no new diagnostics).** The card did not flip to **Ready** after selecting an available model. **Root cause:** `_freeFightAiReady()`/`_modelFlowStatus()` key on `_routeHealth.model_available`, but `_selectModel()` left `_routeHealth` STALE and only refreshed it via an async route-health probe — so Ready appeared late, or never if the probe was slow/flaky/hung. Server side was already correct (selecting an installed model → `routeHealth.model_available:true`, verified live). **Fix:** `_selectModel()` now folds the fresh model state from the authoritative `/api/ai/model/select` response (which carries gate + provider + `model_available` + cloud flags for the NEW selection) straight into `_routeHealth` via `_reconcileRouteHealthFromModelInfo()` **before** the immediate `updatePanel()`, so the card flips to Ready on that render — independent of the probe; the probe then re-confirms (idempotent — same local `/api/tags` check). `_probeRouteHealth()` now returns its promise and its catch no longer clobbers a known-good health (a transient probe error can't drop the card out of Ready). `_freeFightAiReady()`'s contract is UNCHANGED (still reads `_routeHealth`), so Ready still means: gate on + provider not blocked + `model_available===true` (local: Ollama has it; cloud: cloud enabled + key + slug available). Missing/uninstalled model → stays **Needs model** ("Choose an AI model to start." / "Your saved model is not available. Choose another model." / "No AI model found. Start Ollama or choose a cloud model."), Start disabled. No env names in the main card. Tests: **ai-model-ready-state-a 7/7** (select→Ready+Start enabled · Ready is immediate with the probe deferred · saved-missing→Needs model+disabled · no-models→simple message · cloud-ready→Ready "Cloud model" · cloud-blocked→"Cloud disabled" · simple block env-free); regressions green (model-flow 22 · ux-proof 6 · gate-card-d 7 · model-selector 16 · ai-free-fight-ai-only 7 · continuous-loop 42 · card-visibility 7 · card-ux-clarity 43 · coa-ui 29 · repair-loop 7 · prefilter 7 · openrouter 9). `coa-route-json-guard` §7/§8 are an **env-only** red (machine-local gitignored `runtime/ai-model-selection.json` pins a non-default model → 49/49 when moved aside; not code). **Follow-up (2026-06-16, `?v=model-ready-state-a2`): the SECOND ready path.** The `_selectModel` fix only covered the in-card picker; a model picked in the **always-on header HUD** (or any non-card surface) fires `rmooz:ai-model-changed`, and the card's consumer only called `_fetchModels()` — which refreshed `_modelInfo` but NOT `_routeHealth` (the signal readiness reads) → the card stayed "Needs model". Fix: `_fetchModels()` now `_reconcileRouteHealthFromModelInfo()` on the DEFAULT listing (a provider PREVIEW `?provider=` does not move readiness), and the consumer (`_onExternalModelChanged`, extracted + seam) also `_probeRouteHealth()`s. Proven against a LIVE gated server: both the in-card select AND the header-HUD event flip the card to Ready. Tests: **ai-model-ready-state-a 9/9** (+§8 HUD-event→Ready, +own-echo-ignored). `?v=model-ready-state-a2`. `[[project_ai_user_friendly_model_flow]]`, `[[project_local_model_selector]]`. |
| OpenRouter setup + local-slug diagnostic (RMOOZ-OPENROUTER-SETUP-AND-AI-DEMO-H) | 🟡 | `client/shell/free-fight-demo.js` · `scripts/test-ai-model-ready-state-a.js` | **NEW 2026-06-16 (diagnostics only — NO planner/COA/gate/ai-config/Staff-Safe change).** Goal was to bring up OpenRouter cloud mode for the AI Commander demo + verify E2E. **BLOCKED on the key (req #8):** `ai-config.openrouter.apiKey` **hasKey=false** (no `OPENROUTER_API_KEY` env, and the gitignored `ai-secrets.local.js` — present — carries no OpenRouter key), so `cloudAllowed()/openrouterReady()=false`. That is the root of the prior `HTTP 401 Missing Authentication header` — the server has no key to send. **No server was started claiming OpenRouter and no key was committed.** **Verified without the key:** local mode genuinely works (`provider:ollama`, `selected:qwen3-coder:latest`, `model_available:true`, 2 models installed); **Staff-Safe intact + deterministic** (`plan_source:deterministic_coa_fallback`, `llm_called:false`, 3 COAs); tests green (ai-config-guard-f 15 · cross-platform-run-scripts-g 10 · openrouter-cloud-mode-a 9 [mocked] · repair-loop 7 · pacing-c 5); real-key `verify-openrouter-qwen-e2e.js` correctly **skipped**. **Code delivered = the nice-to-have diagnostic (req #17):** when provider is OpenRouter (cloud) but the selected model is a **local-style Ollama slug** (`_looksLocalModel` = no `vendor/` prefix) and unavailable, `_modelFlowStatus` shows **"This is a local Ollama model. Choose an OpenRouter model from the OpenRouter list."** instead of the generic "choose another"; a genuine missing *cloud* slug still gets the generic message. Stays env-free in the main card. Tests: **ai-model-ready-state-a 11/11** (+§9 local-slug-on-cloud + cloud-slug-missing). `?v=openrouter-setup-h`. Runtime selection left as the valid local `qwen3-coder:latest` (req #6 clear-condition not met — provider is not openrouter). **TO FINISH (owner):** put the key in env (`OPENROUTER_API_KEY`) or `ai-secrets.local.js` `{openrouter:{apiKey:'…'}}`, then start with `RMOOZ_ALLOW_SIM_RUN=1 RMOOZ_ALLOW_CLOUD_AI=1 RMOOZ_LLM_PROVIDER=openrouter`, pick a `vendor/model` slug from the live `/api/ai/models?provider=openrouter` list. `[[feedback_free_fight_local_only_security]]`, `[[project_ai_user_friendly_model_flow]]`. |
| Free Fight control-window cloud-state fix (RMOOZ-OPENROUTER-FREE-FIGHT-CONTROL-FIX-I) | ✅ | `client/shell/free-fight-demo.js` · `server/web-server.js` (`/api/ai/model/reset`) · `scripts/test-ai-model-ready-state-a.js` | **NEW 2026-06-16 (UI/diagnostics + a reset route — NO combat/COA/terrain/movement/Staff-Safe planner change).** The Free Fight card showed "Choose an AI model to start" even when OpenRouter was **active and ready** in the running server. **Root cause:** route-health `model_available` is **null for cloud** (the probe only knows ollama `/api/tags`), and `_modelFlowStatus` read `null` as "unavailable". **Fix:** `_modelAvailableEffective()` — route-health is authoritative for LOCAL; for CLOUD (null) it folds in `/api/ai/models` (which DOES compute cloud availability). Used by `_modelFlowStatus` + `_freeFightAiReady` → card now shows **Ready** when openrouter + the slug is in the catalog (proven live against the running OpenRouter server: `state:ready`, `Cloud model`). **Precise diagnostics** (`_modelFlowStatus` uses the RUNNING server's `configured_provider`): **#6** ollama+uninstalled → `Local Ollama model "<m>" is not installed. Pull this model or choose an installed local model (<N> installed locally).`; **#7** cloud intended but server is ollama → `OpenRouter is not active in the running server. Restart with RMOOZ_LLM_PROVIDER=openrouter and RMOOZ_ALLOW_CLOUD_AI=1.`; **#8** openrouter+no-key → `OpenRouter key is not loaded in the running server. Add OPENROUTER_API_KEY or gitignored ai-secrets.local.js and restart.`; **#9** openrouter+local-slug (from -H). **#11 Reset AI Selection** button → new `POST /api/ai/model/reset` (`modelSelection.clearSelection()` — forget the runtime file → env/default) + re-fetch/re-probe. **#16** post-generation auth/401 → readiness note `OpenRouter rejected the request (HTTP 401) — the API key is invalid or was not sent. Rotate/reload…`. **#12** Staff-Safe stays available while AI Commander blocked (verified). **Real 401 diagnosed (#16):** the key in `ai-secrets.local.js` is **invalid/malformed** (len 37, not `sk-or-…`); `/models` is public so it listed 336 models + `openrouterReady()` only checks presence → card can be Ready pre-flight, the 401 surfaces at Generate (→ the new auth note). Tests: **ai-model-ready-state-a 16/16** (+§10 cloud-null→Ready, §11 #7, §12 #8, §13 Reset POSTs /model/reset, §14 Staff-Safe-while-blocked); model-flow-a 22 (#6 msg updated), ux-proof 6, gate-card-d 7, model-selector 16, ai-free-fight-ai-only 7, continuous-loop 42, openrouter-cloud-mode 9, ai-config-guard 15, cross-platform-run-scripts-g 10. **Env-only reds (NOT code, machine-local gitignored `runtime/ai-model-selection.json` recreated by a concurrent writer → provider openrouter + invalid key):** repair-loop/pacing-c/gate-card-d-server/cross-platform-live all **green when the runtime file is moved aside**. `?v=ff-control-fix-i`. `[[project_ai_user_friendly_model_flow]]`, `[[feedback_free_fight_local_only_security]]`. **Follow-up (`?v=ff-control-fix-i2`): pre-flight malformed-key warning.** Because OpenRouter `/models` is public, a present-but-INVALID key still listed 336 models + `openrouterReady()` (presence-only) said cloud-ready → the card said "Ready" then 401'd at Generate. New `openrouterClient.keyLooksValid()` (format heuristic `^sk-or-(v1-)?…`, boolean — never the key) → `/api/ai/models` adds `key_format_ok`; `_modelFlowStatus` (cloud, not blocked, `key_format_ok===false`) → `cloud_disabled` "OpenRouter key looks invalid (it should start with sk-or-). Replace it with a valid key and restart."; `_freeFightAiReady` also disables Start (`code:'bad_key'`). Tests: **ai-model-ready-state-a 18/18** (+§15 malformed→warn+Start-disabled, valid-format→Ready). **⚠️ SECURITY (guard fired):** a concurrent writer had inserted a real `sk-` key into the COMMITTED defaults of `server/ai/ai-config.js` (HEAD clean=0, working tree=1) — `test-ai-config-guard-f` caught it; **reverted `ai-config.js` to HEAD (secret removed, never committed; key belongs in gitignored `ai-secrets.local.js`)**, guard 15/15 again. `[[feedback_free_fight_local_only_security]]`, `[[feedback_shared_working_tree_concurrency]]`. |
| AI Commander speed architecture (RMOOZ-AI-SPEED-ARCHITECTURE-J) | 🟡 | `server/ai/{free-fight-coa-planner,openrouter-client}.js` · `client/shell/free-fight-demo.js` · `.env.example` · `scripts/test-ai-speed-architecture-j.js` (NEW) | **NEW 2026-06-16 (perf/diagnostics/prompt/caching/OpenRouter — NO combat/movement/COA-validation/DB-Lite/terrain/readiness/Staff-Safe change).** Diagnosis: a real turn pays for **two** LLM round-trips — the capability ANALYST (LLM-backed, the per-turn bottleneck) + the commander LLM — on top of fast (~1ms) deterministic spans (intel/terrain/pack/coa/validation/brief). **Phase 3 (biggest win) — capability-profile cache:** module-level `_capCache` keyed by OOB capability fields (id\|side\|platform\|type\|equipment) + side + analyst-mode + model + objective — **position-INDEPENDENT so it survives units moving**; a HIT makes `analyze_unit_capabilities_ms` ~0 on every repeat turn. `debug_timing.cap_cache` hit/miss + `cap_cache_hits/misses` counters; invalidates on OOB/equipment/objective/side change; `RMOOZ_AI_PERF_CACHE=0` disables. (Terrain cache intentionally SKIPPED — ~1ms + position-dependent approach axis → caching by objective would stale it.) **Phase 1.1 — OpenRouter `provider.sort`** (default **latency** for speed; `RMOOZ_OPENROUTER_PROVIDER_SORT=latency\|throughput\|price\|none`). **Phase 1.3 — mode-based output caps** (fast 800 / normal 1200 / deep 2500, was hardcoded 2500; per-tier `RMOOZ_AI_MAX_OUTPUT_{FAST,NORMAL,DEEP}`; deep keeps the full COA budget). **Phase 4 — strict `json_schema`** response_format when a schema is supplied (`RMOOZ_OPENROUTER_STRUCTURED_OUTPUT`, default on) with **self-heal fallback to `json_object`** on a response_format error; the planner ships the COA schema only when `RMOOZ_OPENROUTER_COA_SCHEMA=1` (default OFF until validated on a live key). **Diagnosis UI (#3):** `_coaTimingHtml` → **"AI Performance Breakdown"** (all spans + LLM/repair ms + capability cache hit/miss + provider/model + output tokens). **Before/after:** capability span 14ms→**0ms** across turns (cache hit) in the deterministic demo; with a valid key the analyst's multi-second LLM call → 0 on every repeat turn. Tests: **ai-speed-architecture-j 11/11** (provider.sort, token limits by depth, schema passed+gated, cache miss→hit-survives-movement + equipment-invalidates + disable, debug_timing spans, breakdown UI); regressions green (coa-planner 71 · coa-performance 9 [drift-fixed] · capability-driven 24 · repair-loop 7 · pacing-c 5 · prefilter 7 · tool-contract 41 · movement-execution 11 · local-llm-only 66 · ai-config-guard 15 · cross-platform-run-scripts-g 10 · openrouter-cloud-mode 9 · ai-model-ready-state 18). `?v=speed-arch-j`. **DEFERRED to a follow-up (transparency):** Phase 1.2 streaming (SSE client+server), Phase 2 "AI Quick Move" (compact single-action request + delta context), Phase 5 Staff-Safe preview-then-upgrade UX — each is a larger client/async surface; scoped next. Fixed a pre-existing drift: `test-coa-performance-a` §7 stale fallback-message wording (changed by concurrent `4af0fdc`). `[[project_coa_planner_performance]]`, `[[project_ai_user_friendly_model_flow]]`. |
| Offline inference — keep-alive/warmup/benchmark (RMOOZ-OFFLINE-AGENT-ARCHITECTURE-P · Phase 1) | ✅ | `server/ai/{ollama-client,llm-runtime-config}.js` · `server/web-server.js` · `scripts/run-rmooz-app.js` · `client/shell/free-fight-demo.js` · `test-offline-agent-architecture-p.js` (NEW) | **NEW 2026-06-18 (Phase 1 of P — local-inference SPEED only; NO agent/scheduler/blackboard rewrite).** Audit verdict: the "agent council" mostly ALREADY exists — deterministic validator (`validateCoaPlan`) · deterministic terrain (`tactical-terrain-context`) · capability cache (`_capCache`, -J) · NO-LLM normal ticks (COA Commitment, -L) · performance governor (`ai_depth` fast/normal/deep) · candidate-prefilter · `debug_timing` — so Phases 2–5 (formal blackboard/scheduler, prompt-compression rework, llama.cpp/vLLM adapters) were DEFERRED, not rebuilt. Shipped: **keep_alive** (`RMOOZ_LLM_KEEP_ALIVE`, launcher default **8h**) + **num_ctx** (`RMOOZ_OLLAMA_NUM_CTX`, opt-in — never force-truncates the COA prompt) now threaded into EVERY Ollama call centrally via `applyOllamaOptions`; **`extractTimings()`** turns Ollama ns stats → ms + tokens/sec + cold/warm (load<200ms); **POST/GET `/api/ai/warmup`** (empty-message preload + keep_alive) + **`/api/ai/benchmark`** (one timed gen → load/eval ms, tok/s, num_ctx, wall) both gated by `RMOOZ_ALLOW_SIM_RUN`; **Advanced-diagnostics "Local inference" panel** (warmup + benchmark buttons + readout). **LIVE on qwen3-coder:latest:** COLD wall **9313ms** (load 9228ms) → WARM **110ms** (load 86ms · 185 tok/s) — **~85×** via keep_alive. Tests: **offline-agent-architecture-p 7/7**; FF suite green. `?v=offline-arch-p`. `[[project_ai_model_ready_live]]` |
| Blue/Red/Green/White roles — GREEN neutral-world + summarizer scaffold (RMOOZ-BLUE-RED-GREEN-WHITE-A) | ✅ | `server/ai/green-world.js` (NEW) · `server/ai/green-summarizer.js` (NEW) · `server/wargame-sim-bridge.js` · `server/ai/llm-runtime-config.js` · `scripts/run-rmooz-app.js` · `test-blue-red-green-white-a.js` (NEW) | **NEW 2026-06-18.** Adopts the classic war-game colour model. Audit verdict: Blue (side-agnostic planner), Red (planner per-side + `red-team-agent`), White (deterministic `validateCoaPlan` + WS3 `/api/sim/decide` + commit/journal) ALREADY exist — only GREEN was missing. **GREEN = `green-world.assessNeutralWorld`** — PURE, sync, **NO LLM, NO mutation** (like tactical-terrain-context): from units + objective + terrain ctx it derives population_band, collateral_risk {band,score,drivers}, road_status, infra_status, host_nation, neutral_reaction_score (0-100), with HONEST provenance (inferred_terrain_class / geometric / absent — no census/road/infra layer). Read-only endpoint **POST/GET `/api/wargame-sim/free-fight/neutral-world`** (deterministic, UNGATED — no model). **Summarizer = `green-summarizer.js`** scaffold: OPTIONAL small-model note (turn-delta / why-blocked / civilian-reaction), **OFF unless `RMOOZ_LLM_SUMMARIZER=1` AND `RMOOZ_ALLOW_SIM_RUN=1`**; small-output cap; NEVER plans; ALWAYS deterministic fallback; model via `llm-runtime-config` task **summarizer** (`RMOOZ_LLM_MODEL_SUMMARIZER`, e.g. llama3.2:3b). **One main planner:** launcher now defaults **`RMOOZ_LLM_MODEL=qwen3-coder:latest`** (was the uninstalled ai-config qwen2.5:7b) → card Ready out-of-box (live: `/api/ai/models` selected=qwen3-coder, model_available true). **Live GREEN:** urban + forces-near-objective → collateral 83/100, roads constrained, reaction 85/100, `llm_used:false`. NOT wired into the hot path — normal ticks stay no-LLM. DEFERRED: formal blackboard/scheduler, llama.cpp/vLLM. Tests: **blue-red-green-white-a 8/8**; FF suite green. `[[project_offline_agent_architecture]]` |
| Green World UI + map overlay (RMOOZ-GREEN-WORLD-UI-R) | ✅ | `client/shell/free-fight-demo.js` · `server/ai/green-world.js` · `client/app.html` · `test-green-world-ui-r.js` (NEW) | **NEW 2026-06-18.** Surfaces the deterministic GREEN layer in the Free Fight card (under Advanced controls) + a toggleable map ring — **NO combat/movement/White-validation/DB-Lite/terrain/readiness/Staff-Safe/COA-Commitment/LLM-scheduling change.** Panel `_greenWorldHtml` shows civilian/collateral risk · road status · infrastructure · host-nation pressure · neutral-reaction score · provenance · deterministic note (summarizer off) + a "Green JSON" view + **Refresh Green World** + map-ring toggle. `_refreshGreenWorld` hits ONLY `/neutral-world` (deterministic, ungated) — **NEVER /plan-coas, NEVER the LLM**; debounced (`_greenBusy`); bails without objective/units. Auto-refresh hooks: after deep plan (guarded on `plan.ok`) · after commit · on phase advance (inside `_coaExecTick` — `llm_called_this_tick` stays false) · on replan trigger · manual. Map ring = its OWN Leaflet layerGroup (review-only, `interactive:false`), coloured low/med/high, **default OFF**. Event log: "Green World assessed" + collateral/road/infra change lines. `green-world` road logic refined to honest **"unknown"** without real route data. **LIVE:** panel renders high (83/100)/constrained/Atropia/85, network shows `POST /neutral-world` and **no `POST /plan-coas`**, ring drawn on the real map. Tests: **green-world-ui-r 8/8**; existing **coa-commit-execution-l / persistence-m / live-delay-audit-n** + simple-operator-ux-o + blue-red-green-white-a all green. `?v=green-world-ui-r`. **DEFERRED (explicitly out of scope):** wiring Green into White adjudication; the summarizer note in the UI (needs a gated endpoint). `[[project_offline_agent_architecture]]` |
| Scheduler/agent decision log (RMOOZ-AI-SCHEDULER-DECISION-LOG-S) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-ai-scheduler-decision-log-s.js` (NEW) | **NEW 2026-06-18 — audit/transparency ONLY, record-only.** NO combat/movement/White-adjudication/Green-output/terrain/readiness/Staff-Safe/COA-Commitment/LLM-scheduling change; the log NEVER calls the LLM and adds NO model/network calls (every record is a sync in-memory push). Record shape `{ ts, tick, role(blue\|red\|green\|white\|performance\|unit-controller), action, called_llm, provider, model, duration_ms, reason, skipped_reason, source, result_summary }`; bounded buffer (cap 200) via `_recordDecision` / `_recordPlanDecision` / `_clearDecisionLog`. Recorded at: Blue/Red commander plan + performance-governor depth + White validation (`_recordPlanDecision` in `_generateCoaPlan`), Green refresh (role=green, no-LLM, in `_refreshGreenWorld`), White replan trigger (`_pauseForReplan`), unit-controller on every committed tick (`_coaExecTick`, called_llm=false). UI in Advanced diagnostics `_decisionLogHtml`: latest 10 · colour-coded role badge · LLM/no-LLM flag · duration · reason/skipped + **Clear decision log**. **LIVE:** 6 roles rendered (red deep_plan LLM/9213ms · green no-LLM/22ms · white validate/replan · unit-controller · performance), real Clear empties (6→0), no `/plan-coas` introduced. Tests: **ai-scheduler-decision-log-s 7/7**; green-world-ui-r + COA commit/persistence/live-delay + simple-ux green. `?v=sched-decision-log-s`. **DEFERRED:** per-turn loop (`runNextTurn`) records; log persistence. `[[project_offline_agent_architecture]]` |
| White advisory from Green (RMOOZ-WHITE-GREEN-ANNOTATION-T) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-white-green-annotation-t.js` (NEW) | **NEW 2026-06-18 — White reads Green as a DETERMINISTIC ADVISORY annotation; NEVER a gate.** NO change to `validateCoaPlan` (stays structure/physics-only per `[[feedback_validator_structure_physics_only]]`), combat/movement, Green output, COA execution, or LLM scheduling. `_whiteAdvisory(green)` (pure, no-LLM): worst-of collateral band + neutral-reaction band → `advisory_level` clear\|caution\|restricted, **`gate:false` ALWAYS**. Surfaced in the Green panel ("⚖ White advisory: <level> … Advisory only — not a block; validator unchanged (structure/physics)") and recorded as a White decision (role=white, action=`adjudication_advisory`, called_llm=false, source=`green-world→white`) — only for a COMMITTED COA and only on level CHANGE (dedup; `_whiteAdvisoryLevel` reset on each commit). Never sets `replan_required`/`paused`/`blocked`. **LIVE:** committed COA → White advisory **restricted** (collateral high 83 + reaction 85), decision logged no-LLM, exec NOT gated (`replan_required=false`, `paused=false`, `status=pending`). Tests: **white-green-annotation-t 5/5**; green-ui + scheduler-log + COA commit/persistence/live-delay + simple-ux green. `?v=white-green-annotation-t`. **DEFERRED:** feeding the advisory into the server commit/journal annotation. `[[project_offline_agent_architecture]]` |
| Green→White advisory scoring (RMOOZ-GREEN-WHITE-SCORING-T) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-green-white-scoring-t.js` (NEW) | **NEW 2026-06-18 — Green scored into White's review as ADVISORY input; NEVER invalidates a COA.** NO change to movement/combat/kill-prob/route-clamp/terrain/DB-Lite/readiness/Staff-Safe or the structure/physics validator. `_greenAdvisoryScoring(green)` (pure, no-LLM, no-fetch) → `green_advisory { considered, collateral_risk_band, neutral_reaction_score, advisory_score_delta, warnings, recommendations, provenance }`; delta **low 0 / medium -5 / high -15** (worst-of collateral + neutral-reaction); absent/inferred provenance → "low confidence / inferred" warning. `_applyGreenAdvisoryScoring` composes it onto the CLIENT White review (`_coaPlan._green_advisory`) after the post-plan Green refresh — `validation.ok` untouched. Decision log: role=white · action=`green_advisory_scoring` · called_llm=false · reason "green risk considered"; event log "White advisory: Green collateral risk <band>; score adjusted -X". UI: Green panel "⚖ White considered Green risk: <band> · score Δ <delta> · <recommendation>" + warnings, labelled "advisory only — not a block; validator unchanged". **LIVE before/after:** validation `{ok:true}` unchanged + `green_advisory` added (high, Δ-15); decision/event logged; no LLM, no /plan-coas. Tests: **green-white-scoring-t 5/5**; annotation-t + green-ui + scheduler-log + COA commit/persistence/live-delay + simple-ux green. `?v=green-white-scoring-t`. **DEFERRED:** server-side journal annotation; folding the delta into an actual COA ranking score. `[[project_offline_agent_architecture]]` |
| COA ranking with Green/White advisory (RMOOZ-COA-RANKING-WITH-ADVISORY-U) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-coa-ranking-with-advisory-u.js` (NEW) | **NEW 2026-06-18 — Green/White advisory is now an INPUT to COA ranking/recommendation; advisory-only, NEVER invalidates or blocks Run.** Resolves the -T deferred "fold the delta into a ranking score." NO change to movement/combat/kill-prob/`validation.ok`/Staff-Safe/DB-Lite/terrain/readiness or LLM scheduling. `_rankCoas(plan, green)` (pure, no-LLM, no-fetch) → per-COA `{ base_score, tactical_score, readiness_score, terrain_score, green_advisory_delta, final_score, ranking_reason, ranking_components }`. Per-COA green penalty = plan `advisory_score_delta × exposure` (0.3 standoff … 1.0 full-commit), exposure = `_coaApproachIntensity` (share of MOVE targets within 8km of the objective) — so a lower-tactical, lower-exposure COA can out-rank a high-exposure assault when Green risk is high. `_applyCoaRanking` attaches `coa._ranking` + `_coaPlan._ranking_recommended_idx`, re-selects the default (operator may still pick any), and `_pickRecommendedIdx` honours it. `validation.ok` untouched; Run never blocked. Decision log role=performance · action=`coa_ranking_with_green_advisory` · called_llm=false (recommended COA + Δ + final). Event log "COA ranking updated: Green/White advisory adjusted COA-X by -Y." UI: per-card score breakdown (base + tac + green) + "⚖ Green/White advisory affected ranking (Δ)" + "Recommended because: …"; the card "Recommended" badge follows the ranking. **LIVE before/after:** no-green COA-A 70 (rec) → high-green COA-A **55** vs COA-B **62** → recommendation FLIPS A→B; `validation.ok` stays true. Tests: **coa-ranking-with-advisory-u 6/6**; scoring-t + annotation-t + green-ui + scheduler-log + COA commit/persistence/live-delay + simple-ux + attack-plan-ai-only/mcp + coa-ui/variety green. `?v=coa-ranking-advisory-u`. **DEFERRED:** server-side journal annotation; real per-COA readiness/terrain score layers (0 when absent today). `[[project_offline_agent_architecture]]` |
| Advisory commit journal/context (RMOOZ-ADVISORY-COMMIT-JOURNAL-V) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-advisory-commit-journal-v.js` (NEW) | **NEW 2026-06-18 — persistence/annotation ONLY; NO movement/combat/validation/scoring/LLM/Staff-Safe/execution change.** On COA commit, `_buildCommitAdvisoryContext(i)` (pure, no-LLM, no-fetch) snapshots the decision context into `active_coa_execution_state.commit_advisory_context`: `{ considered, selected_coa_id, recommended_coa_id, was_recommended_selected, operator_override, selected_coa_ranking, recommended_coa_ranking, green_advisory, green_world_summary, white_advisory_summary, decision_log_snapshot(latest 10), recorded_at }`. Persists/restores via existing `_persistCoaExec`/`_restoreCoaExec` (whole-object sessionStorage, scenario-keyed). Degrades honestly to `{ considered:false, reason:'no advisory context available' }` when no ranking/green exists — commit still works. Event log: "Committed recommended COA-X…" or "Operator override: committed COA-Y instead of recommended COA-X…". Decision log role=white · action=`commit_advisory_context_recorded` · called_llm=false (sel/rec/override). UI in the COA Commitment block: "Committed COA advisory context" — Recommended/Selected · Operator override yes/no · Green/White Δ on selected · warnings/recommendations. **LIVE (override):** committed COA-A vs recommended COA-B → `operator_override:true`, context persisted ✓, decision/event logged, no /plan-coas, tick `llm_called_this_tick:false`. Tests: **advisory-commit-journal-v 7/7**; ranking-u + scoring-t + green-ui + scheduler-log + COA commit/persistence/live-delay + simple-ux green. `?v=advisory-commit-journal-v`. **DEFERRED:** writing the context into the SERVER `/api/sim/commit` journal (this lives in the client COA-exec state + sessionStorage). `[[project_offline_agent_architecture]]` |
| Free Fight control window rebuild — tabbed (RMOOZ-FREE-FIGHT-CONTROL-WINDOW-REBUILD-W) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-control-window-rebuild-w.js` (NEW) · `test-free-fight-simple-operator-ux-o.js` (test G updated) | **NEW 2026-06-18 — UI/UX ONLY; NO logic/AI/COA-exec/Green/White/scoring/ranking/scheduling/validation/Staff-Safe/combat change.** `renderAiDecisionHtml` rebuilt as a 5-tab control window — **Operator · COA Plans · Green · White · Diagnostics** — each tab REUSES an existing render fn. Standard tabbed pattern: **ALL panels render into the DOM** (full card stays inspectable → existing tests pass unchanged), only the active one is shown (`display:none` on the rest); `_ffTab` state + `ff-tab-*` binds. **Operator** = `_operatorStripHtml` + `_operatorSummaryHtml` (≤1 primary action; recommended/selected summary; NO raw/Green JSON, benchmark, or scheduler log in the Operator panel). **COA Plans** = `renderCoaPlanHtml` (score breakdown + Recommended-because + override). **Green** = `_greenWorldHtml` (risk/road/infra/host/reaction/provenance + map toggle + refresh). **White** = NEW `_whiteTabHtml` (validation.ok + White advisory + Green/White scoring + warnings/recs + "advisory only — not a block"). **Diagnostics** = `renderCommanderLoopHtml` (model status/Reset-AI + warmup/benchmark + decision log) + manual planner + commit-exec + unit-decision (Staff-Safe lives here). Collapsible **System layers** summary (Blue/Red/Green/White/Unit-Controller). Guarantees verified live: Run/committed-tick make no /plan-coas + `llm_called_this_tick=false`; Green refresh no /plan-coas. Tests: **control-window-rebuild-w 7/7** + simple-ux test G updated to tabs; full FF suite (44) + scripts COA (commit/persistence/live-delay) green. `?v=control-window-rebuild-w`. **⚠️ SUPERSEDED for the operator-facing path by RMOOZ-FREE-FIGHT-CONTROL-HARD-RESET-X (row below):** the owner ruled the tabbed "render-all-into-DOM-hidden" pattern still confused operators and left hidden duplicate `data-act`. `renderAiDecisionHtml` (the 5-tab card) is UNCHANGED but now renders ONLY inside the closed Diagnostics/Legacy drawer of the V2 cockpit; its 7/7 test still passes via the component seam. `[[project_offline_agent_architecture]]` |
| Free Fight control HARD RESET — V2 cockpit (RMOOZ-FREE-FIGHT-CONTROL-HARD-RESET-X) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-control-hard-reset-x.js` (NEW, 13/13) · 8 legacy-layout tests open the drawer (`_setFfLegacyOpenForTest(true)`) | **NEW 2026-06-19 — "Keep the engine. Rebuild the cockpit." UI/UX ONLY; ZERO backend change (planner / COA-exec / deterministic ticks / movement physics / validation / Green / White advisory / ranking / Staff-Safe / LLM provider+model config / warmup+benchmark endpoints / DB-Lite+terrain+readiness+supply all UNTOUCHED).** The operator-facing path is a brand-new **`renderFreeFightControlV2`** that renders **ONLY the active state's view** with **unique `v2-*` data-act ids** (NO hidden duplicates competing for clicks — the root cause of "clicks don't act"). Explicit state machine `_freeFightControlStateV2()`: **empty / planning / ready / committed / running / paused / blocked / complete** + a state badge. Per-state controls (≤2 primary visible): empty→Generate AI Plan (primary, "slow, calls AI") + Staff-Safe (secondary) + model-readiness line; planning→spinner + elapsed (no fake cancel); ready→clickable COA cards (recommended ★ first-class, **selected card highlighted** `data-ff-v2-selected`, **Commit Selected Plan (COA-X)** + operator-override note + Regenerate under Advanced); committed→**Run Plan** + Clear + selected/recommended/override + **Green/White advisory summary "advisory only — not a block"**; running→Pause + phase progress + "no AI on normal ticks"; paused→Resume + Clear; blocked→reason + **Continue anyway** + **Replan with AI under explicit Advanced action**; complete→Generate + Clear. **COA cards:** click `v2-coa-<i>` → `_coaSelectedIdx=i` + repaint → highlight + summary + commit label update IMMEDIATELY; Commit commits the SELECTED COA (not recommended unless selected). **Non-working group-movement-demo controls REMOVED from the operator path.** The entire old crowded UI (objective placement + group demo + the full 5-tab diagnostics card incl. model status / warmup / benchmark / scheduler decision log / raw JSON / manual planner / commit-exec detail / unit-decision / Staff-Safe internals) is PRESERVED but inserted into the DOM **ONLY when the closed "Diagnostics / Legacy" drawer is opened** (`_ffLegacyOpen`, default false) — so the default body has no legacy markers at all. `updatePanel` body = `renderFreeFightControlV2() + _freeFightLegacyDrawerHtml(html)`; binds via `bindFreeFightControlV2()` (legacy binds stay, no-op when closed). **LIVE browser proof (auth-stub, autoPort):** empty=3 unique actions/no leakage; real DOM click on COA-2 card → selected 0→1 + highlight + commit label COA-1→COA-2 + override note (clicks act ✓); committed shows restricted advisory Δ-15/reaction 85 "advisory only"; Run → **zero network calls (no /plan-coas)**, `llm_called_this_tick=false`; blocked shows reason + Continue-anyway + Advanced Replan; drawer opens→all legacy content appears; **no console errors**. Tests: **control-hard-reset-x 13/13**; full FF suite **44/44** (8 legacy-layout tests updated to open the drawer where their content now lives) + scripts COA (commit/persistence/live-delay) green. `?v=control-hard-reset-x`. **Caveat:** V2 lives in `free-fight-demo.js` (a new file can't reach the engine's closure scope); the planning state has no Cancel (the existing generator supports none). `[[project_offline_agent_architecture]]` |
| Free Fight V2 operator walkthrough (RMOOZ-FREE-FIGHT-V2-OPERATOR-WALKTHROUGH-Y) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-v2-operator-walkthrough-y.js` (NEW, 10/10) · `test-free-fight-control-hard-reset-x.js` (#6 copy updated) | **NEW 2026-06-19 — usability follow-up to -X; UI GUIDANCE ONLY, no new AI features, ZERO backend change (planner/COA-exec/ticks/physics/validation/Green/White/ranking/Staff-Safe/LLM-config/DB-Lite/terrain/readiness/supply all untouched).** Adds three additive pieces inside `renderFreeFightControlV2` (no new `data-act`, no new actions): **(1) a compact "WHAT TO DO NOW" stepper** — `_v2StepperHtml`: 5 steps (Generate → Select COA → Commit → Run → Pause/Resume) with done(✓)/current/todo/blocked status that tracks all 8 states (`_v2StepStatus`/`_v2StepperCurrentSet`: empty→1 · planning→1 · ready→2,3 · committed→4 · running→4 · paused→5 · blocked→4-as-`blocked` · complete→5); `data-ff-v2-step="N"` + `data-ff-v2-step-status`. **(2) per-button microcopy** — `_v2MicrocopyHtml`: the exact owner phrases ("⚡ Generate AI Plan — slow — calls AI" / "✅ Commit Selected Plan — locks the selected COA" / "▶ Run Plan — fast — no AI on normal ticks" / "↻ Replan with AI — advanced — calls AI again"). **(3) richer selected-COA summary** — `_v2SelectedSummaryHtml` (replaces the old `_v2OverrideNoteHtml` in the ready state): **Selected COA · Recommended · Operator override yes/no · Final score** (when `_ranking.final_score` present); shows **"Select a COA card first."** (`data-ff-v2="no-selection"`) when nothing is selected; override sentence refined to **"Operator override: you selected COA-X instead of recommended COA-Y."** (the only -X test touched: #6 regex updated to the new copy). Diagnostics/Legacy stays closed by default; no raw JSON/benchmark/decision-log in the operator path. **LIVE browser proof (auth-stub, autoPort 50606):** ready stepper shows ✓Generate→**2 Select COA**→**3 Commit**→4 Run→5; real DOM click on COA-2 → summary flips to override **yes** / Final score 62 / "you selected COA-2 instead of recommended COA-1" + commit label COA-1→COA-2; blocked stepper marks step 4 `blocked`; **no console errors**. Smoke test (req #9) proves empty→ready→committed→running/blocked, COA-click changes selection, Commit uses selected COA, Run no /plan-coas, tick `llm_called_this_tick=false`. Tests: **v2-operator-walkthrough-y 10/10**; **full FF suite 46/46** + scripts COA green. `?v=v2-operator-walkthrough-y`. `[[project_offline_agent_architecture]]` |
| Free Fight V2 real-operator acceptance + Run-feedback fix (RMOOZ-FREE-FIGHT-V2-REAL-OPERATOR-ACCEPTANCE-Z) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-v2-real-operator-acceptance-z.js` (NEW, 6/6) | **NEW 2026-06-20 — acceptance pass that found + fixed ONE V2 usability bug; UI GUIDANCE ONLY, engine FROZEN (planner/COA-exec/deterministic ticks/movement physics/validation/Green/White/ranking/Staff-Safe/LLM-config/DB-Lite/terrain/readiness/supply/scheduler all untouched).** **Reported by operator:** "when I reach Run, it doesn't work" → symptom = **"completes, nothing moved."** **Root cause (NOT a bind/engine bug — Run path proven sound):** a committed COA that HOLDS position (or whose units are already at their objective) runs to `phase_status='complete'` with 0 map movement, and the V2 cockpit said **nothing** about it (the legacy "Map Movement Truth" block is now in the drawer) → operator read silence as broken. **Fix:** `_v2MovementSummaryHtml` + `_v2CoaActionCounts` (render-time, reads the committed COA's own orders + `completed_orders`) appended to the committed/running/paused/complete views: **"Movement: this plan orders N move · M hold · executed K"** + an honest `data-ff-v2="no-movement"` note — "This COA holds position — no unit movement is expected …" when `move===0`, or "completed but no maneuver orders executed — units may already be at their objective" when complete-with-0. Sets expectations in the committed view BEFORE Run, so a hold plan no longer reads as a broken Run. No new `data-act`, no duplicates, no engine change. **Diagnostics also proved (engine sound):** committed→Run→**running**, resolvable unit moved ≥1, **no /plan-coas**, tick `llm_called_this_tick=false`; the earlier "blocked" only happens when the COA's unit_uid doesn't resolve to a loaded scenario unit (data/identity, not cockpit). **LIVE browser proof (auth-stub, autoPort 49715):** hold COA → Run → `complete`, movedUnits=0, cockpit shows "orders 0 move · 1 hold" + "holds position — no movement expected"; **no console errors**. Tests: **v2-real-operator-acceptance-z 6/6** (full flow · maneuver moves · hold-explained · counts · committed-summary · no-dup-actions); X 13/13 · Y 10/10 · **full FF suite 47/47** + scripts COA green. `?v=v2-real-operator-acceptance-z`. **Caveat:** "executed K" counts completed non-HOLD orders (reached-target), not a live per-tick relocation count; the planned move/hold counts are the stable signal. `[[project_offline_agent_architecture]]` |
| Free Fight continuous scenario mode (RMOOZ-FREE-FIGHT-CONTINUOUS-SCENARIO-AA) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-continuous-scenario-aa.js` (NEW, 10/10) | **NEW 2026-06-20 — NEW FEATURE (continuous scenario) layered ON TOP of committed-COA execution; ADDITIVE, engine FROZEN (movement physics/teleport guard/validation/Green deterministic logic/White advisory scoring/COA ranking/LLM provider+model config/Staff-Safe/DB-Lite·terrain·readiness·supply/scheduler all UNCHANGED).** Distinguishes **Run Plan** (`v2-run` → `_runCommittedCoa`, execute the committed COA ONCE, can end "Plan complete") from **Run Scenario** (`v2-run-scenario` → `_runScenario`, keep the fight alive until an end condition). New `_scenario` runtime state `{ scenario_active, scenario_status[idle\|running\|paused\|complete\|blocked], scenario_turn, blue_cycle, red_cycle, current_actor[blue\|red\|white\|green\|unit-controller], end_condition, last_outcome, pending_replan_reason }`. **Loop (`_scenarioTick`/`_scenarioTransition`):** drive the EXISTING deterministic `_coaExecTick` (Blue exec — no LLM, no /plan-coas, `llm_called_this_tick=false`); when phases complete → **White deterministic outcome check** (`_whiteScenarioOutcome`: objective reached/contested, Blue success, Red active, units missing, should-continue, replan-required — NO LLM, decision `role=white·action=scenario_outcome_check·called_llm=false`) → **Green refresh** (`_refreshGreenWorld('scenario_turn')`, /neutral-world only) → **simple deterministic Red reaction** (`_redReaction`: hold/counter/withdraw/block/none, decision `role=red·action=red_reaction·called_llm=false`; Red LLM deferred) → turn++ → **end condition** (`_scenarioEndCondition`: objective_secured / blue_unable_to_continue / red_unable_to_contest / max_turns_reached / operator_stopped) OR pause **"Scenario needs new Blue orders"** (NOT "Plan complete"). **The LLM is reached only on explicit Replan** (existing `_replanCoa` → /plan-coas). **New scenario console** (`_renderScenarioCockpitV2`, shown when `_scenarioActive()`): Plan status · Scenario status · Turn · Current actor · Last outcome · Next required action + Pause/Continue/Stop and (when Blue needs orders) Commit-next / Staff-Safe / Replan(adv). Run Plan's "Plan complete" path is unchanged when no scenario is active. **LIVE browser proof (auth-stub, autoPort 49715):** committed view shows BOTH Run Plan + Run Scenario; Run Scenario → COA completes → **Turn 2, paused, "Objective reached but contested — Red still active", "Scenario needs new Blue orders"** (not Plan complete); decision log carries white/scenario_outcome_check + red/red_reaction (both called_llm=false); event log shows the White adjudication + Red reaction; **no console errors**. Tests: **continuous-scenario-aa 10/10** (run-plan-once · continues-past-completion · turn-increment · white-no-LLM · green-no-/plan-coas · red-no-LLM · needs-orders-not-complete · no-/plan-coas-on-ticks · tick-no-LLM · two-buttons · end-condition); X 13/13 · Y 10/10 · Z 6/6 · **full FF suite 48/48** + scripts COA green. `?v=continuous-scenario-aa`. **DEFERRED (noted):** Red-LLM reaction (advanced replan only, not now); scenario-runtime persistence across refresh (committed COA persists; `_scenario` is in-memory v1); deterministic Red *maneuver* (v1 records a posture decision, doesn't move Red units); auto-continue without operator orders (by design the fight pauses for new Blue orders — no LLM on ticks). `[[project_offline_agent_architecture]]` |
| Free Fight Auto Scenario Director (RMOOZ-FREE-FIGHT-AUTO-SCENARIO-DIRECTOR-AB) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-auto-scenario-director-ab.js` (NEW, 11/11) | **NEW 2026-06-20 — makes Run Scenario a living battle; ADDITIVE on AA, engine FROZEN (movement physics/teleport guard/validation/DB-Lite·terrain·readiness·supply/Green logic/White advisory scoring/COA ranking/LLM config/existing Run Plan + manual Replan all UNCHANGED).** Closes AA's two deferred gaps (deterministic Blue next order; real Red maneuver) + adds an **Auto Continue** toggle. Two modes: **Manual Scenario** (AA behavior — pauses "needs new Blue orders") and **Auto Scenario** (auto-generates the next order + Red maneuvers; continues until an end condition / serious blocked / operator stop). New `_scenario` settings: `auto_continue`, `auto_director_enabled`, `max_auto_turns` (8), `last_auto_order_source`, `last_red_maneuver`. **Deterministic Blue next order** (`_autoDirectorNextBlueOrder`/`_autoDirectorBuildCoa`): from the White outcome picks a posture (contested→secure/MOVE · reached+contested→hold_screen · not-reached→advance/MOVE · weak→consolidate), builds a Staff-Safe COA labelled **`source.type='staff_safe_auto_director'`** and commits it via the EXISTING `_commitCoa` (NO LLM, NO /plan-coas). **Real Red maneuver** (`_redManeuverOrder`): counter/block→MOVE toward objective, withdraw→MOVE away, hold→none; **actually moves Red units through the SAME `_resolveCoaMoves`→`_writeMoveFrame` path (teleport-guarded via `_stepTowardCapped`)** — not just a log. `_scenarioTransition` rewritten: serious-blocked (replan_required) → pause for human even in auto; else White→Green→[end?]→Red reaction+maneuver→turn++→ **auto:** commit auto Blue order + keep `running` (pause only on end/blocked/cap) **manual:** pause "needs new Blue orders". New end condition **`max_auto_turns_reached`** (auto turn cap → operator decision). **Decision log:** `performance·auto_director_next_blue_order`, `red·red_maneuver_order`, `white·scenario_outcome_check`, `green·neutral_world_refresh` — all `called_llm=false`. **Event log:** "Auto Director: generated Staff-Safe Blue next order (…)", "Red maneuver (turn N): counter — moved K unit(s)", "White: objective contested — scenario continues". **UI:** "Auto Continue Scenario" toggle (committed view + console) + "Auto Continue uses deterministic Staff-Safe orders. No AI on normal turns."; console adds Scenario mode (Manual/Auto) · Blue auto order source · Red last maneuver; "needs new Blue orders" hidden in auto unless blocked. **LIVE browser proof (auth-stub, autoPort 49715):** toggle ON → Run Scenario auto-ran to **Turn 8** with `last_auto_order_source=staff_safe_auto_director`, **Red counter moved a unit (lon 54.46→54.40)**, stopped at `max_auto_turns_reached`; sample state @turn 3 captured; decision log has all 4 record types (no-LLM); **no console errors**. Tests: **auto-scenario-director-ab 11/11**; AA 10/10 · X 13/13 · Y 10/10 · Z 6/6 · **full FF suite 49/49** + scripts COA green. `?v=auto-scenario-director-ab`. **DEFERRED:** Red-LLM reaction (still advanced-replan only); scenario persistence across refresh (in-memory); auto Blue order is a single-phase MOVE/HOLD toward the objective (no multi-phase maneuver scheme); `max_auto_turns`=8 / objective-near=8km hardcoded. `[[project_offline_agent_architecture]]` |
| Free Fight auto-scenario formation realism (RMOOZ-AUTO-SCENARIO-FORMATION-REALISM-AC) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-formation-realism-ac.js` (NEW, 10/10) | **NEW 2026-06-20 — ADDITIVE on AB; engine FROZEN (AI planner/LLM config/Green World logic/White ADVISORY scoring/COA ranking/Staff-Safe/DB-Lite·terrain·readiness·supply/existing Run Plan all UNCHANGED — only the scenario *adjudicator* `_whiteScenarioOutcome` + auto-director/Red targets evolve, NOT the protected collateral "White advisory scoring").** Fixes the AB degeneracy where Blue+Red converged on the EXACT objective coordinate. **Deterministic ring formations** around the objective: `_ringPos(obj, km, idx, baseDeg)` (per-unit `idx*25°` offset so units never share a coordinate; capped + teleport-guarded at execution) + named helpers `_objFormation` → objective_center / assault(2km) / support(5km) / screen(3km) / blocking(4km) / reserve(8km); Blue approaches from the SW sector (210°), Red defends from the NE (30°) → opposite sectors, no stacking. **Blue auto order** now targets assault ring (assault element, inside control) + support ring (trailing unit, behind); consolidate/weak holds. **Red maneuver** counter/block→blocking ring (not center), withdraw→away, hold→keep posture; `_redManeuverOrder` returns the target ring. **AREA-based objective control** (`_whiteScenarioOutcome` rewrite): control radius 5km, contest radius 8km → `objective_control` = **Blue** (Blue inside & Red can't contest) / **Red** / **Contested** (both inside) / **Uncontrolled**; exact center never required; exposes `blue_presence`, `red_contest`, `red_ability_to_contest`, `blue_ability_to_continue` (backward-compat `objective_reached/contested/blue_success` kept). **Decision log** adds `performance·formation_assignment` + `white·objective_control_check` (both `called_llm=false`); `red·red_maneuver_order` now carries the target ring. **Event log:** "Formation: Blue assigned assault/support positions.", "Red maneuver: blocking ring around objective.", "White: objective contested by X Red unit(s)." **Console** adds Objective control (Blue/Red/Contested/Uncontrolled) + Blue-inside + Red-contesting counts + Last formation order. **LIVE browser proof (auth-stub, autoPort 49715):** 2 Blue advanced from 15km, 2 Red defended → after the fight **4/4 DISTINCT positions, 0 units at the exact objective center**, control Contested (Blue inside 1 / Red contesting 2), formation "Blue hold_screen → assault/support ring", Red "counter → blocking ring — moved 2 units"; **no console errors**. Tests: **formation-realism-ac 10/10**; AB 11/11 · AA 10/10 · X/Y/Z green · **full FF suite 50/50** + scripts COA green. `?v=formation-realism-ac`. **DEFERRED:** rings are single-radius circles (no terrain-aware/avenue-of-approach placement); base angles (Blue 210° / Red 30°) + radii hardcoded; Red still deterministic (no Red-LLM). `[[project_offline_agent_architecture]]` |
| Free Fight V2 COA→scenario flow BUGFIX (RMOOZ-FREE-FIGHT-V2-COA-TO-SCENARIO-BUGFIX-AB1) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-coa-to-scenario-bugfix-ab1.js` (NEW, 10/10) | **NEW 2026-06-20 — USABILITY/STATE BUGFIX only; no new AI/Green/White/Red/ranking/scheduler/formation features; engine FROZEN.** Operator report: "Generate AI Plan → select COA → Run Scenario doesn't run as expected." **ROOT CAUSE (reproduced):** a STALE committed `_coaExec` — restored from sessionStorage on refresh (`_restoreCoaExec` brings it back PAUSED), or left over from a prior commit — **shadowed a freshly-generated plan**: `_freeFightControlStateV2` prioritised `_coaExec` (returned `paused`/`committed`) so the NEW plan's COA cards + Commit were hidden, and Run/Run-Scenario fired on the OLD COA, not the operator's new selection. The CLEAN flow (no stale exec) already worked. **FIX (identity, not a counter):** new `_committedPlanObj` records the `_coaPlan` *object* a commit came from; `_coaCommitIsStale()` = (a) `_committedPlanObj !== _coaPlan` (a newer plan object loaded) OR (b) `_coaSelectedIdx` moved off the committed `selected_coa_id`. State machine: **`if (ex.active && _coaCommitIsStale()) return 'ready'`** (placed first) → a fresh plan / changed selection sends the cockpit back to `ready` to (re)commit the CURRENT plan, so Run/Run-Scenario can never fire on an out-of-date commit. A restored commit with NO current plan is NOT stale (operator resumes it). Ready shows a `data-ff-v2="recommit-needed"` banner **"Selection changed — commit selected plan before running scenario"** + a Clear button when a stale exec lingers. New `_v2FlowStatusHtml` status line (`data-ff-v2="flow-status"`): selected-not-committed → committed-ready-to-run → running/paused/blocked. `_resetCoaExec` now also clears `_committedPlanObj` (req 7: Clear wipes committed + scenario + identity). **Confirmed NOT broken (already correct):** ready never shows Run Scenario (commit-first); Commit commits the SELECTED COA; Run Scenario runs the committed COA. **LIVE browser proof (auth-stub, autoPort 49715):** committed OLD plan → simulated refresh+restore (paused) → generated NEW plan + selected COA-2 → state recovered to **ready** (was paused) with cards+Commit+recommit banner, Run Scenario hidden; commit → committed COA-2 → **Run Scenario ran COA-2** (not the stale COA-1); **no console errors**. Tests: **coa-to-scenario-bugfix-ab1 10/10**; AC 10/10 · AB 11/11 · AA 10/10 · X/Y/Z green · **full FF suite 51/51** + scripts COA green. `?v=coa-to-scenario-bugfix-ab1`. **CAVEAT:** identity check is within-session (object ref); a restored exec is intentionally resumable until a new plan is generated; `_scenario` still in-memory. `[[project_offline_agent_architecture]]` |
| Free Fight COA commander-quality gate (RMOOZ-REAL-COA-COMMANDER-QUALITY-AD) | ✅ | `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-real-coa-commander-quality-ad.js` (NEW, 6/6) | **NEW 2026-06-20 — COA QUALITY, not movement/UI polish; engine FROZEN (movement physics/teleport guard/Green logic/White advisory scoring/COA ranking/LLM config/V2 state machine/Run flow unchanged).** Fixes "too many units move to the objective center → fake-looking COAs." **Deterministic COA Quality Gate** `_coaQualityGate(coa)` (NO LLM, NO fetch) scores tactical credibility (start 100): all-moves-same-target −40 · >60% at objective center −30 · support/recon/screen at center −20 · single-phase all-move-to-center −25 · no role diversity −15 · no support/security/reserve −15 · no commander-intent/main-effort/supporting/red-assumption/risk-mitigation −8/−8/−6/−6/−6 (role/support checks relaxed for <3 units); **pass ≥ 70**. **Deterministic Staff-Safe commander template** `_staffSafeCommanderCoa(side,units,obj)` — role-separated ring targets (assault 2km / support 5km / screen 3km flank / recon 7km flank / reserve 8km; NO exact center), **3 phases** (recon-screen-support establish → assault → consolidate), + full commander structure (commander_intent, main_effort, supporting_effort, reserve_or_follow_on, security_or_screen, red_assumption, risk_mitigation, control_measures, success_criteria). **Flow** `_runCoaQualityGateFlow` after a manual Generate: grade → **pass** | one **LLM repair** (`_repairCoaPlanOnce` re-requests `/plan-coas` with a repair hint; records `blue·coa_repair_prompt·called_llm=true`) | **deterministic fallback** → replaces COAs with the template, `plan_source='staff_safe_commander_template'`, `llm_status='llm_failed_quality_gate'`. The **auto-director** Blue order now carries commander structure + is graded (passes; stays labelled `staff_safe_auto_director`). **Decision log:** `performance·coa_quality_gate·called_llm=false` (verdict+reasons). **UI:** V2 plan-quality banner (`data-ff-v2="plan-quality"`, verdict pass/repaired/fallback/failed + score + intent + main/supporting effort + `fallback-warning` "NOT AI commander output") + per-card `coa-quality` chip. **Scenario:** the committed COA is always gated (pass/repaired/labelled-fallback) before Run/Run-Scenario — no silent low-quality plan. **LIVE browser proof (auth-stub, autoPort 49715):** shallow all-to-center LLM COA → gate score **0/fail** → fallback to template, role targets **assault 2km · screen 3km · support 5km · reserve 8km (0 at center)**, banner+warning shown, gate decision `performance/called_llm=false`; **no console errors**. Tests: **real-coa-commander-quality-ad 6/6**; AB1 10/10 · AC 10/10 · AB 11/11 · AA 10/10 · X/Y/Z green · **full FF suite 52/52** + scripts COA green. `?v=real-coa-commander-quality-ad`. **CAVEAT:** the LLM repair re-request depends on server planner cooperation (the deterministic template is the guarantee, tested); gate thresholds/penalties hardcoded; structure fields (intent/effort) are scored softly so a role-separated LLM COA without them can still pass on spatial merit. `[[project_offline_agent_architecture]]` |
| Free Fight COA quality HARD ENFORCEMENT (RMOOZ-COA-QUALITY-HARD-ENFORCEMENT-AE) | ✅ | `client/shell/free-fight-demo.js` · `server/ai/free-fight-coa-planner.js` · `client/app.html` · `test-free-fight-coa-quality-hard-enforcement-ae.js` (NEW, 9/9) · `test-free-fight-formation-realism-ac.js` (tick budget bumped) | **NEW 2026-06-20 — CORRECTS AD: the gate was display/generate-path only and never enforced the COMMITTED/EXECUTED COA, so the map still showed all units → objective center.** Engine FROZEN (movement physics/teleport guard/Green/White scoring/V2 state machine except commit-run gating/LLM config). **ROOT CAUSE (two):** (1) the SERVER deterministic builders (`buildDeterministicCoas`, the BLUE-reaction builder) emitted `target:{objLat,objLon,type:'objective'}` (exact center) — verified live: the running server returned COA-1 assault @0km; (2) AD's gate ran only in the manual generate flow — `_commitCoa`/Run never re-checked, so a stale/false-negative COA executed at center. **SERVER fix:** deterministic builders now place each role on a ring (`_ringPt`: assault 2 / support 5 / recon 7 / reinforce 3 km, per-unit angular offset); a top-level `_spreadCenterClusteredCoas(coas,obj)` post-process re-spreads ANY center-clustered/all-same-target COA by role (deterministic AND LLM) before the response returns. **Live proof:** a FRESH real-server instance returns COA-1 assault@2km · COA-2 assault@2km+support@5km · COA-3 recon@7km — **0 at center**. **CLIENT hard enforcement:** `_coaHardBlockReason(coa)` (all-same-target / >50% at exact center / ≥3 moves converge <1.0km / no role diversity) gates **commit** (`_enforceExecutableCoaQuality` → replaces the selected COA in the plan with the role-separated template built from the COA's OWN units, `plan_source='staff_safe_commander_template'`, `llm_status='blocked_low_quality_selected_coa'`) and **Run Plan / Run Scenario** (blocks a stale center-stacking exec with "Blocked: selected COA is not commander-quality — recommit"). Gate strengthened with a near-center CONVERGENCE penalty (−30 if ≥3 moves within 1.2km). **Movement-proof UI:** committed view shows Selected vs Committed/executed target summary (`_coaTargetSummary`) + run-blocked banner. **GOTCHA fixed mid-task:** the first template build used `_coaActiveSide` (returned RED for a BLUE COA) → moved the wrong unit; now units+side derive from the COA's own actions → the EXECUTED units relocate to their role rings. **Live executed proof (both fresh real-server + client):** an all-to-center COA → committed becomes template → units execute to **support@5km · screen@3km · assault@2km, 0/3 at exact center, 3/3 distinct**; no console errors. Tests: **coa-quality-hard-enforcement-ae 9/9** (server builder no-center · server re-spread · commit replaces · Run Plan blocks stale · Run Scenario blocks stale · selected==committed==executed · auto-director separated · convergence rejects near-center · **executed movement → role rings**); AD 6/6 · AB1 10/10 · AC 10/10 (tick budget bumped — its all-center bluePlan is now correctly replaced) · AB/AA/X/Y/Z green · **full FF suite 53/53** + scripts COA green. `?v=coa-quality-hard-enforcement-ae`. **CAVEAT:** the LIVE running :8000 server keeps the OLD planner in memory until restarted (verified the fix on a fresh instance); `_spreadCenterClusteredCoas` only re-spreads when >50% center / all-same (a partially-shallow COA may slip — the client commit gate is the backstop); the running-server pre-existing 4-line `planning_mode` echo rides in this commit (benign). `[[project_offline_agent_architecture]]` |
| Step-1 COA Preparation Gate / unit taskability (RMOOZ-STEP1-COA-PREPARATION-GATE-AE) | ✅ | `client/shell/unit-taskability.js` (**NEW pure module**) · `client/shell/free-fight-demo.js` · `client/app.html` · `test-free-fight-step1-coa-preparation-gate-ae.js` (NEW, 9/9) | **NEW 2026-06-21 — engine-level gate (the user-chosen "Step-1 first" increment; reused by the planned Scenario Control Center rebuild AF as its Panel-1 Readiness gate). Engine FROZEN (movement physics/teleport guard/Green/White scoring/COA ranking/AD quality gate/LLM config/V2 state machine except gating).** Fixes "uploaded a Step-1 GCC BLUE ORBAT and units still move to the objective." **A Step-1 ORBAT is REVIEW-ONLY:** units carrying `source_required` / `needs_review:true` / `exact_unit_position:false` / null `lat`·`lon` / `doctrine_upload_required` / `commander_review_required` are NOT taskable for movement/combat — they may only HOLD / be flagged REVIEW_REQUIRED / SOURCE_REQUIRED / DOCTRINE_REQUIRED. **NEW canonical pure module `unit-taskability.js`** (`window.RmoozTaskability`, dual CommonJS+window, no DOM/scenario mutation/network/LLM): `classifyUnit(unit,ctx)` → `{taskable, reason, review_status, allowed_actions, blocked_actions, blockers{coords,source,doctrine,commander_review}}`; `prepareReport(units,ctx)` → `{units_loaded, taskable, blocked, blocked_by_missing_source/coordinates/doctrine/commander_review (non-exclusive), taskable_units, blocked_units, executable, message}`; `isStep1State`. **Client wiring (`free-fight-demo.js`):** `_classifyUnit`/`_taskabilityCtx` (brief-level doctrine+commander posture from `task_assembly`)/`_rawScenarioUnits`/`_isUnitTaskable`/`_taskableSideUnits`/`_step1PreparationReport`/`_step1Gate` + a strict built-in fallback (errs toward non-taskable if the module didn't load — a placeholder must NEVER move). **Enforcement at 4 points:** (1) the single movement chokepoint `_resolveCoaMoves` suppresses any non-taskable unit on EVERY path (manual apply, committed tick, scenario, Red maneuver, loop) — logged once each "HELD: … Step-1 review-only"; (2) **Generate** runs the gate first — no taskable units → NO movement COA + NO `/plan-coas` call (`_coaPlan={ok:false,_step1_blocked:true,_error:'COA unavailable — Step 1 data requires source/doctrine/commander review.'}`); when partially taskable, only taskable unit ids are sent to the planner; (3) **Commit** refuses a COA that tasks a non-taskable unit (`_coaTasksBlockedUnit` → `_coaCommitBlockedReason`); (4) **Run Plan / Run Scenario** refuse a committed COA that tasks a non-taskable unit (live re-check). COA builders (`_autoDirectorBuildCoa`, `_enforceExecutableCoaQuality` fallback, `_planFallbackUnits`) fill from `_taskableSideUnits` only. **Decision log:** `white·step1_coa_preparation_gate·called_llm=false` (taskable/blocked counts). **Event log:** "Step 1 Gate: X taskable, Y blocked pending source/doctrine review." **UI:** `_v2Step1BannerHtml` readiness banner (taskable/blocked counts · per-reason breakdown · held-units list · "No executable COA until review complete" when nothing taskable; always reflects current units) + empty-state Step-1-blocked body with a "Re-check readiness" action. **LIVE browser proof (auth-stub :49715, real `window.RmoozTaskability` loaded via its own `<script>`):** mixed force (5 units) → 1 taskable (B-5) / 4 blocked (source 3 · coords 1 · doctrine 1), and the movement chokepoint moved ONLY B-5; all-Step-1 force → Generate blocked, **0 `/plan-coas` calls**, banner "0 taskable · 3 blocked / No executable COA until review complete", held B-1/B-2/B-3 = SOURCE_REQUIRED; **no console errors**. Tests: **step1-coa-preparation-gate-ae 9/9** (pure module · per-unit source/null-coords/exact_unit_position/doctrine · gate report+counts+decision-log · readiness banner · Generate-blocked-no-/plan-coas · commit-refusal · chokepoint+run-gate · mixed-force-only-taskable-moves · **AD quality gate still active**); **full FF suite 46/46** (AE-quality 9 · AD 6 · AB1 10 · AC 10 · AB/AA/X/Y/Z/O + 20-test engine/movement batch + 12-test remainder). `?v=step1-coa-preparation-gate-ae`. **CAVEAT:** the next increment **RMOOZ-SCENARIO-CONTROL-CENTER-REBUILD-AF** (full Free Fight window teardown + new `scenario-control-center.js`) is NOT yet done — this Step-1 gate is the reusable engine layer it will consume (the AE banner is the minimal current surface, migrates to AF Panel-1). Brief-level doctrine/commander posture is best-effort from `task_assembly` (per-unit flags take precedence). `[[project_offline_agent_architecture]]` |
| Scenario Control Center — operator window HARD REPLACEMENT (RMOOZ-SCENARIO-CONTROL-CENTER-REBUILD-AF) | ✅ | `client/shell/scenario-control-center.js` (**NEW operator UI module**) · `client/shell/free-fight-demo.js` (engine facade + render swap + V2 deletion) · `client/app.html` · `test-scenario-control-center-af.js` (NEW, 13/13) | **NEW 2026-06-21 — HARD REPLACEMENT of the old Free Fight control window (owner: "treat it as if it never existed"). Engine FROZEN (movement physics/teleport guard/Step-1 taskability/AD quality gate/Green-White/COA commit+exec/continuous scenario/LLM config all UNCHANGED — only the operator UI/flow is new).** **DELETED** the entire V2 cockpit block from `free-fight-demo.js` (451 lines): `_freeFightControlStateV2`, all `_v2*` render helpers, `_renderScenarioCockpitV2`, `renderFreeFightControlV2`, `_freeFightLegacyDrawerHtml`, `bindFreeFightControlV2` + their `*ForTest` seams. `updatePanel` now renders ONLY the new card (legacy `html` build + ~80 legacy binds removed). **NEW pure UI module `scenario-control-center.js`** (`window.RmoozScenarioControlCenter`: `render()` / `bind(bindFn)` / `state()`) — its own small state machine (`no_scenario · step1_review_required · ready_to_generate · generating_coa · coa_review · committed · scenario_running · scenario_paused · scenario_blocked · scenario_complete`), unique `scc-*` action ids (NO old data-act id reused), 6 panels: **1 Readiness** (scenario name · data reliability · source/doctrine/commander status · taskable/blocked counts + reasons · "No executable COA. Step 1 review required." · objective placement), **2 Prepare COA** (Prepare runs Step-1 gate → taskability → ROE/doctrine → quality BEFORE any AI; Staff-Safe deterministic option), **3 COA Review** (per-COA plan_source · llm_called · commander-quality verdict · intent/main/supporting/reserve-security · phases · **exact action-targets table** unit/role/action/target_type/lat/lon/km/ROE/taskable · hard-fail "Rejected: not commander-quality. All units are moving to the objective center."), **4 Commit** (only when executable; stores exactly the reviewed COA + committed target table), **5 Run Scenario** (Run Scenario / Run Plan once / Pause / Stop / Clear; turn · actor · phase · objective control · last movement · White outcome · Green status · Red reaction; no hidden Run before commit), **6 Debug/Evidence** (generated/selected/committed COA raw JSON · executed movement trace · decision log · network calls). **Drives the engine via a NEW clean facade `RmoozFreeFightDemo.engine`** (readiness/state/prepareCoa/coaPlan/select/quality/hardBlock/actionTargets/commit/run/pause/stop/clear/scenarioRuntime/white/green/decisionLog/networkCalls/rawJson/executedTrace) — the UI never reaches into engine internals. One source of truth: selected COA (`coaPlan`+`selectedIdx`), committed COA (`committedExec`), scenario runtime (`scenarioRuntime`); a stale committed COA (`committedIsStale`) cannot shadow a new plan (recommit banner + Run hidden); Clear resets all three + persistence. **LIVE browser proof (auth-stub :49715):** old window GONE (0 `data-ff-v2`, no legacy drawer, no group-demo, old V2 render seam `undefined`), new SCC is the sole operator body; full flow Readiness→Prepare→Review→Commit→Run with **selected == committed == executed targets** (`support:6.9km · screen:4.6km · assault:2.7km`); all-to-center COA rejected + commit produces the Staff-Safe template; **no console errors**. Tests: **scenario-control-center-af 13/13** (old window gone · SCC renders · Step-1 blocks · center rejected+uncommittable · action-targets table · commit stores reviewed · Run hidden pre-commit · Run executes committed only · no stale shadow · every visible button bound · Clear resets); **full FF suite 43/43** (retired X/Y/Z + card-ux-clarity as obsolete; migrated AA/AB/AB1/AD/AE-step1 to SCC seams; trimmed the old-window-body assertions from 7 engine tests + speed-control, keeping their engine coverage). `?v=scenario-control-center-af`. **CAVEAT:** dead sub-render helpers `_operatorStripHtml`/`_coaExecHtml` (old non-cockpit panels) remain unreferenced in the file (not in the operator path; their O/commit-exec tests still pass via seams) — a further micro-cleanup; the continuous-commander-loop render fns also remain as dead code (loop is not part of the new operator card). The 7 trimmed tests retain their engine assertions; old-window-UI assertions are marked `[[retired-by-AF]]`. **AG DEAD-UI CLEANUP (2026-06-21, `?v=scenario-control-center-ag`):** physically DELETED the remaining unreachable old-card code from `free-fight-demo.js` (−841 lines net; file 5904→5064): `renderCoaPlanHtml` (the "Generate AI Attack Plan / Typical plans / select-coa-*" card), `renderAiDecisionHtml` (tabbed container), `renderCommanderLoopHtml`, `renderPlanningTraceHtml`, `_coaExecHtml`, `_greenWorldHtml`, `_whiteTabHtml`, `_ffTabBarHtml`/`_ffPanel`, `_operatorStripHtml`, `_operatorSummaryHtml`, `_systemLayersHtml` + their 10 `*ForTest` seams. **Client-wide grep proof = 0** for `renderCoaPlanHtml` · `_renderCoaPlanHtmlForTest` · `renderAiDecisionHtml` · `Generate AI Attack Plan` · `Typical plans` · `select-coa-` · `AI Attack Plan Reasoning` · `data-ff-v2` · `legacy drawer` · `renderFreeFightControlV2` · `bindFreeFightControlV2`. Test migration: DELETED 3 obsolete old-card-UI tests (control-window-rebuild-w, simple-operator-ux-o, ai-coa-ui-a, coa-commander-narrative-a, ai-visible-marker-truth-a — 5 total); MIGRATED the AI-only/MCP-prompt **honesty** to SCC evidence (ai-attack-plan-ai-only-a, ai-attack-plan-mcp-prompt-a now assert `plan_source`/`llm_called`/quality verdict + `mcp_prompt` via the engine facade); TRIMMED old-card source-scan assertions from advisory-commit-journal-v / coa-ranking-with-advisory-u / green-white-scoring-t / green-world-ui-r / white-green-annotation-t / ai-scenario-agnostic / ai-speed-control / real-scenario-unit-feed / local-llm-timeout-polish / llm-decision-trace (kept engine, marked `[[retired-by-AG]]`). AF test §14 now asserts PHYSICAL deletion (16 fns + 10 seams gone + banned-tokens-zero). **LIVE DOM proof (full flow):** main = SCC; `_aiPanel`/`_cmdrPanel` absent; 0 banned tokens in the document; Step-1 blocks · all-center rejected · selected==committed==executed (`support:6.9 · screen:4.6 · assault:2.7km`) · Run hidden pre-commit · Clear resets · no console errors. test-scenario-control-center-af **13/13**; **full FF suite 45/45**. KEPT (engine, allowed): Step-1 taskability, COA quality enforcement, movement helpers, Green/White engine, decision log, `_isRealLlmPlan`, `renderCommanderPanel` (loop brief — loop is a separate feature, not the operator card), `renderAiPanel` (permanent no-op). **AF FOLLOW-UP (2026-06-21, `?v=scenario-control-center-af2`):** the first pass replaced only the `_panel` window body; a SECOND operator surface remained — `renderAiPanel` (`_aiPanel`, the "AI Attack Plan Reasoning" floating panel, 8 live callers). DISCONNECTED `renderAiPanel` to a permanent no-op (removes `_aiPanel`; reasoning-HTML builder deleted). Confirmed the tabbed COA-card surface (`renderAiDecisionHtml` → `renderCoaPlanHtml`/`renderCommanderLoopHtml` + `select-coa-*`/"Generate AI Attack Plan"/"Typical plans") has **0 live callers** (its entry `renderAiDecisionHtml` is uncalled → transitively unreachable; `updatePanel` calls none of the old renderers). `renderCommanderPanel`/`_cmdrPanel` is loop-only (absent after mount; needed by the LLM-gate test) and `_card` is a unit-intel click popup — neither is in the SCC operator path. **DOM proof (live, full flow mount→objective→commit→run):** main panel = SCC (`data-scc="window"`, state `scenario_running`); `_aiPanel`/`_cmdrPanel`/unit-card ALL absent; doc has NO `Generate AI Attack Plan`/`Typical plans`/`select-coa-*`/`data-ff-v2`/tab-bar/reasoning-panel; no console errors. test-scenario-control-center-af now **15/15** (added §14 source-proof: V2 deleted · `renderAiDecisionHtml` 0 live callers · `updatePanel` calls no old renderer · `renderAiPanel` no-op; §15 DOM-proof: no old surfaces). **full FF suite 47/47.** NOTE: the user's "still see the old card" was a STALE BROWSER TAB (pre-AF JS cached) — bumped cache-bust to `af2` + hard-refresh required. The dead unreachable functions (`renderCoaPlanHtml`/`renderAiDecisionHtml`/tab helpers/`_operatorStripHtml`/`_coaExecHtml`/loop render) are retained ONLY to feed engine-display unit tests (AI-only gate / MCP prompt / ranking) — item-17 "unreachable from operator path" is satisfied; physical deletion would require migrating 16 `_renderCoaPlanHtmlForTest` assertions. `[[project_offline_agent_architecture]]` |
| SCC Prepare COA — live planner pipeline (RMOOZ-SCC-PREPARE-COA-LIVE-AH) | ✅ | `client/shell/scenario-control-center.js` · `client/shell/free-fight-demo.js` (engine facade `executedTargetSummary`) · `client/app.html` (`?v=scenario-control-center-ah`) · `test-scc-prepare-coa-live-ah.js` (NEW, 9/9) | **NEW 2026-06-21 — wires the SCC "Prepare COA" to the REAL COA pipeline end-to-end; NO old Free Fight UI reintroduced (engine UNCHANGED).** Most scaffolding existed from AF/AG (Prepare → `_generateCoaPlan` → `/api/wargame-sim/free-fight/plan-coas`; Step-1 gate blocks no-taskable + filters taskable units to the planner; quality enforcement at generate + commit). AH ADDED the evidence/columns gaps: COA Review **target table** now has the full AH column set (unit · role · action · target_type · lat · lon · **distance_from_objective_km** · taskable · **ROE status** · **reason**); COA card shows **llm_status · risk · confidence** (+ existing plan_source/llm_called/quality verdict+score/intent/main/supporting/screen/reserve/phases); **Evidence panel** adds the **target-equality proof** block (`selected_target_summary` == `committed_target_summary`, + `executed_target_summary`) + the **readiness report JSON** + raw planner response/selected/committed JSON; engine facade gains **`executedTargetSummary()`** (km-from-objective of actually-executed final positions). Flow: Prepare → `preparing_coa`/`generating_coa` → blocked→`step1_review_required` (no `/plan-coas`) OR taskable→POST `/plan-coas` (network logged) → `coa_review` (no auto-commit) → select → Commit (commit-time enforcement; selected==committed unless replaced, replacement labelled) → Run (committed COA only) → Evidence. **LIVE browser proof (preview :49715, realistic `/plan-coas` stub since the preview server has no model):** clicking Prepare sent **POST /api/wargame-sim/free-fight/plan-coas** with only taskable unit ids → COA-1 "Supported assault" in Review with the full target table (B-2 support 6.9km / B-3 screen 4.6km / B-1 assault 2.7km, reason col, ROE permitted, taskable yes) · `source deterministic_diverse_coa · llm_called false · commander-quality PASS (100)` · **selected == committed** (`support:6.9 · screen:4.6 · assault:2.7km`) · Run shown only after commit · Evidence target-equality block present · **0 banned tokens · no console errors** (screenshot captured). Tests: **scc-prepare-coa-live-ah 9/9** (clean render · Step-1 blocks no `/plan-coas` · `/plan-coas` called when taskable · response in Review · exact target table · select changes state · all-center rejected+uncommittable · executable commits selected · Run hidden→shown · Run executes committed · Evidence 3-way summaries · Clear resets · banned tokens zero); AE step1 ✓ · AE quality-hard-enforcement ✓ · AF/AG `scenario-control-center-af` 13/13 · **full FF suite 46/46**. **CAVEAT:** the live proof stubs the planner RESPONSE in the browser (preview server runs no Ollama); against the real :8000 server `npm run app` with a model, the same Prepare path returns real LLM COAs (deterministic fallback when no model). Executed summary is capped per-tick progress toward the committed targets (movement physics unchanged), not instantaneous arrival. `[[project_offline_agent_architecture]]` |
| AI commander COA quality / reliability (RMOOZ-SCC-COA-COMMANDER-QUALITY-AI) | ✅ | `server/ai/free-fight-coa-planner.js` (extraction + schema + normalizeCoa passthrough) · `server/ai/rmooz-ai-tool-contract.js` (strict prompt) · `client/shell/free-fight-demo.js` (capture_raw_llm + `llmEvidence` facade) · `client/shell/scenario-control-center.js` (Evidence honesty block) · `client/app.html` (`?v=scenario-control-center-ai`) · `test-scc-coa-commander-quality-ai.js` (NEW, 8/8) | **NEW 2026-06-21 — makes the AI commander output RELIABLE + STRUCTURED + HONEST inside SCC Prepare COA. Engine UNCHANGED except the LLM-output path; deterministic fallback PRESERVED; a fallback is NEVER dressed as an AI plan.** **DIAGNOSIS:** the AH smoke test's `invalid_json` was qwen3-coder OUTPUT VARIABILITY (sometimes fences/commentary/truncation) AND the model returned its own loose schema (0/8 commander fields). **FIXES:** (1) hardened **`extractCoaJson`** (strips ``` fences, parses balanced top-level `{...}` string-safely, picks the unambiguous coas-object; `json_extraction_failed` when ambiguous, `invalid_json` when truncated/empty) replaces the naive regex; (2) **prompt hardening** (`MCP_COMMANDER_INSTRUCTIONS`+`SYSTEM_CONTRACT`+`composeRepairPrompt`): EXACTLY ONE JSON object, no markdown/fences, demand commander_intent/main_effort/supporting_effort/reserve/security/red_assumption/risk_mitigation/success_criteria + per-action reason/roe_status/taskable + role-separated/no-all-center + HOLD-restricted; (3) **Ollama STRUCTURED OUTPUT** — enriched `_coaOutputSchema` (requires the commander fields) is now passed as Ollama **`format`** (grammar-constrained; default ON, `RMOOZ_COA_SCHEMA_OFF=1` to disable) — this FORCED the model to emit the fields; (4) **`normalizeCoa` now carries** the 8 commander fields + phase title/purpose (they were being stripped → SCC saw 0/8); (5) one structured **repair** (existing loop) feeds the exact validator rejections; (6) honest **`*_fallback` status** (`invalid_json_fallback`/`schema_invalid_fallback`) + **raw-capture-on-failure** so a fallback is labelled, never dressed as AI; (7) SCC **Evidence honesty block** (`data-scc="ai-honesty"`: plan_source/llm_called/llm_status/repair_attempted/is_real_llm + "AI was called, but a fallback plan was used…" + raw LLM output preview) via the `llmEvidence()` facade. **REAL-SERVER PROOF (qwen3-coder:latest on Ollama):** before the schema-as-format fix → `llm_status:ok` but **0/8** commander fields; after → `plan_source:llm · llm_status:ok · repaired:false · ~22s`, **3 COAs each 8/8 commander fields** with real intent prose (Recon-Security / Deception-Flank / Direct-Approach), every action with a reason; SCC COA Review renders Intent/Main effort/Supporting/Reserve-Security + the full target table + `commander-quality PASS (100)` (screenshot); no console errors. Tests: **scc-coa-commander-quality-ai 8/8** (extraction clean/fenced/commentary/multiple→ambiguous/truncated/empty · prompt JSON-only+commander-fields+repair-feeds-errors · normalization drops invented units · `normalizeCoa` carries all 8 commander fields+phase purpose · real-llm is_real_llm=true · invalid_json_fallback honest+Evidence "AI called, fallback used"+raw preview · schema_invalid_fallback honest · all-center AI COA rejected+uncommittable); **full FF suite 47/47**. **CAVEAT:** structured output depends on the Ollama version supporting schema-as-`format` (qwen3-coder honored it here); a model that emits malformed JSON anyway still falls back honestly (proven path). The `_coaOutputSchema` `required` forces the KEYS — value quality is the model's (qwen3-coder produced sound prose); `RMOOZ_COA_SCHEMA_OFF=1` reverts to prompt-only. `[[project_offline_agent_architecture]]` `[[project_free_fight_ai_only]]` |
| SCC Step-1 training-simulation approval (RMOOZ-SCC-STEP1-TRAINING-APPROVAL-AK) | ✅ | `client/shell/unit-taskability.js` (training_approved ctx) · `client/shell/free-fight-demo.js` (`_trainingApproved` + facade) · `client/shell/scenario-control-center.js` (Panel-1 approve/clear + banner + Evidence) · `client/app.html` (`?v=scenario-control-center-ak`) · `test-scc-step1-training-approval-ak.js` (NEW, 10/10) · `test-free-fight-step1-coa-preparation-gate-ae.js` (message-wording update) | **NEW 2026-06-21 — adds an explicit operator "Approve Draft for Training Simulation" workflow so a review-only Step-1 ORBAT (the real-app "0 taskable / 379 blocked, source 379 / commander 379" blocker) can be RUN for TRAINING without weakening the gate or faking source verification. Engine UNCHANGED; the Step-1 gate (AE) is NOT lowered; unit source flags are NEVER mutated.** **WHY:** the AE gate correctly reports review-only units as non-taskable, so Prepare COA is refused — but the operator needs to exercise a COA against the draft. **DESIGN:** review-only units that are blocked by source/doctrine/commander **but have a verified position** are *training-eligible*; an explicit operator approval makes ONLY those taskable, **in simulation mode only**, loudly labelled "SIMULATION ONLY — not source-verified". **Units missing coordinates STAY blocked** (you cannot move a unit with no position). **Pure resolver (`unit-taskability.js`):** `classifyUnit(unit, ctx)` honours `ctx.training_approved===true` → for a unit blocked ONLY by source/doctrine/commander (not coords) returns `{taskable:true, simulation_only:true, source_verified:false, review_status:'TRAINING_APPROVED', overridden:{source,doctrine,commander_review}}`; coords blocker is never overridden. `prepareReport` adds `training_eligible` (blocked-but-not-by-coords count), `simulation_taskable`, `training_approved`, `simulation_only` + a training-offer message. **Client (`free-fight-demo.js`):** `var _trainingApproved`, `_taskabilityCtx()` threads `training_approved`, `_isSimulationOnly()`, `_approveTrainingSimulation()`/`_clearTrainingApproval()` (decision log `white·step1_training_approval{,_cleared}·called_llm:false` + event log "⚠ SIMULATION ONLY: operator approved N…"), `_step1PreparationReport()` emits the AK fields, the generated plan is stamped `_simulation_only`, and the engine facade gains `trainingApproved()/simulationOnly()/approveTrainingSimulation()/clearTrainingApproval()` + readiness AK fields. **SCC (`scenario-control-center.js`):** Panel 1 shows an "Approve Draft for Training Simulation" button (`scc-approve-training`) when not-executable + training-eligible; after approval a loud "⚠ SIMULATION ONLY — not source-verified" label + "Clear training approval" (`scc-clear-training`) + a persistent top banner "⚠ TRAINING SIMULATION ONLY — units are NOT source-verified" in every downstream state; Panel 6 Evidence records `training_approved/training_eligible/simulation_taskable` + that the override does NOT source-verify (readiness JSON still carries each unit's original flags). **LIVE browser proof (preview :49715):** review-only ORBAT (3 source-required WITH coords + 1 missing-coords) → BEFORE: 0 taskable / 4 blocked / 3 training-eligible / not executable / offer+approve button shown; AFTER approve → **3 taskable (all simulation-only) / 1 still blocked (B-4 missing coords) / executable / state ready_to_generate**, Prepare COA enabled, top banner + SIMULATION-ONLY label + Clear button, B-1 taskable / B-4 still NOT, **raw `source_required` flag still set** (screenshot). Tests: **scc-step1-training-approval-ak 10/10** (pure resolver overrides source/doctrine/commander not coords + counts/messages · readiness before · approve button shown · approve transition + coords-blocked stays blocked · gate+chokepoint move approved units / coords-missing suppressed · loud labels+banner+Clear · decision/event/Evidence record · flags unchanged · plan simulation_only stamp · clear reverts + gate NOT weakened); AE updated for the new training-offer message wording (9/9 still green); **full FF suite 57 passed / 3 pre-existing unrelated failures** (commander-brief-ui textarea, doc-review-step1 enemy-force-structure, step1-unified-bases RED base count — all fail identically on baseline HEAD with AK stashed). `?v=scenario-control-center-ak`. **CAVEAT:** training approval is a per-session runtime override (not persisted); Clear reverts to full Step-1 review. The approval is the operator's call and is fully recorded — it does NOT source-verify, and a unit with no coordinates can never be tasked even under approval. `[[project_offline_agent_architecture]]` |
| COA Commitment Mode (RMOOZ-COA-COMMIT-EXECUTION-L) | ✅ | `client/shell/free-fight-demo.js` · `scripts/test-coa-commit-execution-l.js` (NEW) | **NEW 2026-06-16 (execution architecture — NO combat/movement/COA-validation/DB-Lite/terrain/readiness/validator/teleport-guard/Staff-Safe change; reuses the existing apply path).** Stops the "AI thinks every action/reaction turn" waste. The operator commits ONE generated COA and **RMOOZ executes it phase-by-phase deterministically with NO LLM on normal ticks**; the AI is re-engaged ONLY on a replan trigger or operator Replan. Added (an ADDITIONAL mode alongside the AI-every-turn loop, which is unchanged): **`_coaExec` = active_coa_execution_state** (active/side/selected_coa_id/selected_coa/current_phase_index/phase_status[pending\|running\|complete\|blocked]/unit_order_status/completed_orders/branch_triggers/replan_required/replan_reason/ticks/stuck_ticks/timestamps/last_tick_timing). **`_commitCoa`** (persist + `coa_commit_ms`), **`_coaExecTick`** (executes ONLY the current phase via `_resolvePhaseMoves` → same `_stepTowardCapped` clamp/teleport guard + `_writeMoveFrame` + `_logExecutedMoves`; HOLD_POSITION never moves; advances phase when all orders reach target; marks COA complete; **`llm_called_this_tick:false`**, `coa_tick_execute_ms`, `replan_trigger_check_ms`), **`_runCommittedCoa`/`_pauseCommittedCoa`/`_resetCoaExec`**, **`_replanCoa`** (the ONLY operator path that re-calls the AI → `_generateCoaPlan`). **Deterministic replan triggers** (`_checkReplanTriggers`): assigned unit missing/destroyed, objective changed, invalid target, phase stuck ≥4 ticks, active-force loss >50%, user Replan → pause + `phase_status:blocked` + reason + offer Continue/Replan(AI)/Staff-Safe. **UI** (`_coaExecHtml`, in the COA card): Commit / Run / Pause / Replan + status (Active COA · Current phase · Orders complete/total · status · tick) + replan-reason banner + **"✅ AI is NOT being called on normal ticks"** + per-tick timing proof. **Local-LLM proof:** Deep Plan via Ollama `qwen3-coder:latest` → `plan_source:llm`, 2 COAs, **53.5s** (commander 31.5s + analyst 10.8s) — the OLD design paid that **every** turn; Commitment Mode pays it **once**, then N deterministic ~0ms ticks (e.g. a 10-turn run ≈500s → ≈50s). Tests: **coa-commit-execution-l 9/9** (commit persists state · tick executes phase w/o LLM + no plan-coas fetch · phase advances · HOLD doesn't move · missing-unit trigger fires+pauses · Replan calls AI once · Staff-Safe available · Deep Plan ≥2 COAs · UI controls + no-LLM label); regressions green (coa-planner 71 · continuous-loop 42 · movement-execution 11 · coa-ui 29 · speed-arch-j 11 · ai-model-ready-state 18 · gate-card-d 7 + the required suites). `?v=coa-commit-exec-l`. **DEFERRED (transparency):** server-side persistence of `active_coa_execution_state` (currently client runtime), enemy-contact-near-route trigger (hook noted), branch-action auto-execution (branches stored, not yet auto-applied) — next increment. `[[project_coa_planner_performance]]`, `[[project_free_fight_ai_only]]`. |
| COA Commitment persistence (RMOOZ-COA-COMMIT-PERSISTENCE-M) | ✅ | `client/shell/free-fight-demo.js` · `scripts/test-coa-commit-persistence-m.js` (NEW) | **NEW 2026-06-16 (UI-state persistence only — NO combat/movement/DB-Lite/terrain/readiness/validator/teleport-guard/Staff-Safe/AI-planner change; the deterministic tick executor from -L is UNCHANGED so the no-LLM guarantee holds by construction).** The committed COA now **survives a browser refresh.** Persists `active_coa_execution_state` to **sessionStorage** (`rmooz_coa_exec_state`), **scenario-keyed** (`_scenarioKey()` = scenario id/name) — this is operator UI state, NOT scenario world-state or a journal write, so it does not touch the AI/sim boundary. `_persistCoaExec()` writes on every state change (commit · each tick / phase advance · pause · replan-block · clear); `_restoreCoaExec()` runs on `mount()` and brings the state back **PAUSED + `_restored:true`** (operator presses Run to resume — no auto-run, **no AI call on restore**). Restored fields are identical: selected_coa_id / current_phase_index / phase_status / unit_order_status / completed_orders / replan_required / replan_reason / ticks / stuck_ticks. UI: **"↺ Restored committed COA from session. Press Run to resume."** (`data-ff-coa="restored-note"`). Event log: committed / **restored** / phase advanced / completed / replan PAUSED (req #7). Safe clear (`_resetCoaExec`) removes the persisted key (req #8). **Stale-scenario guard:** a persisted COA from a different scenario is NOT restored (scenario_key mismatch). Tests: **coa-commit-persistence-m 8/8** (commit persists · refresh/mount restores same fields · restored ticks w/o LLM + no plan-coas fetch · clear removes persisted · phase advance persists · replan persists blocked · event-log lines · cross-scenario not restored); regressions green (coa-commit-execution-l 9 [unchanged] · speed-arch-j 11 · coa-planner 71 · continuous-loop 42 · coa-ui 29 · movement-execution 11 · panel-window-controls 65 · ai-config-guard 15 · cross-platform-run-scripts-g 10 · openrouter-cloud-mode 9 · ai-model-ready-state 18 · ux-proof 6 · card-visibility 7 · free-fight-ai 9). `?v=coa-commit-persist-m`. **Caveat:** sessionStorage is per-tab and cleared when the tab closes (survives refresh/reopen-of-tab, not a full browser quit) — a true cross-session/server-side store remains the next step if needed. `[[project_coa_planner_performance]]`, `[[project_free_fight_ai_only]]`. |
| COA-commit live-delay audit + cadence fix (RMOOZ-COA-COMMIT-LIVE-DELAY-AUDIT-N) | ✅ | `client/shell/free-fight-demo.js` · `scripts/test-coa-commit-live-delay-audit-n.js` (NEW) | **NEW 2026-06-16 (diagnosis + one targeted pacing fix + instrumentation — NO combat/AI-planner/DB-Lite/terrain/validator/movement-physics/Staff-Safe change).** Operator still felt "delay" after L+M. **Audit (clean local-only ollama env: health `configured_provider:ollama`, `model:qwen3-coder:latest`, `selection_source:env:RMOOZ_LLM_MODEL`, `model_available:true`; served JS `?v=coa-…` + all COA-commit markers).** Measured a committed run: per-tick **work=0ms**, persist stringify **0.001ms**, **0 `/plan-coas` fetches**, `llm_called_this_tick=false`. **ROOT CAUSE — tick CADENCE, not compute:** `_runCommittedCoa` scheduled deterministic ticks at the cinematic LLM-loop speed `_ffSpeed().moveAnimMs` → **x1 (default) = one step every 6000ms** (x5 2500 · x15 1000 · fire 450 · fire2 300). That 6s wait per step IS the felt delay (it existed to make the slow LLM loop watchable; pointless for instant deterministic execution). **FIX (only the bottleneck):** committed-COA cadence decoupled from the cinematic speed — `_coaExecTickMs()` = `max(120, min(COA_EXEC_TICK_MS=500, moveAnimMs))` → **x1/x5/x15 now tick every 500ms (12× faster at default)**, fire/fire2 still accelerate (450/120ms). Step size / clamp / teleport guard UNCHANGED (same moves, sooner). **Instrumentation (req #7, permanent):** per-tick `last_tick_timing` now carries `ui_update_ms`, `map_paint_ms`, `event_log_ms`, `storage_persist_ms`, `tick_interval_delay_ms` (+ existing work/trigger/llm) and the COA-exec card renders them — so any *remaining* browser cost (full `updatePanel()` re-render + Leaflet redraw each tick, which headless can't measure) is visible live to the operator. Tests: **coa-commit-live-delay-audit-n 6/6** (x1 brisk ≤600ms not 6000 · fire2 faster · timing spans exposed · 20 ticks still 0-LLM/0-fetch · UI shows the spans); regressions green (coa-commit-execution-l 9 · coa-commit-persistence-m 8 · speed-arch-j 11 · coa-ui 29 · continuous-loop 42 · speed-control 43 · panel-window-controls 65 · ai-model-ready-state 18 · gate-card-d 7 · ux-proof 6). `?v=coa-live-delay-n`. **Caveat / next:** if the operator's live `ui_update_ms`/`map_paint_ms` read high at fire speeds, the follow-up is a *light* per-tick DOM update (move only the changed markers + exec status) instead of the full-panel `updatePanel()` — flagged, not done (would be a larger surgical change; the cadence fix resolves the felt delay at the default speed). `[[project_coa_planner_performance]]`, `[[project_free_fight_ai_only]]`. |
| Unit Identity Resolver (RMOOZ-UNIT-IDENTITY-CONTRACT-A) | ✅ | `client/shared/unit-identity-resolver.js` (~480 L) | **NEW 2026-06-15; v2 owner correction same day.** The ONE shared unit-identity contract. Dual-mode (CommonJS `require` + `window.RmoozUnitIdentity`); pure, no DOM/network, never mutates raw. Exports `resolveUnitIdentity` / `normalizeSelectedUnit` / `displayUnitName` / `unitIdentityForLlm`. **v2 SEPARATE fields (not a swap):** `internalKey`/`uid` = system-linking key KEPT AS-IS (`R-047`) → no sim change; `canonicalId` = stable authored id (`fires-47`); `tacticalCode` = `R-047`; `displayName` = operator-facing platform/capability name (e.g. "Rocket Artillery Battery", or DB-Lite "F-16C") — NEVER the raw code or synthetic key; `platformLabel`/`typeLabel`/`capabilityLabel` so type never degrades to UNIT/generic. Authored name/platform always beats DB-Lite (injected via `opts.platformLabel`); stale copies reconcile vs the canonical scenario unit (`opts.scenario`). **All 7 surfaces consume it:** `adjudicator-map.js` markers, `shell/unit-status-panel.js`, DB-Lite enrichment (fed as opts), `shell/free-fight-demo.js` `normUnit`, server `ai/{free-fight-llm-capability-analyst,rmooz-ai-tool-contract}.js` OOB, `ai/commander-brief.js` ("Name (CODE)"), `shell/event-log.js` ("Unit selected: <displayName>"). Loaded `app.html` before consumers. Tests: identity-contract 14 · panel-identity 10 · ai-unit-identity 6. **v1 owner-rejected; v2 committed cleanly @`353e387` (resolver module visible there).** |
| Free Fight Demo (both subsystems) | ✅ | `shell/free-fight-demo.js` (~3.7k L, `?v=user-friendly-model-flow-a`) | **NEW (DOC-UNDERSTANDING-1 → Free Fight commander).** The big client surface for BOTH Free Fight subsystems: the symbolic action/reaction demo AND the **AI Commander** card (Generate AI Attack Plan + continuous loop). Movable-window panel, Map Movement Truth panel, **AI Planning Trace** UI, depth/mode selectors, Generate-5, the **operator-friendly model flow** (AI Model · Status · Select AI Model — `USER-FRIENDLY-MODEL-FLOW-A`), and **Advanced diagnostics** (collapsed) holding the 4-signal AI status card (execution gate / raw provider / model availability / local-only policy + the exact fix per block — `GATE-CARD-D`) + the raw model dropdown. Drives `/api/wargame-sim/free-fight/*`; AI-only gates + honest labels (§A rows 103–110). `window.RmoozFreeFightDemo`. **Display/demo only — no scenario/world-state mutation, no final tasking/weapons.** |
| Free Fight AI-Lite (deterministic offline planner) | ✅ | `shell/free-fight-ai.js` (~180 L) | **NEW.** Heuristic offline planner (nearest + capability + terrain/route feasibility) — review-only fallback when the LLM is unavailable. `window.RmoozFreeFightAI`. |
| Free Fight demo AI panel | 🟡♻️ | `shell/free-fight-demo-ai-panel.js` (~57 L) | Thin `renderDecision()` helper; the main logic moved into `free-fight-demo.js` (kept for Node tests). |
| AI Model HUD | ✅ | `shell/ai-model-hud.js` (~190 L) | **NEW (RMOOZ-LOCAL-MODEL-SELECTOR-A).** Always-on header local-model picker + route-health probe; cross-syncs with the Free Fight card via `rmooz:ai-model-changed`; now shows "FF blocked (gate/remote provider)" from `/api/ai/models` `provider_blocked` (a selected model never implies FF can run). `window.AppShellAiModelHud`, mount `#ai-model-hud-mount`. SoT = `ai/model-selection.js` (§C). |
| Symbol Identity resolver (global) | ✅ | `shell/symbol-identity.js` (~110 L) | **NEW.** `window.RmoozSymbolIdentity` — global symbol-identity bridge for Free Fight unit intel. |
| Unit Intel Normalizer | ✅ | `shell/unit-intel-normalizer.js` (~214 L) | **NEW.** `window.RmoozUnitIntelNormalizer` — Arabic unit-intel symbol + platform-identity normalization for Free Fight. |
| SIDC Preview bridge | ✅ | `shell/sidc-preview.js` (~128 L) | **NEW.** `window.RmoozSidcPreview` — review-only SIDC code preview. |
| Demo Scenario Preview | ✅ | `shell/demo-scenario-preview.js` (~525 L) | **NEW (AI decision demo preview).** `window.RmoozDemoPreview` — sandbox scenario painter for the AI decision demo; review/preview only, no mutation. |
| Domain-aware demo movement | ✅ | `shell/domain-movement.js` (~135 L) | **NEW.** `window.RmoozDomainMovement` — terrain/domain-aware movement for the free-fight demo glide. |
| COA Realism / Sovereign-Territory Gate (RMOOZ-COA-REALISM-GATE-A) | ✅ | `shell/coa-realism-gate.js` (~253 L) · `shell/free-fight-demo.js` (6 edit sites) · `server/ai/free-fight-action-engine.js` (`opts.realismGate`) · `app.html` · `test-free-fight-coa-realism-gate-a.js` (NEW, 15/15) | **NEW 2026-06-22.** Pure heuristic gate that enforces COA physical realism — sovereign territory, water-crossing domain, objective-capture feasibility, and input-completeness scoring. Loaded BEFORE `free-fight-demo.js` so `window.RmoozCoaRealismGate` is present at init; dual CommonJS+window export; no DOM/fetch/LLM. Five exports: `classifyPosition(lat,lon)` (theatre bbox priority order: Arabian Gulf→Iran→Arabian Peninsula/GCC); `validatePlacement(unit)` (BLUE in Iran/RED territory → blocked unless `forward_deployed:true`; water placement NOT checked here — false positives on coastal UAE/Qatar); `validateMovementStep(from,to,opts)` (BLUE into Iran → held; any unit into Gulf without naval/airlift → held; amphibious landing without amphibious mode → held); `gateObjectiveCapture(side,outcome,movementLog)` (blocks `objective_control='red'` when movementLog has unresolved violations); `scoreInputCompleteness(brief)` (8-field check → score 0-100, label 'template'/'partial'/'complete'). Wired into `free-fight-demo.js`: (1) `_movementValidationLog[]`/`_domainHeldUids{}`/`_placementValidation[]` state vars; (2) `_getCoaRealismGate()` helper; (3) domain gate injected in `_resolveCoaMoves` chokepoint (per-step, adds `distance_km`/`domain_validation` to move records); (4) `_logExecutedMoves` shows `distance_km`/`[DOMAIN_BLOCKED:...]`; (5) `_whiteScenarioOutcome` gates RED capture; (6) `_validateAllPlacements()` + `_newScenario` reset + 4 test seams. `free-fight-action-engine.js` gains an optional `opts.realismGate` injection point. Tests: **15/15** (classifyPosition · validatePlacement BLUE-in-Iran/forward_deployed/GCC · validateMovementStep Gulf-blocked/Gulf-naval-ok/BLUE-into-Iran · captureGate with/without violations · inputCompleteness empty/full · integration via demo seams). `[[project_offline_agent_architecture]]` |
| Objective X canonical source-of-truth (RMOOZ-OBJ-CANONICAL-A) | ✅ | `shell/free-fight-demo.js` (2 edit sites: `setObjective` + `_buildAiRequestBody`) · `test-free-fight-objective-canonical-a.js` (NEW, 10/10) | **NEW 2026-06-22.** Bug fix: operator-placed Objective X is now the canonical COA objective. Previously `_buildAiRequestBody` prioritised `sc.obj` (loaded scenario) over `_objective` (operator-placed). Fix: (1) Priority inverted — explicit operator sources (`user_marked_demo_objective` / `reused_previous` / `opts`) win first; `sc.obj`/`sc.objective`/`sc.objectives[0]` is fallback; brief-derived and payload are last resort. (2) `setObjective(latlon)` now syncs `window.RmoozScenario.scenario.obj` to the placed point, preserving old value as `_previous_objective`; `setObjective(null)` restores from `_previous_objective`. Added test seams `_getObjectiveSourceForTest` + `_getObjectiveForTest`. Tests: **10/10**. |
| Free Fight side-role logic (RMOOZ-SIDE-ROLE-A) | ✅ | `shell/free-fight-demo.js` (`_staffSafeCommanderCoa` rewrite · `_autoDirectorBuildCoa` · `_autoDirectorNextBlueOrder` · `_redManeuverOrder` · `_sccActionTargets` · `_sccActionTargetsForTest` seam) · `server/ai/free-fight-coa-planner.js` (freedom-mode system prompt) · `test-free-fight-side-role-a.js` (NEW, 9/9) | **NEW 2026-06-22.** Rule: RED is always the attacker; BLUE is always the defender of Objective X. `_staffSafeCommanderCoa` now branches by side — RED gets `assault/recon/support/screen/reserve` roles with NE-axis targets (approach from RED_BASE_DEG); BLUE gets `screen/intercept/defend/reinforce/reserve` roles with NE-facing defensive positions (blocking/screening line facing Red axis). `_autoDirectorBuildCoa` (auto-director) changed from `_assaultRing`→`_blockingRing` and role `assault`→`intercept` for BLUE. `_redManeuverOrder` changed role label from `posture` to `'assault'` (offensive). `_sccActionTargets` now returns side-aware `target_type` (RED→`attack/recon/support/screen`; BLUE→`defend/intercept/screen/observe/reinforce`). Event log records: "RED ATTACK COA generated against Objective X" and "BLUE DEFENSE COA generated to defend Objective X". freedom-mode system prompt in `free-fight-coa-planner.js` restricted by side (BLUE: no attack/assault; RED: no defend/intercept). Tests: **9/9** (RED offensive roles · RED targets NE · BLUE defensive roles · BLUE targets NE · event log RED · event log BLUE · SCC RED target_type · SCC BLUE target_type · planner side-specific). Zero regressions (auto-director 11/11 + objective-canonical 10/10). |

---

## B. Wargame / adjudicator visualization (`UI_MOdified/client/wargame/`)

| Module / Feature | Status | Key file | Notes |
|---|---|---|---|
| Adjudicator Map | ✅ | `wargame/adjudicator-map.js` (~5.3k L) | Leaflet renderer: 4 BLS semicircles, OBJ target, Red units, pipeline route, Blue destruction choreography, breach badges. **Now also hosts the W3 presentation suite** (rows below): attrition, echelon roll-up, event pins, movement trails, legend, symbol scaling/resolver. |
| `drawScenario()` | ✅ | same | Builds overlay; caches bls_template + objCoord; registers units. Markers emit selection (see **D1**). |
| `applyState()` per-step | ✅ | same (`bls_status` loop ~`:4608`) | Full per-step wiring: bls_status colors, breach badge, blue-destroyed fade/stagger, red-degraded updates, forward-merge/rewind-replace. See **D2**. |
| Adjudicator HUD | ✅ | `wargame/adjudicator-hud.js` (~2k L) | `renderStep()` → `applyState`; timeline scrubber re-walks history. |
| Contact halo | 🟡 | `wargame/adjudicator-map.js` (`updateContactHalo`) | Wired into applyState but implementation minimal/placeholder. |
| AOR phase line | 🟡 | `wargame/adjudicator-map.js` (`updateAorPhaseLine`) | W3-rich only; plumbing intact, not fully verified. |
| Per-step attrition visuals (AN1) | ✅ | same (`computeStepAttrition` :3825, `applyStepAttrition` :3876) | Maps the scenario's OWN per-step `affected[]`/`engagement_arcs[]` `status_change` → DESTROYED/DEGRADED marker treatment (reuses `renderMarkerByStatus`, same as the live HUD); cumulative + reversible; wired into `applyStepProgress`. No scenario mutation, no fabricated combat fields; no-op for non-W3. Tests 26/26 (`test-an1-attrition-visuals.js`). Implements problem #2 of the P0B animation-readiness audit. (`244cc39`) |
| Echelon roll-up | ✅ | same (`buildEchelonDivisionGroups` :742, `renderEchelonRollup` :794, `buildAggregateIcon` :774) | CMO-style division↔units aggregation: at wide zoom a division's units collapse to one aggregate icon, expanding on zoom-in. Toggle `setEchelonRollup`; wired into draw (:2028), `applyStepProgress` (:5019), and zoom. Additive/reversible (hides via CSS class, no marker-pipeline mutation); no-op for non-W3. Tests 20/20 (`test-an-echelon-rollup.js`). (`4d0d463`) |
| Event pins (AN2) | ✅ | same (`renderEventPins` :975) | Per-step on-map event pins with provenance (actor / intended effect / doctrine) from the scenario's own step data. Wired into `applyStepProgress` (:5021). No mutation. Tests 25/25 (`test-an2-event-pins.js`). (`7c7cfb7`) |
| Movement trails (AN4) | ✅ | same (`renderMovementTrails` :1026) | Per-step movement-trail polylines from `*_unit_step_prev`/`_coords`; seeded at step 0 (:2030), updated on step change (:5024) and zoom (:1622, hide/show by roll-up). Tests 18/18 (`test-an4-movement-trails.js`). Closes a P0B gap. (`d7f7b72`) |
| Engagement legend + formation guide | ✅ | same | On-map legend for engagement-arc colors/arrowheads + a formation guide overlay. Test `test-an3-arrowheads-legend.js` (⚠️ committed but has uncommitted edits in the working tree). (`7f36574`) |
| Symbol scaling + formation hover-peek | ✅ | same (`setFormationPeek` :862) | Echelon-scaled unit symbols + hover to "peek" (temporarily expand) a rolled-up formation. Tests 18/18 (`test-unit-scaling-hover.js`). (`4da9a41`) |
| SIDC family-symbol resolver (SYM2) | ✅ | same | Remaps the 36 W3 units whose specific child SIDC entity codes milsymbol 2.0.0 rejects → their canonical parent family symbol (naval / air-defense / etc.), so they render a proper glyph instead of a generic diamond/square. Honest category-level only. Tests 21/21 (`test-sym2-unit-symbol-resolver.js`); root-cause audit `docs/unit-symbol-fidelity-audit.md` (SYM1, 13/13). (`bc0e861`) |
| Engagement mission graphics (MG1) | ✅ | same (`renderEngagementMissionGraphics`) | Renders each step's `engagement_arcs` as APP-6-style **mission graphics**: **ONE side-coloured axis arrow per side** (centroid of all actor→target arcs — Red "Axis of attack", Blue "Counterattack axis"), built via the shared `createManeuverArrowPolygon` (polyline + `makeArrowhead` fallback). Wired into `applyState`, `applyStepProgress`, and a zoomend re-render. **2026-06-01: made GLOBAL** — the engagement animation (mission graphics + salvo launches) is now gated on **DATA** (`scenarioHasEngagementArcs`), not the `w3-rich` tag; any scenario carrying `engagement_arcs[]` animates, and `updateAttackArrows` defers its static chevrons to the animated path on a data basis. Owner ruling: animations are global (follow CMO model). Tests reconciled to this grouped renderer: `test-pr-mg1-grouped.js` (27/27) + `test-mg1-mission-graphics.js` (27/27). *(Note: tender-noether's old TMG-bridge variant — `buildReadonlyTmgMarker`/`getTmgIconOptions` — was dropped in the merge; that app.js bridge still exists but is no longer the MG1 renderer.)* |
| Cesium 3D globe view | ✅ | `wargame/cesium-view.js` (~570 L) | Parallel 3D renderer to the 2D Leaflet map (CesiumJS 1.116, lazy-loaded from CDN on first toggle). `window.AppCesiumView` API: `drawScenario`/`applyState`/`clearScenario`/`setVisible`/`toggle`/`enterPlaceMode`/`loadCesiumAssets`/`isVisible`. Unit billboards (milsymbol SIDC), altitude-by-domain, parabolic engagement arcs + salvo animation, step-sync. Driven by `adjudicator-map.js` via `window.AppCesiumView.applyState(...)` (synced only when 3D visible, :5423) reading `_lastState`/`_lastScenario` getters (:5858). Toggled by `adjudicator-hud.js` `toggle3DGlobe()` (:824, `#wg-adj-3d-btn`). **Keep in sync with 2D map changes — `[[feedback_keep_cesium_3d_in_sync]]`.** From `claude/tender-noether-123cf0`, preserved in the 2026-06-01 consolidation. |
| DEM-backed 3D terrain | ✅ | `wargame/cesium-view.js` (`makeTerrainProvider`) | `CustomHeightmapTerrainProvider` (WebMercator tiling) fetches real Libya relief from `/api/dem/heights/{z}/{x}/{y}`; `scene.verticalExaggeration = 1.6`. **Graceful flat fallback** if DEM/network absent (zero regression — degrades to prior ellipsoid globe). Visual verification still pending (needs `libya_demx5.tif` + Cesium CDN). |
| DEM 2D overlay layer | ✅ | `client/dem-layer.js` (107 L) | 2D Leaflet hillshade overlay; `window.DemLayer` (`toggle`/`show`/`hide`/`setOpacity`/`queryElevation`/`fetchMeta`). Wired to `#dem-toggle-btn` ("Terrain") + `#dem-opacity-slider` in the adjudicator HUD overlay. |
| Coverage / threat rings (P1, CMO-style) | ✅ | `wargame/adjudicator-map.js` (`coverageRingRadiiKm`, `renderCoverageRings`) + `cesium-view.js` (`renderCoverageRings`) | **2026-06-01.** Per-unit sensor-coverage (dashed, no fill) + weapon-threat (solid, faint fill) rings. **Radii come from the SAME range model the sim uses — the RMOOZ DB-Lite**: `shell/detection.js` `sensor_class[].ref_range_nm` + `shell/engagement.js` `weapon_class[].max_range_nm`, read live off `window.AppDetection`/`window.AppEngagement` (those two modules are now loaded in `app.html` before the map; small in-sync fallback mirror for Node tests). Resolution order: explicit km on the unit (`sensor_range_km`/`weapon_range_km`/`threat_range_km`) → declared `sensors[]`/`weapons[]` components (DB by class) → **the committed DB-Lite capability catalog** (`shell/world-state-db.js` → `AppWorldStateDB.enrichUnit`, now loaded in `app.html` before the map) classifies role/domain → a `sensors[]`/`weapons[]` profile (`classifyKind`: air_defense / naval_combatant / ground_maneuver / air_unit / ew_site / generic), then the DB lookup applies (km is the DB's, nm→km ×1.852, no invented numbers). `enrichUnit` **never overwrites authored components**, so a unit's own sensors/weapons still win. This is the SAME catalog the detection/engagement engines use — **no parallel `RING_ROLE_CLASS` table in the renderer** (deleted 2026-06-01). Tooltip tags the resolved DB class (e.g. `[long_range_sam]`). `generic`/unclassifiable units draw nothing (no fabrication). **OFF by default**, toggled via `#wg-adj-rings-btn` → `window.AppAdjudicatorMap.{toggle,set,isVisible}CoverageRings`; dedicated non-interactive pane (z 415); wired into `applyStepProgress`; skips not-appeared + destroyed units. **No scenario mutation, no fabricated combat-state fields** (read-only overlay). 3D parity in `cesium-view.js` (ground-clamped ellipses) driven by the 2D toggle + 2D DB lookup as the single source of truth (`[[feedback_keep_cesium_3d_in_sync]]`). Tests 41/41 (`test-coverage-rings.js`, requires the real DB1 catalog + a guard that the fallback range mirror equals the live DB). The old hardcoded `RED_405EW` EW halo was generalized to all EW/SIGINT emitters (`findEwEmitters`). *(Two earlier drafts retired: invented domain/role/echelon estimates → DB ranges via a renderer-local role table → the committed DB1 catalog, per owner: "from the DB, not by random" + "use the work already in the repo.")* |
| Firing-solutions overlay (ENG1 live) | ✅ | `wargame/adjudicator-map.js` (`computeEngagementRecords`, `renderEngagements`, `getEngagements`) + `cesium-view.js` (`renderEngagements`) | **2026-06-01.** Wires the previously-orphaned **ENG1** engine (`shell/engagement.js` `computeEngagements`) into the live map so the operator sees **who currently has a valid shot at whom** — a detection-gated, WRA/range/magazine/fire-control-gated firing picture that shifts as units move. Reuses the SAME enriched units + contacts the DET1 overlay feeds (`AppWorldStateDB.enrichUnit` → `AppDetection.computeContacts`), then runs `AppEngagement.computeEngagements`. Draws a shooter→target dotted line per **`engaged`** candidate, coloured by the shooting side (blue/red), weight ∝ Pk; tooltip tags weapon class · Pk% · salvo · range. The full record set (incl. `blocked` + `reason`: out_of_range / weapons_hold / winchester / no_fire_control_channel) is available via `getEngagements(state)` for diagnostics/AI. **COMPUTED firing solutions — distinct from the scenario's authored `engagement_arcs`** (adjudicated kill outcomes drawn elsewhere). **OFF by default**, toggled via `#wg-adj-eng-btn` → `window.AppAdjudicatorMap.{toggle,set,isVisible,get}Engagements`; dedicated non-interactive pane `rmoozEngagementsPane` (z 425, below contacts/above rings); wired into `applyStepProgress` after the contacts. **No scenario mutation, no fabricated combat-state fields**; the engine clones magazine state and never mutates input (read-only overlay). 3D parity in `cesium-view.js` (ground-clamped dashed polylines) driven by the 2D toggle + `getEngagements(state)` as the single source of truth (`[[feedback_keep_cesium_3d_in_sync]]`). Tests 28/28 (`test-engagement-overlay.js`: real engine behaviour — in-range→engaged, far→blocked out_of_range, undetected→no record, hold→weapons_hold, own-side/weaponless→none — + read-only/off/no-fabrication source guards). |
| Air-defense Coverage Summary Panel (Phase 5C) | ✅ | `wargame/coverage-summary.js` (200 L) | **NEW 2026-06-03.** Read-only air-defense coverage composition panel. Groups units by category (SAM / AAA / Early Warning Radar) and shows approximate ranges. Helps operators understand coverage composition without engagement simulation. `window.AppCoverageSummary` — `gatherCoverageData(scenario)` + `renderPanel(scenario)`. Loaded `app.html:4823`. No mutation, no scenario write. Part of DB-Lite / coverage enrichment chain (Phase 5B-C/5C). |
| Detection contacts overlay (DET1 live) | ✅ | `wargame/adjudicator-map.js` (`buildDetectionUnits`, `renderDetectionContacts`, `getDetectionContacts`) + `cesium-view.js` (`renderDetectionContacts`) | **2026-06-01.** Wires the previously-orphaned **DET1** engine (`shell/detection.js` `computeContacts`) into the live map so each side's sensor picture appears/fades as units move each step — the core CMO "feel alive" behaviour. Per step it builds pseudo-units from the live markers' CURRENT positions (`getLatLng` → `[lon,lat]`), enriches each via the **SAME DB1 catalog** the rings use (`AppWorldStateDB.enrichUnit` → `sensors[]`/`rcs_class`), runs `computeContacts`, and draws a small marker on each DETECTED target coloured by the HOLDING side (blue `#3a96d2` / red `#c41e1e`; **firm** = filled solid, **tentative** = dashed hollow); tooltip tags method (radar/esm)·confidence·range·classification. Detection physics (radar-horizon, RCS scaling, EMCON, ESM passive) and ranges are 100% the engine's — nothing invented in the overlay. Skips not-appeared + destroyed units (no ghost contacts); own-side never appears (engine-gated). **OFF by default**, toggled via `#wg-adj-contacts-btn` → `window.AppAdjudicatorMap.{toggle,set,isVisible,get}DetectionContacts`; dedicated non-interactive pane `rmoozContactsPane` (z 430, above rings); wired into `applyStepProgress` after the rings. **No scenario mutation, no fabricated combat-state fields** (read-only overlay). 3D parity in `cesium-view.js` (ground-clamped points) driven by the 2D toggle + `getDetectionContacts(state)` as the single source of truth — Cesium re-runs no detection of its own (`[[feedback_keep_cesium_3d_in_sync]]`). Tests 30/30 (`test-detection-contacts.js`: real engine behaviour + read-only/off-by-default/no-fabrication source guards). First consumer of the orphaned `shell/*` engine layer on the live UI. |

---

## C. Server — AI agents + sim boundary (`UI_MOdified/server/ai/`, `server/sim/`)

All ✅ done & wired unless noted. Persistence/endpoint in Notes.

| Module | Endpoint(s) | Notes |
|---|---|---|
| Adjudicator Agent (`ai/adjudicator-agent.js`) | `/api/sim/propose`, `/api/ai/adjudicate` (legacy shim) | Per-step LLM transition; 3-layer validation; `propose` = no mutation; `commit` writes journal. |
| Red/Blue-Team Agent (`ai/red-team-agent.js`) | `/api/ai/red-team/propose`, `/api/ai/blue-team/propose` | Perspective-flipped tactical actions; rule-validated; no mutation. |
| COA Agent (`ai/coa-agent.js`) | `/api/ai/coa` | 3–5 courses of action; schema-validated. |
| Terrain API (`server/terrain-api.js`) + DEM service (`server/dem-service.js`) — **NEW/REWIRED** | `GET /api/terrain/health`, `GET /api/terrain/elevation?lat&lon`, `POST /api/terrain/profile` | **GIS-TERRAIN-1 T-1+T-2 (2026-06-12,** design `docs/gis-terrain-integration-design.md`**):** read-only endpoints over the formerly **orphaned** dem-service (drift resolved — it was unwired); elevation stamps `source.type=terrain_layer`, profile (distance/min-max/slope summary) stamps `gis_analysis` + `needs_review` (interim 15°/30° thresholds pending G-6 doctrine cards); **graceful without a DEM** (`DEM_PATH` env). Planning Model: `terrain_layer`(3)/`gis_analysis`(2) source types, `terrain_layers[]`/`terrain_analysis[]` + attach helpers. Hillshade `renderTile`/`renderHeights` exist in dem-service but have **no route yet** (T-4/T-5). Test: `scripts/test-terrain-api-1.js` (13). |
| Terrain Analysis v1 (`server/terrain-analysis.js`) — **NEW** | — (server helpers; consumers wired in T-4) | **GIS-TERRAIN-1 T-3 (2026-06-12):** deterministic products on T-1/T-2 — point · route (distance/profile/slope, `mobility_warning` none/slow_go/no_go/unknown) · area (8×8 grid, polygon-ring filter, neighbor-pair slope) · landing-zone suitability (flatness/size/optional coastline → suitable/marginal/unsuitable/unknown, **never invents: unknown stays unknown**); ALL thresholds interim-flagged pending G-6 doctrine cards; structured degradation without DEM (`dem_not_configured`/`no_terrain_data`); PM attach helpers `attachRouteTerrain`/`attachLocationTerrain`/`attachLandingZoneTerrain` → `terrain_analysis[]` with `ref_kind`/`ref_id` (inputs never mutated, provenance preserved). Test: `scripts/test-terrain-analysis-1.js` (11, incl. forced-missing-DEM child proc). |
| Scenario Loader (`ai/scenario-loader.js`) | `/api/ai/scenarios`, `/api/ai/scenario/:name` | mtime-cached; validates on load; enriches in-memory only. |
| Scenario Validator (`ai/scenario-validator.js` + `scenario-schema-spec.js`) | `/api/scenario/import` | Parametric step/unit ranges; `{ok,errors,warnings}`. **DOC-UNDERSTANDING-1 (2026-06-10):** per-side caps raised to **500** (`red_units`/`blue_units_initial` + new optional `neutral_units`); retired the W3-only 200 red cap; validation NOT removed (501 still errors). Test: `scripts/test-unit-limits-500-1.js`. |
| Scenario Normalizer (`ai/scenario-normalizer.js`) — **NEW** | — (called by `/api/wargame-sim/import` before save) | **DOC-UNDERSTANDING-1 / Phase C (2026-06-10):** deterministic per-side cap ≤500 before save. Preserves phase-referenced / HQ / air-defense·sensor·HVA / objective-linked / BLS-linked units; aggregates duplicate support units; never invents; emits before/after + preserved/aggregated/dropped report. No-op when under cap. Test: `scripts/test-scenario-normalizer-1.js`. |
| Document Classifier (`ai/document-classifier.js`) — **NEW** | — (JS gate, pre-generation) | **DOC-UNDERSTANDING-1 / Phase D (2026-06-10):** deterministic Arabic-first doc-type classifier (أمر إنذاري / OPORD / FRAGO / Intel / ORBAT / Mixed / Unknown); diacritics-folding normalize; side-presence + order-structure flags. Test: `scripts/test-document-classifier-1.js`. |
| Operational Brief assembler (`ai/operational-brief.js`) — **NEW** | `POST /api/wargame-sim/analyze` (read-only) | **DOC-UNDERSTANDING-1 / Phase D (2026-06-10):** sha256 slot-dedupe → one *Mixed Operational Document*; Arabic/English side-segmenter; `analyzeDocuments()` extracts text → classify → dedupe → side-separate → seed brief + ambiguities; returns the AI-Understanding payload. LLM fill deferred to deployment (`llm_fill.available=false`). **Phase F:** `/analyze` also accepts a JSON body — `classifyJsonInput` detects Operational Brief / RMOOZ Scenario / Unknown; `validateBrief`/`normalizeBrief`/`unknownToBrief`/`understandingFrom{Brief,Scenario}`. Tests: `scripts/test-operational-brief-1.js`, `test-document-understanding-1.js`, `test-phase-f-generate-1.js`. **MULTI-COUNTRY-A (2026-06-13):** `classifyJsonInput` now also detects `multi_country_step1` (per-country ORBAT JSON / `{sheets}`); `emptyBrief`/`normalizeBrief` carry the additive coalition layer (`coalitions`/`participants`/`countries`/`country_orbats`/`country_bases`/`coalition_totals`); `understandingFromBrief` adds the coalition rollup + "Coalition Step 1 ORBAT" label. Test: `scripts/test-multi-country-orbat-a.js` (20/20). |
| Operation Templates (`ai/operation-templates.js`) — **NEW** | — | **DOC-UNDERSTANDING-1 / Phase F:** phase skeletons + per-side roles/geometry for `amphibious_landing` (عملية إبرار, 4 draft phases prep→approach→landing→secure), `attack_objective`, `defend_objective`, `reconnaissance`, `air_defense`; `inferTemplateId` from brief text. |
| Brief→Scenario Generator (`ai/brief-to-scenario.js`) — **NEW** | `POST /api/wargame-sim/generate` | **DOC-UNDERSTANDING-1 / Phase F:** deterministic, NO-LLM `generateScenarioFromBrief()` — RMOOZ generates a DRAFT scenario from the *reviewed* brief + template; requires operator objective coords (else `requiresObjective` → UI asks to set on map); units are draft/needs-review, low placement confidence, missing-fields list; output normalized (≤500/side) + schema-validated before save. `/generate` also loads a posted RMOOZ Scenario JSON (validate+normalize+write+set-active). Wired to the review screen's "Generate Scenario" (no generation from raw chunks/LLM text). Test: `scripts/test-phase-f-generate-1.js`. |
| XLSX reader (`ai/xlsx-text.js`) — **NEW** | — (used by `/api/wargame-sim/analyze` workbook path) | **DOC-UNDERSTANDING-1 / MULTI-COUNTRY-A (2026-06-13):** dependency-free `.xlsx`→`{sheets:[{name,rows}]}` via Node `zlib` (no npm `xlsx`) — generalizes `docx-text.js`'s ZIP walk to read `workbook.xml` + `workbook.xml.rels` + `sharedStrings.xml` + each worksheet (shared/inline/number/bool cells, column-letter→index). Never throws. Test: `scripts/test-multi-country-orbat-a.js`. |
| Multi-country coalition ORBAT (`ai/multi-country-orbat.js`) — **NEW** | `POST /api/wargame-sim/analyze` (kind `multi_country_step1`; `workbook_base64` or `{countries[]}`) | **DOC-UNDERSTANDING-1 / MULTI-COUNTRY-A (2026-06-13):** many-vs-one coalition Step 1 model. Default side map (Iran→RED; UAE/Qatar/Bahrain/Kuwait/Oman/KSA→BLUE; `sideOverrides` to override; unknown→UNKNOWN, flagged). `parseCountrySheets` (Arabic+English section/column heuristics) → countries w/ air/naval/land bases + platform lines + coords; `buildMultiCountryStep1`/`buildBriefFromMultiCountry` emit `coalitions`/`participants`/`countries`/`country_orbats`/`country_bases`/`coalition_totals` and **project down** into `proposed_units`/`placement_candidates`/`enemy_bases` (RED) so existing Step 1 consumers work unchanged. **Review/map/demo foundation only:** every unit/base/anchor `needs_review:true` + `exact_unit_position:false`; **no final scenario units, tasking, COA, or adjudication.** Fixture: `devtools/fixtures/multi-country/make-fixture.js` (dependency-free `.xlsx` writer; 7 country sheets). Test: `scripts/test-multi-country-orbat-a.js` (20/20). **MULTI-COUNTRY-DEMO-A (2026-06-13):** proposed_units now carry `source_type:'external_excel_orbat_candidate'` (canonical review-only candidate marker); consumed by the demo layer below. |
| JSONC parser (`ai/jsonc.js`) — **NEW** | — (used by bridge `readJsonBody`) | **MDMP-EXTERNAL-1 / G-1 (2026-06-11):** string-aware JSONC cleaner (`//`, `/* */`, trailing inline comments, trailing commas) so external staff-tool files parse without corrupting Arabic narrative; unparseable `/analyze` body now → explicit 400 (no silent DOCX fallthrough). Test: `scripts/test-mdmp-external-detect-1.js`. |
| MDMP-external detection (`ai/operational-brief.js` `detectMdmp`) — **NEW** | `POST /api/wargame-sim/analyze` (kind `mdmp_external`) | **MDMP-EXTERNAL-1 / G-1:** key-fingerprint detection of the other app's MDMP-stage JSON (planning_guidance / staff_brief / coa_development / coa_analysis / coa_comparison). Fixtures: `devtools/fixtures/mdmp-external/`. |
| MDMP External Adapter (`ai/mdmp-external-adapter.js`) — **NEW** | `POST /api/wargame-sim/analyze` (single file or `{bundle:[…]}`) | **MDMP-EXTERNAL-1 / G-2 (2026-06-11, owner D1–D10 locked):** deterministic step3→2 BLUE COAs + `force_comparison` (9 categories + qualitative + S/W ×5); step4→`courses_of_action[].wargame_turns[]` (7 A/R/C event families, `white_decision`/`result` start EMPTY per D2) + RED most-likely COA (MD never invented, D3); step5→per-COA evaluation + `coa_recommendation` (`decided_by` operator-only, D9); placeholder scrub → `missing_information` (no-invention); `{file, keys[]}` citations per populated field. Brief schema additive: `courses_of_action`/`force_comparison`/`coa_recommendation` (survive `normalizeBrief`; surfaced in `understanding.coas`). Test: `scripts/test-mdmp-adapter-1.js`. |
| COA Review Panel (`client/shell/coa-review-panel.js`) — **NEW** | — (client; mounted by `doc-understanding-review.js` `data-el="coa-panel"`) | **DOC-UNDERSTANDING-1 / G-3 (2026-06-11):** commander-friendly COA cards from `brief.operational_brief.courses_of_action[]` — BLUE "Possibility N" first, Enemy Most Likely, Enemy Most Dangerous **placeholder** when missing ("not generated / needs Generate More", D3 never invented). Client-state Select/Reject/Edit/Compare (DOM-free state machine, node-tested); recommendation strip is **rationale-only, never auto-selected** (D9); **generation gate**: COAs present ⇒ operator-selected BLUE COA required (exact message), approval stamped as `approved_coa_id` + COA `status:'approved'` (survives `normalizeBrief`); Generate More disabled-pending (deployment LLM). Harness: `devtools/fixtures/doc-understanding/verify-coa.html` (+ `make-sample-coa.js`); browser-verified, 0 console errors. Test: `scripts/test-coa-review-panel-1.js` (11). **Next: G-4 Unit Tasking Mode** per `docs/coa-wargame-design.md`. |
| G-4 Unit Tasking Mode Overlay Gate — **DESIGN ONLY** | `docs/g4-unit-tasking-mode-design.md` | **DOC-UNDERSTANDING-1 / G-4-A (2026-06-12):** locks Unit Tasking Mode as an **overlay-only** design gate, not live scenario mutation. Future implementation may edit only tasking intent fields (`action_component`, `action_what`, `action_why`, `action_intended_effect`, `action_doctrine_cited`, plus `step_index`/`phase` reference) after dry-run diff + explicit **"Approve tasking overlay"** user action. Append-only change log + single/all revert required. Explicitly forbidden: assigned objective commit, readiness/posture, movement/orders/coordinates/routes, final unit creation/placement, imported source/proposed_units/placement-anchor/world-state baseline/live-unit mutation. World State may project overlay for preview only and must label it separately from baseline-derived tasking. No UI/runtime code in G-4-A. |
| G-4 Tasking Overlay Store — **PURE / UNWIRED** | `shell/tasking-overlay-store.js` | **DOC-UNDERSTANDING-1 / G-4-B (2026-06-12):** pure UMD store for overlay data model, candidate validation, dry-run before/after diff, explicit approval contract (`dry_run_reviewed:true` + **"Approve tasking overlay"**), append-only change log, single-entry revert, revert-all, active-entry projection, and read-only tasking preview projection. **Not loaded by app.html; no UI controls, no backend/storage, no live apply, no world-state baseline mutation, no imported JSON/source mutation, no final units/placement/movement/readiness/posture changes.** Test: `test-g4-tasking-overlay-store.js` (38/38). |
| G-4 Tasking Overlay Preview Wiring — **READ-ONLY** | `app.html` + `shell/world-state.js` + `shell/tasking-overlay-store.js` | **DOC-UNDERSTANDING-1 / G-4-C (2026-06-12):** loads the pure overlay store before World State and exposes `AppWorldState.buildTaskingOverlayPreview(ws, overlay)` as an explicit preview-only path. The helper clones the input snapshot, leaves `derived.unit_tasking` baseline-derived, writes overlay output only to `derived.unit_tasking_overlay_preview`, and stamps preview metadata plus overlay entries with `read_only:true`, `overlay_only:true`, `baseline_mutation:false`. No imported-source mutation, no live scenario mutation, no persistent storage, no UI edit/approval controls, no backend calls, no final units/placement/movement/readiness/posture changes. Test: `test-g4-tasking-overlay-preview.js` (17/17). |
| G-4 Tasking Overlay Preview Indicator — **READ-ONLY DISPLAY** | `app.html` + `shell/unit-status-panel.js` | **DOC-UNDERSTANDING-1 / G-4-D (2026-06-12):** Selected Unit / Object Status Card current-orders area now reads only `ws.derived.unit_tasking_overlay_preview[uid]` and shows a display-only indicator: **"Overlay preview available"**, source label `g4_tasking_overlay`, `baseline_mutation:false`, plus `read_only:true` / `overlay_only:true` when present. It does not replace or mutate baseline `derived.unit_tasking`; it adds no edit controls, approve/apply buttons, backend calls, storage, imported-source mutation, live scenario mutation, or movement/readiness/placement changes. Test: `test-g4-tasking-overlay-preview-indicator.js`. |
| G-4 Tasking Overlay Dry-run Diff Viewer — **READ-ONLY DISPLAY** | `app.html` + `shell/unit-status-panel.js` | **DOC-UNDERSTANDING-1 / G-4-E (2026-06-12):** Selected Unit / Object Status Card current-orders area can render an already-built dry-run diff from `ws.derived.unit_tasking_overlay_dry_run_diff` / `ws.derived.tasking_overlay_dry_run_diff`. The viewer shows before/after values for allowed tasking fields only (`action_component`, `action_what`, `action_why`, `action_intended_effect`, `action_doctrine_cited`, plus `step_index`/`phase` references), metadata `read_only:true`, `overlay_only:true`, `baseline_mutation:false`, and forbidden-field warnings from blocked diff reasons. It adds no edit form, approve/apply control, backend call, storage, imported JSON/live scenario mutation, or baseline `derived.unit_tasking` mutation. Test: `test-g4-tasking-overlay-dry-run-diff-viewer.js`. |
| Planning Model (`ai/planning-model.js`) — **NEW** | — (used by `/api/wargame-sim/placement`; G-4 consumer) | **DOC-UNDERSTANDING-1 / G-3A (2026-06-12, recovered from 06-11 E-drive work):** ONE deterministic planning model from EITHER front door (L13/L14) — `fromOperationalBrief(brief)` / `fromWorldState(ws,scenario)` — + `mergePlanningModels()` fusion with surfaced conflicts (L15: operator-declared > reviewed > derived > AI-suggested; loser preserved, `needs_review`, never silently resolved). Every object carries `source {type,file,key,origin,confidence}` (enforced taxonomy). Pure/deterministic (no Date/RNG/IO). PATH B copies `ws.derived.unit_objective_links` (needs OBJLINK-B). Test: `test-planning-model-g3a.js` (43/43). |
| Location Intelligence Resolver (`ai/location-intelligence.js`) — **NEW** | `POST /api/wargame-sim/placement` (via bridge) | **DOC-UNDERSTANDING-1 / G-3B (2026-06-12, recovered):** location MENTIONS (Arabic/English, e.g. «قاعدة بندر عباس») → reviewable placement CANDIDATES: rung ladder (explicit coord > gazetteer/location_db > incident_log > passed-in llm placeholder), AO bbox check, incident awareness, staleness; every candidate `needs_review:true`, `known_base` ⇒ `exact_unit_position=false` + `base_known_exact_unit_position_unknown` warning — NEVER final placement, no invention. Offline/deterministic. Design: `docs/location-intelligence-design.md` (landed via origin `d117698` v3 rulings L14-L16). Test: `test-location-intelligence-g3b.js` (43/43). **GIS-TERRAIN-1 T-4A (2026-06-12):** opt-in `includeTerrain:true` (resolveMention + enrichPlanningModelLocations) attaches advisory `candidate.terrain` (terrain_available/elevation_m/own confidence/warnings/`terrain_layer` source/needs_review) to lat/lon candidates; default OFF = byte-identical G-3B (terrain stack lazy-loaded); advisory only — never rejects/moves/re-scores. Test: `test-location-terrain-t4a.js` (8). |
| Placement Candidates Panel (`client/shell/placement-candidates-panel.js`) — **NEW** | — (client; mounted by `doc-understanding-review.js` `data-el="placement-panel"`) | **DOC-UNDERSTANDING-1 / G-3C (2026-06-12, recovered):** read-only surfacing of G-3B candidates inside AI Understanding Review (sibling of coa-review-panel). Wizard `attachPlacement()` posts `{brief}` to `/api/wargame-sim/placement` (best-effort, never blocks review) → `payload.placement` → cards with type/confidence/AO/incident/warnings + needs-review badge + "exact unit position: yes/no" honesty line. **No Accept/Reject buttons — mutation surface deferred** (read-only by design per AI/sim boundary). Browser-verified (Bandar Abbas + Chabahar from real resolver). Test: `test-placement-candidates-panel-1.js` (15/15). **GIS-TERRAIN-1 T-4A-V (2026-06-12):** renders `candidate.terrain` when present (render-only — no browser terrain calls): availability/elevation/terrain-confidence/warnings/source.type + "Advisory only — للاستئناس فقط" label; `terrain_available:false` = amber warning chip, never an error; terrain-less candidates render byte-identically. Harness: `devtools/fixtures/doc-understanding/verify-placement-terrain.html`; browser-verified on DEM-less box. Test: `test-placement-terrain-t4av.js` (15). **MULTI-COUNTRY-A (2026-06-13):** map anchors now carry `country`+`side` in title/popup so RED-country vs BLUE-coalition anchors read distinctly (side already drives marker color). Render-only; falls back to `brief.placement_candidates`. |
| Base / Object Status Panel (`client/shell/base-status-panel.js`) — **NEW (inventoried 2026-06-12)** | — (client; opened from Step-1 base anchors + placement markers via `RmoozBaseStatusPanel.open`) | **DOC-UNDERSTANDING-1 / BASE-STATUS-A:** read-only panel for a base anchor — groups its **proposed** units by symbol category, shows side/type/location, evidence + doctrine warnings, and per-unit catalog status. **Review-only** (`needs_review:true`, `exact_unit_position:false`); creates NO final units/tasking/execution. Unknown platform → "Catalog required / يحتاج ربط بقاعدة البيانات". First concrete impl of the generic **Object Status Card** architecture (`docs/object-status-card-design.md`, `7e1338f`); a prior generic-panel unification was reverted (`86af86e`/`083afb1`). Now delegates systems lookup to SYMBOL-DB-B (below). Tests: `test-base-status-panel-a.js` (17/17, incl. SYMBOL-DB-B integration) + `test-step1-unified-bases-map-anchors.js` (STEP1-C, 6/6 — unified Step-1 bases + map anchors). **MULTI-COUNTRY-A (2026-06-13):** `allBases()` also reads `country_bases` (BLUE coalition bases resolve); the card now renders **Country** + **Side** rows and a country chip. Still read-only. |
| Symbol DB — proposed-unit categorizer (`client/shell/symbol-db.js`) — **NEW** | — (client UMD `window.RmoozSymbolDB`; Node-requireable; used by base-status-panel) | **DOC-UNDERSTANDING-1 / SYMBOL-DB-B (2026-06-12):** `categorize(unit)` enriches a review-only proposed unit with `symbol_category`/`platform_class`/`platform_name`/`catalog_match_status`/`catalog_confidence`/`capability_summary`/`sensors[]`/`weapons[]`/`magazines[]`/`unknown_fields[]`/`needs_review`. Category = SYMBOL-DB-A ladder; **systems sourced ONLY from the DB1 catalog** (`world-state-db.js` `classifyKind`/`capabilityFor`) — **never invented**. Status ladder: `declared` (operator) > `matched` (named platform) > `role_class` (generic role profile — flagged "verify") > `category_only` / `unknown` → systems `[]` + "Catalog required / needs_review". Pure/deterministic (no Date/RNG/IO/LLM/DOM), read-only. Test: `test-symbol-db-b.js` (37/37, incl. a no-invent invariant: every systems array is empty or a byte-for-byte DB1 clone). |
| Symbol Registry — object/base/platform symbols (`client/shell/symbol-registry.js`) — **NEW** | — (client UMD `window.RmoozSymbolRegistry`; Node-requireable; used by placement-candidates-panel + base-status-panel) | **DOC-UNDERSTANDING-1 / SYMBOL-DB-B symbol mapping (2026-06-13):** maps object_type/base_type → display symbol descriptor `{glyph, label_en, label_ar, kind}` for **air_base ✈ / airport / naval_base ⚓ / port / land_base ▣ / friendly_trial_anchor ◇ / base_facility ⬢ / radar_site ◎ / air_defense_site ⊕ / logistics_node ▦ / hq ★ / objective ◉ / infrastructure ⌂ / unknown ?** + platform categories (air_fighter/air_attack/air_transport/maritime_patrol/helicopter/uav/naval_surface/submarine/ground_unit/air_defense/radar/logistics/unknown). `normObjectType` reads `site_type\|base_type\|anchor_type\|placement_type`; `resolveBaseSymbol` two-tier fallback (base-ish unmapped → base_facility + warning; truly unknown → unknown + warning); `iconHtml(sym,{side})` builds the anchor divIcon. **Replaces the generic "B" base marker** — Step 1 anchors now render their base-type symbol (placement-candidates-panel delegates here; local glyph guard if unloaded). Base Status Panel adds a **Symbol** section (object_type/base_type/symbol/symbol_category/symbol_source/catalog_match_status + fallback warning). Distinct from milsymbol (`symbology.js`) — review/display only, no final units/markers-per-aircraft. FUTURE note (not built): sortie lifecycle parked→taxi→runway→airborne→enroute→mission→return→landed. Test: `test-symbol-registry-a.js` (11/11). |
| DOCX text extractor (`ai/docx-text.js`) — **NEW** | — | **DOC-UNDERSTANDING-1 / Phase D:** dependency-free .docx→text via Node `zlib` (no python/npm); offline. Validated on real Arabic fixtures. |
| AI-Understanding review renderer (`client/shell/doc-understanding-review.js`) — **NEW** | — (client) | **DOC-UNDERSTANDING-1 / Phase E:** shared renderer for the pre-generation review screen (type · mission · friendly/enemy/neutral · objectives · phases · constraints · proposed counts/bounds · ambiguities + Generate/Edit/Upload-More/Cancel). Used by the import wizard (`scenario-import-wizard.js`, "Review AI Understanding" button) + standalone `client/doc-understanding-verify.html`. Browser-verified. **MULTI-COUNTRY-A (2026-06-13):** additive `renderCoalitionRollup()` — countries detected, RED/BLUE country counts, per-country base+proposed breakdown, per-side coalition totals; renders only when `understanding.coalition`/`brief.countries` present (non-coalition Step 1 output byte-unchanged; STEP1-C substrings intact). **MULTI-COUNTRY-DEMO-A (2026-06-13):** adds a "Demo Movement — حركة عرض (demo only)" button (coalition briefs only) that calls `RmoozDemoMovement.mount(payload)`. |
| Demo units converter (`client/shell/demo-units.js`) — **NEW** | — (client UMD `window.RmoozDemoUnits`; Node-requireable) | **DOC-UNDERSTANDING-1 / MULTI-COUNTRY-DEMO-A (2026-06-13):** DEMO-ONLY `buildDemoUnits(payload)` → `proposed_units → demo_units` (each `demo_only:true`/`review_only:true`/`movement_status:'demo'`/`source_proposed_unit_id`/`exact_unit_position:false`) + per-base `groups[]` with `category_counts` across the 9 demo buckets (air_fighter/air_attack/air_transport/maritime_patrol/helicopter/uav/naval_surface/ground_unit/unknown). Category via the SYMBOL-DB ladder (`RmoozSymbolDB`/base-status `normalizePlatform`) mapped to buckets; built-in fallback when absent. **NEVER writes scenario/world-state; never 'approved'.** Test: `scripts/test-multi-country-demo-a.js` (14/14). |
| Demo movement controller (`client/shell/demo-movement.js`) — **NEW** | — (client UMD `window.RmoozDemoMovement`; Node-requireable) | **DOC-UNDERSTANDING-1 / MULTI-COUNTRY-DEMO-A (2026-06-13):** SYMBOLIC Start/Pause/Reset demo. `init` builds demo groups + selects a small sample (3 BLUE preferring distinct countries + 2 RED) + computes the demo objective (centroid of anchors); `step` glides `group.current = lerp(anchor→objective)`; `reset` returns to anchors. Pure state/math run headless (Node-tested); the Leaflet layer (offset per-country demo markers; popup shows country/side/base/grouped-platform-count/`review_only`; click → Base Status Panel) + control panel (with mandatory labels **Demo movement · Not final tasking · Requires commander approval · عقيدة غير مرفوعة / Doctrine pending**) render only when `mapReady()`. **No world-state/scenario mutation, no final tasking/COA/adjudication, no weapons/damage, no Node timers.** Wired `app.html` after symbol-db/base-status. Test: `scripts/test-multi-country-demo-a.js` (14/14). |
| AI Provider Router (`ai/ai-provider.js`) | `/api/ai/provider/status`, `/api/ai/health` | Resolves auto/ollama/claude/zen + fallback. |
| AI Config (`ai/ai-config.js`) | — | Env/secret/JSON overlay; single source of truth for endpoint/provider/keys. **Model now layered by `model-selection.js` (below).** |
| **Model Selection (`ai/model-selection.js`) — NEW** | `GET /api/ai/models`, `POST /api/ai/model/select` | **RMOOZ-LOCAL-MODEL-SELECTOR-A (2026-06-16):** single source of truth for the LOCAL model the **whole app** uses. Precedence: runtime file `runtime/ai-model-selection.json` (operator UI pick) → `RMOOZ_OLLAMA_MODEL` → `RMOOZ_FREE_FIGHT_MODEL` → `RMOOZ_LOCAL_LLM_MODEL` → `RMOOZ_AI_MODEL` → ai-config default. No network I/O (no cycle with ollama-client); corrupt file → safe fallback. `getSelectedModel()` is read by **ollama-client** (dynamic default + live `DEFAULT_MODEL` getter), all four **free-fight** modules (decision/coa-planner/capability-analyst/llm-plan — the last also dropped its `'auto'` provider default, closing a cloud-leak), plus step1-llm-fill/coa-agent/adjudicator (truthful `model_used`). Replaces the divergent per-module chain that ignored `RMOOZ_OLLAMA_MODEL` and defaulted to an **uninstalled** `qwen3-coder:latest`. `RMOOZ_ALLOW_SIM_RUN` stays the **only** execution gate; an uninstalled selection → `model_available:false` + a "run `ollama pull`" warning, **never a silent swap**. UI in two places, cross-synced via `rmooz:ai-model-changed`: always-on header HUD (`client/shell/ai-model-hud.js` → `#ai-model-hud-mount`) + a Free Fight card block (`free-fight-demo.js`). Route health (`free-fight-coa-planner.routeHealth` + bridge `/plan-coas/health`) now emits `selected_model`/`selection_source`/`available_models_count`. Persistence file gitignored (`UI_MOdified/runtime/`). Tests: `test-ai-model-selector-a.js` (16/16) + updated real-LLM E2E `scripts/verify-ai-free-fight-real-llm-e2e.js` (selects via API → asserts `model_used`==selected). **Anti-dup:** distinct from the adjudicator HUD's `#wg-adj-provider`/`#wg-adj-model` per-call override. |
| Ollama / Claude / Zen clients (`ai/ollama-client.js`, `claude-client.js`, `zen-client.js`) | `/api/ai/generate`, `/api/ai/chat` | Claude client uses prompt caching; Zen = OpenAI-compatible proxy. **ollama-client default model is now the operator-selected model (live, via `model-selection.js`).** |
| Monte Carlo Runner (`ai/monte-carlo-runner.js`) | `/api/ai/mc/start`, `/:runId/events` (SSE), `/cancel`, `/aggregate` | N trials, semaphore-bounded; writes `data/mc-runs/<runId>/trial-NNN.jsonl`. |
| Feedback Store (`ai/feedback-store.js`) | `/api/ai/feedback`, `/summary` | Append-only `data/feedback/<scenario>.jsonl`. |
| Lesson Store (`ai/lesson-store.js`) | `/api/ai/lessons` | Append-only `data/lessons.jsonl`. |
| Learning Store (`ai/learning-store.js`) | — (read-side) | Computes priors from past MC runs; feeds adjudicator prompt. |
| Report Builder / Render (`ai/report-builder.js`, `report-render.js`) | `/api/ai/report.json`, `/report.html` | baseline vs live vs MC distribution. |
| Prompts (`ai/prompts/*.txt`) | — | adjudicator (Ollama + Claude variants), red/blue-team, coa; hash logged per trial. |
| Schemas/validators (`ai/adjudicator-schema.js`, `adjudicator-validator.js`, `coa-schema.js`, `parametric-baseline.js`) | — | Enums + monotonicity clamp + plausibility; baseline fallback when AI fails. |
| **Journal (sim boundary)** (`sim/journal.js`) | internal only | **Append-only `data/journal/<runId>.jsonl`, writes to disk** (`fs.appendFileSync`, `:146`). Single durable mutation path. See **D3**. |
| **Proposal Store** (`sim/proposal-store.js`) | internal only | In-memory, 15-min TTL, single-use `consume()`. Not durable. |
| **WarGamingGEN Sim Bridge** (`wargame-sim-bridge.js`, 280 L) | `/api/wargame-sim/{status,stage-doc,run,publish,import}` | **NEW 2026-06-05 (Wave 7, FAST-DOC-1/2).** Mounted `web-server.js:47`, routed `:463`. Stages red/blue `.docx` → **spawns the real WarGamingGEN Python sim** (`tests/test_full_run.py --all`, `LLM_LOCAL_FORCE_FALLBACK=0`) **only when `RMOOZ_ALLOW_SIM_RUN=1`** (else returns the exact manual command); publishes each run to a **dated `export_to_rmooz/<run-id>/`** folder (+`latest.json`); import reuses the **same `scripts/port-wargame.js` porter** and stamps `source_run`. Writes files only — never mutates `window.*`. Env: `RMOOZ_ALLOW_SIM_RUN`/`RMOOZ_SIM_MODEL`/`RMOOZ_WARGAMEGEN_DIR`/`RMOOZ_TESTINGAI_DIR`. **DEBUG-DOCX-1 (2026-06-05):** the hardcoded `C:/Users/ADMIN/…` default was dead on non-ADMIN boxes (staged DOCX/run/import silently targeted a missing tree) → replaced with `resolveTestingAiDir()` (env → `%USERPROFILE%\Desktop\TestingAI` → legacy). Verified `/status` resolves the real tree (`docs:{red,blue}=true`). See `docs/integration/debug-docx-1-change-propagation-diagnosis.md`. **RC-3 publish-before-import explicit in the UI** (`wargame-sim-import.js?v=3`): "Publish latest run" button + ordered 1→5 flow. **FAST-DOC-2 (2026-06-05):** added read-only `computeFreshness()` (latest.txt target / newest run / published-export mtime+size+sha / `runs/latest` real-dir+STALE flag) surfaced in `/status` + a freshness panel; **stale-before-import guard** — `/import` returns 409 unless `?confirm=1` when the export is older than the newest run (client two-step "Import anyway (stale)"). No delete, no DOCX parse, import still via `port-wargame.js`. Tests `test-fast-doc-2-publish-before-import.js` 37/37 (FD1 still 30/30). Doc `docs/integration/fast-doc-2-publish-before-import-clarity.md`. ⚠️ Still open: **RC-1 `runs/latest` stale dir — detected+warned, NOT deleted** (Safe Cleanup Plan = DEBUG-DOCX-1 §10); `qwen2.5:7b` partial schema-fallback. |
| **World State Engine seam** (`sim/world-state-engine.js`) | `POST /api/sim/decide` | **NEW 2026-06-01.** Server seam composing WS1+DB1+DET1+ENG1+WS3; requires the framework-free `client/shell/*` engine modules (deliberate shared logic — flagged, not drift). `project`/`transition`/`run`; `adjudicator.commitDecisions()` journals the WS3 transition (`source='deterministic-sim'`). Test: `test-ws-server-engine.js` (10/10). Server-side WS3 wiring = `[[project_ws3_server_wiring]]`. |

### Free Fight AI Commander + Intel foundation — server modules (detail in §A rows 98–113)
> All wired through **`server/wargame-sim-bridge.js`** → `POST /api/wargame-sim/free-fight/{plan-coas, plan-coas/health, llm-plan, demo-ai-step, test-llm}` (NOT `web-server.js` directly — judge wiring by the full require graph). **Local-only:** `zen`/`claude`/`openai`/`auto` short-circuit to `remote_blocked` with **zero** `aiProvider.generate` (`[[feedback_free_fight_local_only_security]]`). All provider/model/timeout/repair config flows through the one resolver `llm-runtime-config.js` (`[[project_llm_runtime_config_resolver]]`).

| Module | Lines | Notes |
|---|---|---|
| `ai/free-fight-coa-planner.js` | 1864 | Multi-COA attack planner; `_buildPlanningContext`+`_assemblePlan(seed)`; repair loop; `ai_depth` fast/normal/deep; deterministic Staff-Safe floor. LOCAL-ONLY `_callLlm`. §A rows 101/103/106–110. |
| `ai/rmooz-ai-tool-contract.js` | 1233 | MCP-style tool contract: `composeCommanderPrompt`/`composeRepairPrompt` + `validateCommanderCoaTool` (structure/physics-only — invented-id/teleport/bounds/kill/naval-land-move/malformed; **accepted ⟺ 0 violations**). §A rows 100/103/109/112. |
| `ai/free-fight-llm-capability-analyst.js` | 706 | LLM intel-analyst enriches OOB → asset selection. LOCAL-ONLY. §A row 112. |
| `ai/free-fight-llm-decision.js` | 289 | Unit-level action bridge; remote → `remote_blocked`. |
| `ai/free-fight-llm-plan.js` | 236 | Step-level move/hold plan via the resolver. |
| `ai/free-fight-action-engine.js` | 300 | Deterministic unit-level action brain (demo/test fallback). **RMOOZ-COA-REALISM-GATE-A (2026-06-22):** `validateAction` gains `opts.realismGate(from,to,opts)` injection point — caller may pass a territory/domain check; gate returns `{ok,reason}`. |
| `ai/free-fight-situation-triggers.js` | 182 | BLUE situation/ROE warning + escalation triggers. |
| `ai/candidate-prefilter.js` | 157 | Pre-filter the force → top 10–25 candidates before the MCP prompt (free guardrail via `allowedUnitIds`). §A row 109. |
| `ai/tactical-action-library.js` | 349 | 16-action doctrine-free vocabulary + 3 COA archetypes + `computeActionGeometry`. §A row 100. |
| `ai/tactical-terrain-context.js` | 180 | 8 GIS factors w/ honest provenance (real DEM is Libya-only); feeds action geometry + LLM prompt. §A row 99. |
| `ai/commander-brief.js` | 433 | Commander-brief narrative + generic coalition posture; "Name (CODE)". |
| `ai/coalition-posture-engine.js` | 234 | Coalition ORBAT/posture (RMOZ-INTEL-CAPABILITY-TERRAIN-ZONE-A). |
| `ai/scenario-intel.js` | 147 | Shared intelligence foundation; requires terrain-effects + coa-variation engines. |
| `ai/platform-capability-catalog.js` | 283 | Unit capability profiles (intel foundation); used by tool-contract + contact-detection. |
| `ai/contact-detection-engine.js` | 147 | Contact-detection for the COA tool. |
| `ai/sovereign-zone-engine.js` | 184 | Sovereignty/zone checks; used by tool-contract + terrain context. |
| `ai/roe-escalation-engine.js` | 96 | ROE escalation; used by tool-contract. |
| `ai/llm-runtime-config.js` | 206 | **Canonical** provider/model/timeout/repair/draft resolver; the 5 free-fight modules + `model-selection` delegate (zero direct env reads — grep-proven). `getProvider()` returns RAW provider so `isRemoteProvider` still blocks zen. §A row 112. `[[project_llm_runtime_config_resolver]]`. |
| `ai/ai-json.js` · `ai/ai-guardrails.js` | 37 · 83 | **AI-GLOBAL-REFACTOR-PHASE-1** shared helpers: safe JSON extraction + unit/base/confidence normalization. |
| `ai/terrain-effects-engine.js` · `ai/coa-variation-engine.js` | (WIP) | WIP stubs but **WIRED** (required by `scenario-intel.js` + `rmooz-ai-tool-contract.js`) — not dead. |

---

## D. Cross-cutting client (`UI_MOdified/client/`)

| Module / Feature | Status | Key file | Notes |
|---|---|---|---|
| Launch Hub — shell | ✅ | `home.html` + `home.js` (13 kB) | **NEW 2026-06-03/04.** `index.html → home.html → app.html` entry flow. Hub displays 5 action buttons + 3 secondary links. `home.js` real intent wiring: **demo** → `go('demo')`; **load** → browser file picker → JSON parse → `sessionStorage['rmooz.pending-import']` → navigate; **new** → inline bilingual notify (authoring in development, no navigation); **editor** → `go('editor')`; **resume** → `sessionStorage['rmooz.last-json']` check → navigate or inline notify; **settings/layers/help** → `go(intent)`. All navigation uses relative URLs (`WORKSPACE_FILE='app.html'`, `resolveClientUrl()`, `buildWorkspaceUrl()`) — works from any served path. Auth guard via `/api/auth/me`. Embedded bilingual I18N (en/ar). No engine, no mutation, no backend writes. Auth: `landing-auth.js` routes post-login to hub. `[[feedback_nav_relative_urls]]`. |
| Login / auth page | ✅ | `landing-auth.js` (~13 kB) | Self-contained login form (index.html). Validates username (2+ chars) + password (4+ chars). Routes to workspace post-auth via `resolveClientUrl()` (relative). Bilingual (en/ar). |
| Map engine | ✅ | `map-engine.js` | Snap/eraser/scallop/freehand; stateless, init via `AppMapEngine.init(ctx)`. |
| Auto Draw (circle-X → frontline → flank AOI) | ✅ | `free_draw_signature.js` (~2k L) + `autoDrawCircleXFlankLines` in `app.js` | Guided workflow: formation+affiliation popup → place circle-X → scalloped frontline through circles → floating flank panel (Front/Deep/Both draw cards) → AOI polygons + echelon labels. **BUGFIX 2026-06-11 (two stacked causes):** (1) `map-hide-panels.js` hid `#auto-flank-controls` on the SAME map click that completed the frontline, so the draw cards vanished the instant they appeared — hide is now stage-aware (only when workflow idle); (2) with a CUSTOM formation selected the panel had no card at all (both built-in cards hidden, no custom card existed) — the panel now builds a card for the selected custom formation (name + Front/Deep captured before the popup is destroyed) wired through `callFlank`, and a stale custom default in `localStorage['fd-default-formation']` no longer auto-selects a ghost formation (customs aren't persisted). Browser-verified on the verify stub. |
| Map click → clean view (P3) | ✅ | `map-hide-panels.js` | Hides context/chat/ORBAT/ops-intel/flank panels on map click for an unobstructed view; `AppMapHidePanels.hideAll` API reused by scenario "present". Flank-panel hide is stage-aware as of 2026-06-11 (see Auto Draw row). |
| Popups | ✅ | `popups.js` | HTML builders for geo/symbol/TMG; ♻️ ~40 `bindPopup` calls remain inline in `app.js`. |
| Tile/basemap mgmt | ✅ | inside `app.js` (~289–410) | MBTiles + OSM fallback; tile-server probe; error banner. Monolithic in app.js. |
| Layer/folder UI | ✅ | `ui/controllers/layers-controller.js`, `ui/panels/layers-panel.js` | Search/collapse/visibility; layer state owned by app.js. |
| Measure tool | ✅ | `ui/controllers/measure-controller.js`, `ui/state/measure-state.js` | Distance / range-circle / range-sector. |
| Units core registry | ✅ | `units.js` | Hierarchy + SIDC building; modal create/edit; init `AppUnits.init()`. |
| Units ORBAT treeview | 🟡♻️ | `units-orbat.js` | Tidy-tree SVG modal, read-only (no in-modal editing). ♻️ duplicates symbol/tree render vs the dock. |
| Units ORBAT dock | ✅♻️ | `units-orbat-dock.js` | Flat tree + drag-to-place → `/api/units/:id/place`. Tool-rail "Forces/ORBAT" toggle (`#orbat-toggle-btn` in `app.html`) (re)opens it after P3 clean-view hides it; rebuilds on wargame-HUD scenario change via `AppUnitsOrbatDock.refresh()` (commits `9498932`, `b6356d5`, 2026-05-30). ♻️ second render path for same unit data. |
| Units map markers | ✅ | `units-map.js` | Echelon-scaled markers; cursor-follow placement; `__APP_UNITS_PLACING` flag. |
| i18n + language toggle | ✅ | `i18n.js` (~4.7k L) | EN/AR; `window.t()`/`setLanguage()`; applies `dir="rtl"`; syncs to server. |
| RTL layout | ⏸️ | `style.css` | Context-panel covered by unit-panel in AR; LTR-only assumptions, no logical props. Deferred — `[[project_rtl_context_panel_overlap_deferred]]`. |
| Chat (client) | ✅ | `chat.js` | Polling team chat; rooms/groups/presence/invites; role-gated writes. |
| Plan import/export | ✅♻️ | `io.js` | Extended GeoJSON v3 (`__layers`/`__folders`); ♻️ parametric-shape geometry builders duplicated with app.js. |
| Server sync & auth | ✅ | `server-sync.js` | Session cookie, prefs, plan persistence scheduling; auto-run IIFE. |

---

## E. Server REST API surface (`UI_MOdified/server/web-server.js`)

All endpoints below are ✅ wired with real handlers. Grouped; see web-server.js for handler locations.

- **Auth/session:** `POST /api/auth/login`, `/logout`, `/register`; `GET /api/auth/me` (→ `app-data.handleAuthApi`; scrypt). *Note: verify env stubs `/api/auth/me` to avoid redirect loop — `[[reference_browser_verify_static_server]]`.*
- **Prefs:** `GET|POST /api/prefs`.
- **Plans:** `GET|POST /api/plans`; `GET|PUT|DELETE /api/plans/:planId` (atomic write; legacy `.json`→`.geojson`).
- **AI provider/health:** `GET /api/ai/health`, `/api/ai/provider/status`.
- **AI model selector (RMOOZ-LOCAL-MODEL-SELECTOR-A):** `GET /api/ai/models` (local provider's installed models + current selection + `model_available` + `allow_sim_run`), `POST /api/ai/model/select` `{model}` (persists the app-wide local model to `runtime/ai-model-selection.json`).
- **AI generation:** `POST /api/ai/generate`, `/api/ai/chat`.
- **COA / Red / Blue:** `POST /api/ai/coa`, `/api/ai/red-team/propose`, `/api/ai/blue-team/propose`.
- **Scenarios:** `GET /api/ai/scenarios`, `/api/ai/scenario/:name`; `POST /api/scenario/import` (GeoJSON, ≤25MB); `POST /api/scenarios` (**Edit Mode durable save** — validates via `scenario-validator`, sanitises name, **409 anti-clobber** unless `?overwrite=1`, writes `data/scenarios/{name}.json`, clears cache + sets active; `web-server.js:695`); `GET /api/scenario/events` (SSE reload).
- **Sim boundary:** `POST /api/sim/propose` (no mutation), `POST /api/sim/commit` (LLM-proposal commit — mutates + journals), `POST /api/sim/decide` (**WS3 deterministic decision commit** — derives World State, applies a WS3 decision, journals `source=deterministic-sim`). D3 unlocked 2026-06-01.
- **WarGamingGEN integration (Wave 7):** `GET /api/wargame-sim/status`, `POST /api/wargame-sim/{stage-doc,run,publish,import}` — DOCX→sim→dated-export→import bridge (`server/wargame-sim-bridge.js`; sim run gated by `RMOOZ_ALLOW_SIM_RUN=1`). GeoJSON path reuses the existing `POST /api/scenario/import`.
- **Free Fight AI (local-only, via `server/wargame-sim-bridge.js`):** `POST /api/wargame-sim/free-fight/plan-coas` (multi-COA commander planner), `GET|POST /api/wargame-sim/free-fight/plan-coas/health` (`allow_sim_run`·`ai_execution_enabled`·`provider`·`configured_provider`·`provider_blocked`·`model`·`model_available`·`selected_model`·`reason_if_blocked`), `POST /api/wargame-sim/free-fight/llm-plan`, `POST /api/wargame-sim/free-fight/demo-ai-step`, `GET|POST /api/wargame-sim/free-fight/test-llm`. Gated by `RMOOZ_ALLOW_SIM_RUN=1`; **local-only** (remote providers → `remote_blocked`, zero cloud calls). §A rows 98–113, §C census. Other bridge routes: `GET /api/wargame-sim/sources`, `POST /api/wargame-sim/{analyze,placement,generate,generate-preview,regenerate,cancel,objective-override}` (DOC-UNDERSTANDING-1 review→generate pipeline).
- **Unified Import Wizard (UNIFIED-IMPORT-2, 2026-06-06):** one primary "Import Scenario" flow (`client/shell/scenario-import-wizard.js`): upload DOCX → Start → real checkpoint progress → auto publish/import → opens on the map; on stop → Continue (`/run?resume=1`) / Restart / View Logs / **Import Partial** (≥4 phases). Bridge adds `/status` progress fields (`phases_done/total`, `partial_available`, `partial_import_allowed`, `last_run_id`, `can_resume`, `status`), `POST /api/wargame-sim/regenerate` (replay checkpoints→geojson, **no LLM**), and `/import?partial=1` (partial metadata + anti-clobber 409 unless `?replace=1`). The 3 legacy cards move under collapsed **Advanced Import Tools** (kept). Porter/validator unchanged. Test: `test-unified-import-2-wizard.js` (25/25). Docs: `docs/integration/unified-import-2-wizard-implementation.md`. **Deploy env (DOC-ENV-1, REQUIRED for one-click generation):** `RMOOZ_ALLOW_SIM_RUN=1`, `RMOOZ_TESTINGAI_DIR`, **`RMOOZ_PYTHON`=`…\TestingAI\.venv\Scripts\python.exe`** (the bridge does NOT auto-detect the venv; bare `python` → `ModuleNotFoundError: openai` and the run dies instantly — wizard fails gracefully), `RMOOZ_SIM_MODEL=qwen2.5:7b` (served via Ollama). Documented in `UI_MOdified/.env.example` + the impl doc's Troubleshooting.
- **Local Wargame import (LOCAL-IMPORT-2, 2026-06-05):** `GET /api/wargame-local/status`, `GET /api/wargame-local/file`, `POST /api/wargame-local/import` (`server/wargame-local-bridge.js`; client card `client/shell/wargame-local-import.js`). Imports run folders copied into `data/imports/wargame_outputs/<run_id>/` (REQUIRES `all_phases.geojson`) through the existing porter — no TestingAI/`runs/latest`/flat-bundle dependency. Stale-run guard (409 + `?confirm=1`), provenance `source='WarGamingGEN-local'`. Run folders git-ignored; README/.gitkeep tracked. Test: `test-local-import-2-wargame-output-folder.js` (13/13). Docs: `docs/integration/local-import-2-wargame-output-folder-import.md`.
- **Legacy:** `POST /api/ai/adjudicate` (routes through propose+commit; `source='legacy-shim'`).
- **Feedback/lessons:** `POST /api/ai/feedback`, `GET /api/ai/feedback/summary`; `POST|GET /api/ai/lessons`.
- **Reports:** `GET /api/ai/report.json`, `/api/ai/report.html`.
- **Monte Carlo:** `POST /api/ai/mc/start`, `GET /:runId/events` (SSE), `POST /:runId/cancel`, `GET /:runId/aggregate`.
- **Chat:** messages (`GET|POST /api/chat/messages`), upload, groups (create/join/invite/invite-code/leave/delete + `/mine`), presence, `rooms/members`, identity (`GET|POST /api/chat/me`).
- **Units (SQLite):** `GET /api/units/tree`, `/:id/children`, `/search`, `/code-check`; `POST /api/units`, `PATCH /api/units/:id`, `POST /api/units/:id/{move,place,unplace,delete,restore}`. All writes require auth.
- **DEM (Libya terrain):** `GET /api/dem/tile/:z/:x/:y.png` (hillshade+colormap PNG), `GET /api/dem/heights/:z/:x/:y` (octet-stream Float32LE real-metre height grid for Cesium `CustomHeightmapTerrainProvider`; 204 outside coverage, 400 on bad input), `GET /api/dem/info` (coverage + pixel-scale meta), `GET /api/dem/elevation?lon=&lat=` (point elevation) — all served by `server/dem-service.js` (`readRegion` bilinear resampler over `libya_demx5.tif`). Powers both the 2D `dem-layer.js` overlay and the DEM-backed 3D terrain.
- **Static:** `/`, `/app`, `/app.html`, `*.html|js|css`, `/uploads/*`, `/maps/*`.
- **Tile server (separate, port 8080, `server/tile-server.js`):** `GET /services/:tileset/:z/:x/:y.:fmt` (MBTiles → PNG/JPEG).

---

## F. Test / verify infrastructure (repo root)

| Pattern | Count | Validates | Run | Recent |
|---|---|---|---|---|
| `test-pr-*.js` | 60 | Static: source/regex/`new Function()` sandbox; DOM IDs, exports, i18n keys, forbidden-element absence. **No server.** | `node test-pr-<n>.js` | 289L, 288M, 288L, 287L2, 287C, 286L2 |
| `verify-pr-*.js` | 16 | Browser: Playwright vs `http://localhost:8000/app.html`; clicks, DOM, screenshots → `docs/pr-*-verify/`. **Needs server.** | start server, then `node verify-pr-<n>.js` | 288M, 288L, 287L2, 285A, 284 |

**No unified test runner / npm script / CI** — each file is standalone. Highest PR number present ≈ **289L** (test) / **288M** (verify). **Census 2026-06-16: ≈81 test files total.** The Free Fight / intel / identity / model-selector suites added since live as `test-*-a.js` under **`UI_MOdified/scripts/`** — e.g. `test-free-fight-{repair-loop,candidate-prefilter,zen-blocked,demo-pacing-c,ai-gate-card-d}-a.js`, `test-llm-runtime-config-a.js`, `test-ai-model-selector-a.js`, `test-rmooz-ai-tool-contract-a.js`, `test-tactical-{action-library,terrain-context}-a.js`, `test-intel-{capability-catalog,sovereign-zone,terrain-effects,coalition-posture}-a.js`, `test-unit-identity-contract-a.js`, `test-platform-identity-enrichment-a.js` — plus the **server-dependent** E2E `scripts/verify-ai-free-fight-{prefilter,real-llm}-e2e.js` (need a running server + a pulled local model).

Newer feature work uses **`test-p0*` / `test-an*` / `test-sym*` / `test-unit-*`** naming (authoring / animation / symbology) alongside `test-pr-*`, same standalone static-check style. **Wave 1:** `test-p0-authoring-foundation.js` (33/33), `test-an1-attrition-visuals.js` (26/26), `test-p0b-animation-readiness.js` (audit-inventory). **Wave 2** (committed, all green): `test-an-echelon-rollup.js` (20/20), `test-an2-event-pins.js` (25/25), `test-an4-movement-trails.js` (18/18), `test-p5b-selected-unit-readout.js` (19/19), `test-sym1-unit-symbol-fidelity.js` (13/13), `test-sym2-unit-symbol-resolver.js` (21/21), `test-unit-scaling-hover.js` (18/18), `test-an3-arrowheads-legend.js` (legend), and `test-mg1-mission-graphics.js` (27/27, mission graphics) — all committed and green. **Wave 5/6** (committed, green): `test-obj-a.js` (33/33), `test-obj-b.js` (29/29), `test-obj-c.js` (51/51, OBJ-C display panel), `test-ws-eng1-a.js` (13/13, engagement outcome ownership), `test-doctrine-evidence.js` (78/78, DOCTRINE-A), `test-l3a-why-not.js` (80/80, L3-A evaluator), `test-l35a-alternatives.js` (29/29, L3.5-A alternatives). **Wave 7** (integration + Phase 6): `test-fast-int-2-wargame-geojson-import.js` (21/21), `test-fast-doc-1-docx-sim-bridge.js` (30/30), `test-fast-int-3-home-launch-integration.js` (24/24), `test-phase-6f-a-applied-state.js` (50/50) — all green; plus `test-phase-5b/5c/5d-1`, `test-phase-6b/6c-a/6c-c/6e-a*`. **FAST-DOC-2:** `test-fast-doc-2-publish-before-import.js` (37/37 — freshness detection, stale `runs/latest` detected-not-deleted, stale-import 409 guard, no auto-import/no DOCX parse). ⚠️ **`test-db-1-a-middle-east-catalog.js` — 29 FAILING** (data/test schema drift, see Drift **D5**); do not assume Wave 7 is all-green.

---

## Known duplications & cleanup candidates ♻️

1. **Unit hierarchy rendering** — `units-orbat.js` (tidy-tree SVG modal) vs `units-orbat-dock.js` (flat docked list): two render paths for the same SIDC data.
2. **Parametric shape geometry** — circle/sector/oval builders duplicated between `io.js` (export) and `app.js` (element construction).
3. **Popup binding** — `popups.js` exports builders, but ~40 `bindPopup` calls remain inline in `app.js`.
4. **Workspace cards** — see `docs/scenario-workspace-consolidation-map.md` for the catalogued **9× duplication patterns + 3 dead fields** across ~150 DOM surfaces (authoritative; do not re-audit).
5. **Tile/basemap logic** — embedded inline in `app.js` rather than a module (monolith, not duplication, but a refactor candidate).
7. ~~**Middle East platform catalog vs DB1**~~ ✅ **RESOLVED (D5, 2026-06-09; re-confirmed 2026-06-16).** `shell/middle-east-platform-loader.js` + `data/db/middle-east/platforms.json` were **DELETED** (verified gone this audit); the 18 useful ME platform entries were folded into the single DB1 catalog (`shell/world-state-db.js` `CAPABILITY_CATALOG`, now 29 entries) — no parallel range source remains, "ranges from the DB" rule honoured. `[[project_middle_east_catalog_drift]]`.
6. **Stray `wargame 6/` dir** ⚠️ — `UI_MOdified/client/wargame 6/` (adjudicator-hud/map/client, red-team-controller, scenario-state, approved-actions) is a **dead duplicate** of the live `wargame/` modules; **not referenced in `app.html`/`app.js`**. Echoes the previously-removed stray `wargame 5/`. Today's merge accidentally touched its `adjudicator-hud.js` copy (mtime 2026-06-03). **Delete candidate** — confirm with owner before removing.
8. **SYMBOL-DB-A category ladder mirror** ◑ *(low-risk, intentional)* — the platform-name→`symbol_category` regex ladder (tomcat/orion/hercules/…) exists in **both** `shell/symbol-db.js` (canonical, SYMBOL-DB-B) and `shell/base-status-panel.js` (inline). The panel runs its inline ladder for the category and **delegates only systems lookup** to `RmoozSymbolDB`, keeping the inline copy as a defensive fallback if the categorizer fails to load. Copies are byte-identical; the heavy catalog/systems logic is single-source (DB1). **Cleanup candidate:** have the panel delegate the category lookup too (drop the inline ladder) once load-order is guaranteed. Added 2026-06-12 (SYMBOL-DB-B, `e42e2d5`).
9. **Geo math copy** ♻️ *(low-risk)* — `latLngToKm()` / `kmToLatLng()` / `distKm()` are defined identically in `maneuver-arrow.js` and `turn-engine.js`; both could delegate to `AppUtils`. Found 2026-06-16.

---

## Gaps / not-yet-built

- **Real journal/export/download chain (client):** contract + draft-preview + export-preview all read-only; no persistence or download (by design, deferred).
- **Timeline → sim wiring:** transport bar is scaffolding; no playback dispatch beyond the adjudicator HUD's own scrubber.
- **Unit Panel sim sections:** P5b added marker-derived **operational** readouts (symbol / current-step status / capability); **Combat/C2** sections are still placeholders pending a unit sim-data feed.
- **Scenario Catalog (external CMO/CSP51):** drafted contract unwired; UI reverted — `[[project_external_scenario_catalog_deferred]]`.
- **Briefing values:** some workspace briefing fields are mock pending a real feed — `[[project_briefing_mock_values_deferred]]`.
- **Known issue KI-1:** wargame1 baseline violates the BLS-4 never-SECURE invariant (pre-existing; see `docs/known-issues.md`).
- **No unified test runner** (see §F).
- **Decision Package import:** internal + external "خطوات صنع القرار" contracts — already wired in `scenario-workspace.js` + `web-server.js` — `[[project_decision_package_import]]`.
- **Scenario Authoring Mode — IN PROGRESS (Wave 3):** **Slices 1 + 2A + 2B + 2C + 2D-1 + 2D-2 BUILT & wired** — `shell/scenario-edit-mode.js` (`window.AppEditMode`, mounted in app.html, ~2014 LOC). **Slice 2D-2 (Map-as-editor, 2026-06-02)** replaces lon/lat textareas with **click-to-draw** affordances on the live map: **"Set objective on map"** (single click → `obj.coord`), **"Draw AO on map"** (click vertices + double-click → closed GeoJSON Polygon pushed to `ao_boundaries[]` with auto-name "AO N"), **"Draw pipeline on map"** (click waypoints + double-click → replaces `pipeline[]`), **per-BLS "Pick coord on map"** (single click → BLS coord). Uses native Leaflet — no plugin. Audit confirmed no Leaflet.Draw/Geoman exists in the codebase; pattern mirrors the existing hand-rolled vertex-collection in `app.js` geo-tools + `free_draw_signature.js`. New helpers `_beginPickOnMapPolygon` / `_beginPickOnMapPolyline` extend the Slice 2D-1E `_beginPickOnMap` single-shot pattern with live preview (`L.polygon`/`L.polyline` updates each click), double-click to finish, Enter accepts, ESC cancels. After each finish, `_maybeRepaintMap()` copies the draft into live `RmoozScenario` and calls `AppAdjudicatorMap.drawScenario` so the new shape renders immediately (preview only — Save draft is still the official commit). Static test `test-edit-mode-slice2e.js` 30/30 green covering vertex accumulation, polygon auto-close, polyline open-ended, minimum-vertex enforcement, external-cancel cleanup. Browser-verified on wargame3 at 820×900: obj coord updated, AO polygon (5-point closed ring) added, pipeline replaced (24→5 waypoints), BLS coord moved. Earlier slices: **Slice 2D-1 (Forces redesigned for scale, 2026-06-02)** replaces the Slice 2B flat list with a **tree + detail-pane + search** that handles real wargame3-scale data (70 Red + 83 Blue): grouped by **Side → Echelon → unit**, compact one-line rows (sym + uid + label + role/bls tags), click-to-select + single-unit **detail pane** below the tree so only ONE unit's full editor lives in the DOM at a time. Search/filter bar matches across uid/label/role/bls/echelon (case-insensitive); filtered tree auto-expands all matching groups. Group-collapse state persists across rerenders. **Live measurement on wargame3 → Forces step: 51,688 px → 785 px scroll (65× reduction); 1,198 inputs → 1 at rest, 10 when a unit is selected.** Also fixes a **silent data-corruption bug** introduced in Slice 2B: the `role` field is now `<input list="…">` + `<datalist>` (free-text with 7 CMO maneuver-role *suggestions*), not a strict `<select>` — wargame3's 33 distinct role values (`mech_inf_div`, `sam_s300`, `submarine`, `awacs`, …) round-trip without coercion. Add-Red/Add-Blue moved into the toolbar; "Pick coord on map" button on the detail pane reuses Leaflet's once-click handler to set the selected unit's `coord` (CMO Insert-then-click flow analog; never touches `/api/units/:id/place`). Static test `test-edit-mode-slice2d.js` 43/43 green (incl. wargame3-scale evidence: 33 distinct roles, 0 overlap with the old strict enum). Browser-verified on real wargame3 data. **Slice 2D-2 next = Map-as-editor for AO / pipeline / objective** (Leaflet draw integration, reusing existing draw-panel/shapes-panel tools — to be audited first). Earlier slices: **Slice 1** = Metadata + Sides + Posture. **Slice 2A (Geography, 2026-06-02)** = AO + Forces Geometry (`map_bbox`, `ao_boundaries`, `obj` with `carver` 0..60 hard block, `pipeline`, `throughput_ceilings_km`, `bls_template`). **Slice 2B (Forces, 2026-06-02)** = Red OOB + Blue OOB editing (`red_units[]` / `blue_units_initial[]`, writes into in-memory draft NOT the durable `/api/units` SQLite; `blue_units_base_ids` derived on Save). **Slice 2C (Layout + New Scenario, 2026-06-02)** restructures the editor into a **stepped left-rail navigator** matching CMO's 13-step build-order (Metadata → Map → Sides → Posture → Doctrine → Time → Weather → Forces Geometry → Forces → Missions → Events → Briefing → Validate & Save) — replaces a 4136-px crowded scroll with a 520-px per-step pane (≈ 8× reduction). Each step has a completion-pill indicator (green ✓ / yellow ⚠ / empty / dashed-gap); 4 engine-GAP steps (Doctrine, Weather, Missions, Events) show placeholder cards naming the future slice. Adds two trivially-missing build-order cards: **Step 6 Time & Duration** (`phase_table[]` editor + "Synthesize H-3 → H+120 (6 steps)" preset; keeps `phase_table.length === steps.length` in lockstep) and **Step 12 Briefing** (per-step EN/AR narrative textareas, RTL on AR). Adds the long-missing **"+ New scenario" affordance**: inline form (name, EN/AR label, base template) → stamps a fresh draft via `AppScenarioAuthoring.buildStandardScenarioAuthoringTemplate()` + Slice 1/2A/2B defaults, opens Edit Mode at Step 1. Adds **"Save As JSON"** (client-side Blob download — boundary-safe; the locked guard is journal download, not arbitrary file download) and **"Save to server"** via new endpoint **`POST /api/scenarios`** (in `web-server.js`): validates with the existing `scenario-validator`, sanitises name, returns 409 anti-clobber unless `?overwrite=1`, writes to `data/scenarios/{name}.json`, clears cache + sets active (mirroring `/api/scenario/import`'s pattern). Combined static tests: `test-edit-mode-slice2a.js` 50/50 + `test-edit-mode-slice2b.js` 46/46 + `test-edit-mode-slice2c.js` 50/50 = **146 client assertions green**. End-to-end server test `test-api-scenarios-post.js` (spawns web-server on a temp data dir + random port) = **17/17 green** (POST 200 + 409 + overwrite=1 + 400 on malformed + 400 on validator-fail + name sanitisation). Pure helpers exposed at `AppEditMode._testing` (STEPS table, stepIsComplete, synthesizeDefaultPhaseTable, ensureStepsMatchPhaseTable, PHASES_ENUM). CSS for the rail/cards in `app.html`. **Next = DOC1: Doctrine/ROE/WRA** — the highest-value engine gap per the playbook (Step 5 ⚠️ GAP); makes AI proposals auditable. **Wave 4 UI-polish (2D-1J/K/M/N/O):** "Editing: {name}" indicator chip + saved-state badge (unsaved/in-memory/on-disk/on-server), "Start from existing" picker in the New-Scenario form, editor now a fixed-position overlay that expands over the map when ON and collapses to a banner during Pick-on-map. **Slice 2 is functionally complete; next = DOC1.** `[[project_workspace_editable_owner_ruling]]`.
- **WS3 client direct-decisions not yet wired:** the State Transition Engine runs server-side (`/api/sim/decide`) and is embedded client-side, but the live map doesn't yet let an operator issue direct MOVE/ENGAGE/EMCON decisions through it (future Step-2 per-unit actions).
- **`scen-catalog-contract.js` — stub/unloaded:** file exists, export incomplete, not in app.html. Leftover from the deferred external-catalog work — `[[project_external_scenario_catalog_deferred]]`.
- **CMO-style animation coverage (P0B audit):** the W3 presentation suite landed — per-unit attrition (AN1), movement trails (AN4), event pins (AN2), echelon roll-up, engagement legend, symbol scaling/hover, the SIDC family resolver (SYM2), and engagement **mission graphics** (MG1, reusing the operator TMG builder). **Wave 2 closed most of P0B problems #1–#2 for W3.** Still open: on-map **phase label**, **timeline event ticks**, **before/after step compare**, and the core **coverage gap** — all the rich animation is still gated on `schema_variant === "w3-rich"`, so non-W3 imports (Decision Packages, CSP51) get markers + movement only. `docs/scenario-animation-presentation-readiness-audit.md`.
- **Basic Scenario Authoring Foundation (next planned):** `home.html` "Start New Scenario" shows an honest "in development" message today. The next step is to make it functional: a blank RMOOZ-native draft template → editable name/region/sides/objectives/units in-browser → export JSON → load into app via the existing global loader. No backend save, no simulation, no command execution. `[[project_authoring_slice2_next]]`.
- **L3.5-B (Feasible Alternatives display):** `generateFeasibleAlternatives()` logic is done in `action-feasibility.js`. Needs a display slice (`why-not-panel.js` expansion or a sub-panel) to surface the alternatives beneath each blocker/risk in the Why-Not panel.

---

## 🎯 ACTIVE BUILD ROADMAP — RMOOZ Direction Reset (2026-06-01)

> **Charter:** Build RMOOZ as a **2D regional operational command-decision simulator** =
> *World State + AI Decision Support + Operator Review + Operational Visualization.*
> **Mimic CMO structurally & behaviorally, NOT by data** — composite platforms (multi-radar /
> multi-weapon / magazines), continuous movement, detection/engagement rules — using **our own
> regional, class-based, data-driven values**. No CMO DB copy · no global sim · no DB-size-first ·
> 2D only · structure-first. See memory [[project_rmooz_direction_reset]].
> **Every PR must answer:** *"Does this make the scenario feel more alive & responsive to operator
> decisions?"* and **must not break Wargame 3.**

**Build order (each PR is small, additive, Node-testable; "Follows" = hard dependency):**

| PR | What it contains | Follows | Effort | Alive? |
|---|---|---|---|---|
| **WS1** *(in progress)* | **World State projection + component platform model.** `client/shell/world-state.js` (`window.AppWorldState`): `deriveWorldState(scenario,step)` → normalized snapshot (meta/region/objectives/units/contacts/lines/balance/decisions). Units carry component slots `sensors[]`/`weapons[]`/`magazines[]` (empty for W3) + kinematics (`course`/`heading`/`speed_kn`). `applyDecision()` transition seed. Pure, **unwired** — W3 untouched. `test-ws1-world-state.js`. | — (foundation) | M | substrate |
| **MOVE1** *(done)* | **Realistic continuous movement.** `wargame/movement-playback.js` (`window.AppMovementPlayback`) tweens markers between the renderer's own integer-step positions over a wall-clock clock, wired to the transport bar (`#tl-play`/`#tl-pause`/`#tl-speed` via `rmooz:timeline-ui-action`). **NOTE:** `applyStepProgress` does *not* interpolate unit positions by fractional progress (verified — only integer-step snap), so MOVE1 snapshots step k & k+1 and lerps between them (no flicker, honors W3's computed advance). Additive, calls only public map API, W3 render unchanged. Verified: midpoint between endpoints, play/pause/seed work. **Fixes "movement not like CMO."** *(Follow-up: WS-driven per-unit speed; HUD step-sync.)* | WS1 | M | ✅✅ high |
| **VIS1** *(done 2026-06-01)* | Map polish. **Ground-truth before building:** on-map **phase label** already lives in the SITREP banner (`adjudicator-map.js` phasePill); **before/after step-compare** already exists (scenario-workspace live→preview strip, `#sw-wt-cmp-strip`) — neither rebuilt. **Timeline event ticks** BUILT: read-only per-step track under `#timeline-strip` — `shell/timeline-event-ticks.js` (`window.AppShellTimelineEventTicks`), one segment/step from `RmoozScenario.scenario.steps` (events = `engagement_arcs`+`affected`), marks event-steps + highlights current step, reconciles via dirty-checked poll + `rmooz:playback-tick`. Visual-only (no scenario/step mutation). Verified in-browser (17 ticks on W3, current-step highlight, no console errors). EN/AR i18n. | WS1 | S–M | ✅ |
| **WS2** *(done)* | **Live integration: objective status + objective logic.** `computeObjectiveStatusDisplay()` in `world-state.js` reads CAPTURED evidence (force ratio + losses) from computed balance; *non-CAPTURED passes through*. Wired: `adjudicator-hud.js` + `adjudicator-map.js` read `ws.derived.objective_status_display || state.objective_status` (fallback to authored). Parity-proof (WS2 + WS4 together closed the ownership inversion). Test: `test-ws2-live-integration.js` (18/18). | WS1 | S | ✅ |
| **WS2.5** *(done)* | **Derived-field rules engine** — `DERIVATIONS` registry in `world-state.js` (pure pattern; each rule is an independent function); `applyDerivations()` re-runs all computed fields when inputs change. Registry: `balance_summary` → `objective_status_display` → `bls_status`. Parity-safe fallback pattern on all reads (WS-derived || authored). Tested as part of WS-BLS-A/B suites. | WS1 | S | ✅ |
| **WS3** *(done)* | **State Transition Engine** — `client/shell/world-state-transition.js` (`window.AppWorldStateTransition.applyDecision`). Closes the loop *World State → decision → new World State → new options*. Decisions: MOVE · SET_EMCON · SET_READINESS · SET_WRA · RESUPPLY · ENGAGE. Composes DET1+ENG1 (ENGAGE is detection-gated, applies attrition + magazine decrement). **Recomputes contacts after every decision** (e.g. EMCON-on reveals contacts) and returns an explainable `effects[]` changelog (feeds DOC1/AI). Pure, Node-testable, **unwired**. 15/15 in `test-ws3-transition.js`; "my decision changed the battle" narrative verified. | WS1 (+DET1+ENG1) | M | ✅✅✅ |
| **WS4** *(done)* | **Balance computed from World State units** — `computeBalanceSummary()` derives `force_ratio_value` + losses from live unit weights (echelon-based, swappable via `getUnitOperationalWeight`). Wired: `applyDerivations` runs balance first, then objective. Parity-safe fallback (WS-computed || authored mirror). Test: `test-ws4-balance.js` (24/24). Map/HUD read `ws.derived.balance_summary || state.balance`. **First complete reactive chain WS1→WS2→WS2.5→WS4 stable (207 tests, all green).** | WS2+WS3 | M | ✅✅ |
| **WS-BLS-A** *(done)* | **Presence-only BLS rule** — `computeBlsStatus()` (STAGED if no RED nearby / CONTESTED if RED within 10nm). First ownership inversion moving authority from authored `state.bls_status` into World State. Test: `test-ws-bls-a.js` (27/27). Map/HUD fallback pattern: `ws.derived.bls_status || state.bls_status`. | WS1 | S | ✅ |
| **WS-BLS-A2** *(done)* | **HUD reads World State BLS** — wire `adjudicator-hud.js` to read BLS from `ws.derived.bls_status` (same fallback as map). Ensures map/HUD agreement. Part of WS-BLS-A suite. | WS-BLS-A | S | ✅ |
| **WS-BLS-B** *(done 2026-06-03)* | **Control-based BLS rule — TEMPORARY simple model.** `computeBlsStatusB()` upgrades ownership from presence-only to control: STAGED (empty) / SECURED (RED only) / DENIED (BLUE only) / CONTESTED (both). **Documented as TEMPORARY, explainable, replaceable by MTH1 with a richer control-score formula** (e.g., force ratio weighting, distance decay). Wired: `DERIVATIONS.bls_status = computeBlsStatusB`. Uses same 10nm radius, parity gates, no mutation. Test: `test-ws-bls-b.js` (31/31). W3 verified: step 0=STAGED, step 3=SECURED, step 5+=CONTESTED. Map/HUD agreement confirmed. | WS1+WS2.5 | S | ✅✅ |
| **OBJLINK-B** *(done 2026-06-12, recovered from 06-11 E-drive work)* | **Unit→objective/BLS/route links** — `world-state.js` derives `ws.derived.unit_objective_links`: per-unit objective (INFERRED) / BLS (DECLARED via `bls_id`) / route summary (endpoints from `kinematics.course`); null-safe, no mutation, snapshot-only. Foundation for G-3A PATH B (`fromWorldState` copies the links verbatim) and later G-4 tasking. Test: `test-objlink-b.js` (29/29). | WS1 | S | ✅ |
| **DB1** *(done)* | **RMOOZ DB-Lite — role→capability catalog.** `client/shell/world-state-db.js` (`window.AppWorldStateDB`): `enrichWorldState(ws)` attaches `sensors[]`/`weapons[]`/`magazines[]` + `rcs_class`/readiness/supply/doctrine_tags to units **from role+domain** (generic `classifyKind`, data-driven `CAPABILITY_CATALOG` — extend by adding a row; authored components never overwritten). **Lights up the whole stack on real W3: raw=0 contacts → enriched=139**, ENGAGE resolves end-to-end, EMCON-silent drops contacts. Lightweight per direction (no size, no CMO data). Pure, Node-testable, **unwired**. 15/15 in `test-db1-capabilities.js`. **Phase 5D-1 (2026-06-03):** CAPABILITY_CATALOG expanded to **10 entries** — 5 generic role-based (air_defense / naval_combatant / ground_maneuver / air_unit / ew_site / generic) + **4 Soviet-era platforms**: `sam_s300` (S-300; 2 fire-control channels; standoff), `sam_s75` (S-75; 1 channel; tactical), `aaa_zsu` (ZSU-23-4; 4000 rounds; point-defense autonomous), `radar_p37` (P-37 EWR; strategic; no weapons). Middle East expansion (`feat(db-1-b)`) is on unmerged branch `claude/busy-ritchie-e5c133`. | WS1 | M | enables |
| **DB2** | Class tables: `sensor_class` / `rcs_class` / `weapon_class` / `doctrine_tags` (small, data-driven). | DB1 | S | enables |
| **DET1** *(done)* | **Detection rule module** — `client/shell/detection.js` (`window.AppDetection.computeContacts`). Public formulas, our values: radar-horizon `1.23(√h₁+√h₂)`, RCS range `R_ref·(σ/σ_ref)^¼`, EMCON gating, ESM passive (1.5× emitter range), LOS hook (DET2). Carries a seeded **DB-Lite** (`DEFAULT_DB`: `sensor_class`/`rcs_class`/domain alt+RCS defaults) — partially satisfies DB1/DB2. Pure, Node-testable. **NOW WIRED into the live UI 2026-06-01** via the "Detection contacts overlay (DET1 live)" row above (`adjudicator-map.js` builds pseudo-units from live markers → enrich (DB1) → `computeContacts` → read-only contact markers; OFF by default, 3D parity). Also feeds the server seam (`sim/world-state-engine.js`). 15/15 in `test-det1-detection.js`; W3 units (no sensors) → 0 contacts (no fabrication). | WS1 (+ seeded DB-Lite) | M | ✅✅✅ |
| **ENG1** *(done)* | **Engagement rule** — `client/shell/engagement.js` (`window.AppEngagement.computeEngagements`). Chain: detection-gated → WRA/ROE (hold + range mode max/75%/NEZ) → ammo → fire-control channels (point-defense autonomous) → salvo Pk `1−(1−pk)^n` → magazine decrement. Every non-shot has an explainable `reason` (out_of_range / winchester / no_fire_control_channel / weapons_hold) — feeds DOC1/AI. Seeded weapon DB-Lite. Pure, Node-testable. **NOW WIRED into the live UI 2026-06-01** via the "Firing-solutions overlay (ENG1 live)" row above (`adjudicator-map.js`: enriched units + DET1 contacts → `computeEngagements` → read-only shooter→target firing-solution lines; OFF by default, 3D parity). Also feeds the server seam (`sim/world-state-engine.js`). 17/17 in `test-eng1-engagement.js`; W3 (no weapons) → 0 engagements. | DET1 | M–L | ✅✅ |
| **DOC1 / DOCTRINE-A** *(evidence source done 2026-06-03)* | **Doctrine/ROE as visible, auditable EVIDENCE.** `computeDoctrineEvidence()` (9 types) → objective_evidence ledger → OBJ-C Doctrine group. Operator can read ROE/WCS/EMCON/posture/inheritance/priority right now. SOURCE layer ONLY — no ROE/WRA/targeting behavior (those are DOCTRINE-B consumer / DOCTRINE-C interpretation, gated). `test-doctrine-evidence.js` 78/78; value-audit passed. Working tree (uncommitted). | WS3 | M | ✅ |
| **TASK1** | **Tasking/Mission layer** — structured mission objects + tasking view. | WS3+DOC1 | M | ✅ |

**🌟 North-star loop (the demo we're driving toward):** *start app → choose side → place units →
draw their paths → place enemy → hit play → watch it unfold on its own* — for a scenario the operator
**builds in-app**, not just Wargame 3. This works **only** because every rule engine reads from the
shared World State substrate, and the AUTHOR track below **must emit the same scenario shape WS1
projects** (units with `kinematics.course`, sides, objectives, components). That contract is what makes
one engine serve both W3 and hand-built scenarios.

**AUTHOR track (build-your-own; converges with the rule engines for "watch it on its own"):**

| PR | What it contains | Follows | Effort |
|---|---|---|---|
| **AUTH1** | Place units on map for a chosen side (reuse Side Picker + ORBAT place) → writes into the working-copy scenario in WS1 shape. | WS1 + edit-mode slice-1 | M |
| **AUTH2** | **Draw a unit's path** (click waypoints) → `unit.kinematics.course`; MOVE1 then animates it. | WS1 + MOVE1 + AUTH1 | M |
| **AUTH3** | Place enemy + **Run**: hand the authored scenario to the playback clock so MOVE1/DET1/ENG1 advance it autonomously. | AUTH2 + MOVE1 + DET1 + ENG1 | M |

**📐 Realism rule (current build):** RMOOZ adds **mathematical rules** to feel as real as possible —
kinematics (MOVE1 ✅), detection (radar-horizon + RCS, DET1), engagement geometry (ENG1). Unit
class/state values feed these formulas. *"As real as possible"* is a standing goal for every rule PR.

**🔮 FUTURE (deferred — do NOT build unless requested):** **Personnel / maintenance / reliability /
controlled uncontrolled-events** — equipment can't operate by itself (crew availability, fatigue/shift,
maintenance status, reliability, supply, optional explainable malfunctions). These become **gates &
multipliers** on the math rules (reliability × Pd; crew gates firing; fatigue → +OODA; under-maintenance
= not taskable) and **citable constraints** in AI COA proposals. Controlled/explainable/optional —
never fake game randomness. Full schema + plug-in points: memory
[[project_personnel_maintenance_reliability_future]]. Targets: World State · DB-Lite · Readiness/Supply ·
Doctrine/ROE/AI-explanation.

**Phasing:** WS1→MOVE1→VIS1 finish presentation/movement (priority #1) on the new substrate · WS2–WS4
build the World State Engine (priority #2) · DB1–DB2 DB-Lite (#3) · DET1/ENG1 the CMO-style mechanics ·
DOC1 doctrine (#4) · TASK1 tasking (#5). Editable-workspace work ([[project_workspace_editable_owner_ruling]])
is parked. Each PR ships its own `test-*.js` and a re-verified W3 render.

---

## TODO — CMO→RMOOZ capability roadmap (chosen set, sourced from `docs/cmo-functional-rules/exhaustive/`)

> 🔒 **GUARDRAIL — build only what's on this chosen list.** The single source of truth for CMO behavior
> is **`docs/cmo-functional-rules/exhaustive/`** (945 caption-grounded rules, 9 buckets). The list below is
> the **explicitly chosen** subset RMOOZ will implement. **Do NOT invent functions or build CMO mechanics
> that are not on this list** — if a desired function isn't here, add it here first (citing the governing
> rule file), get it chosen, *then* build. Anything in the "Deliberately NOT building" list stays out.

Ordered by value÷effort, filtered through RMOOZ's ground-amphibious / AI-adjudication thesis. Per-rule
behavior (inputs/thresholds/formulas) lives in the exhaustive bucket specs cited per item.
**Buckets:** CORE = central to RMOOZ, ADJ = adjacent/clear value. **Note:** Wave 3 already shipped the
World-State/MOVE1/DET1/ENG1/DB1 engines + Edit Mode Slice 1; **Wave 4 completed Edit Mode Slice 2** (Geography,
Forces, stepped layout, New Scenario, map-as-editor, durable `POST /api/scenarios`) — items 1 & 3 below are now
**shipped inside Edit Mode**. The next chosen item is **DOC1: Doctrine/ROE/WRA** (item 4 below — highest-value
engine gap, makes AI proposals auditable).

- [ ] **1. Surface Sides + Posture cards** — CORE, XS. `sides`/`postures` are schema-ready + loader-defaulted but discarded at load; add two read-only cards.
- [ ] **2. Consolidate 3 clock surfaces → 1 + add explicit `scenario_clock` (start_utc/duration_hours)** — CORE, S. Closes a PARTIAL gap *and* a known 3× duplication.
- [ ] **3. Wire the P0 authoring schema into a guided "scenario readiness" mode** (CMO build-order) — CORE, M. Foundation shipped (`shell/scenario-authoring-schema.js`), just unwired.
- [ ] **4. Doctrine/ROE/WRA as read-only *data* the adjudicator cites** — CORE, M. Makes AI proposals auditable (core safety story).
- [ ] **5. Structured read-only "Missions/Tasking" view synthesized from step actors** — CORE, M. ATO-like rollup, not a planning engine.
- [ ] **6. Minimal unit "capability + readiness/supply" descriptors** — ADJ, S–M. Fills Unit Panel's empty Combat/C2 sections; feeds adjudicator.
- [ ] **7. Optional per-zone `environment` (weather/sea-state) overrides** — ADJ, M. Weather without a global physics model.
- [ ] **8. "Why did the adjudicator allow/deny this?" explainer panel** — ADJ, M. Borrows CMO's "why won't it fire" UX for AI-proposal review.
- [ ] **9. IADS/SAM coverage-envelope overlay (visual only)** — ADJ, M. The one in-domain idea from the sensors engine; no detection sim.
- [ ] **10. Declarative `events[]` rule object (data, previewable, no code)** — ADJ, M. In-bounds analogue of CMO's Event Editor; never Lua.

**Deliberately NOT building** (out-of-thesis, catalogued in the doc): detection/EW/IADS *simulation*, weapons/engagement firing math, naval/subsurface mechanics, airfield component model, mission-planning engine, Lua execution.

---

## Pointers to deep docs (don't restate — link)

- `docs/scenario-workspace-consolidation-map.md` — authoritative card/duplication inventory (~150 surfaces).
- `docs/read-only-surface-audit.md` — A=safety-by-design / B=not-wired / C=production classification.
- `docs/rmooz-roadmap.html` — visual program roadmap (done/build/blocked/plan).
- `docs/scenario-schema.md`, `docs/wargame3-schema.md`, `docs/scenario-template.json` — scenario JSON contracts.
- `docs/rmooz-authoring-foundation.md` — P0 Scenario Authoring foundation (schema / template / gap-fill / diagnostics / draft-safety guard; P0→P4 roadmap). Module unwired by design.
- `docs/scenario-animation-presentation-readiness-audit.md` — P0B audit: what animates per step (W3-rich), coverage gaps (non-W3 = markers+movement only), per-unit fidelity, static-units gap. Re-runnable via `node test-p0b-animation-readiness.js`.
- `docs/unit-symbol-fidelity-audit.md` — SYM1 audit: 117/153 W3 units render proper milsymbol glyphs, 36 fall back (milsymbol 2.0.0 rejects specific child SIDC entity codes); fix = role/domain→canonical-family remap (SYM2). Re-runnable via `node test-sym1-unit-symbol-fidelity.js`.
- `docs/pr-166-external-scenario-pack-audit.md` + `docs/scenario-pack-audit/` — CSP51 audit (630 scenarios; don't re-run).
- **`docs/cmo-functional-rules/exhaustive/`** — ⭐ **the single source of truth** for CMO behavior: 945 caption-grounded rules across 9 buckets, the chosen-function roadmap above sources from here. (Superseded + replaced the older `cmo-scenario-editor-application.md` and `cmo-vs-rmooz-capability-comparison.md`, now deleted.)
- `docs/cmo-functional-rules/5-build-playbook.md` + `sample-sahil-corridor.json` — worked "build a scenario the CMO way" example (validates `ok: true`); the target shape for the authoring editor.
- `docs/cmo-pgatcomb-playlist-inventory.md` — CMO tutorial playlist index (maps title→videoId→`cmo-captions/<id>.txt`).
- `docs/cmo-model-reference.md` — **companion** narrative of CMO's operating model from the **printed manual** (`CMO manual EBOOK.pdf`, 425pp): combat loop, sensors/weapons, doctrine/ROE/WRA/EMCON, the "why won't it fire" checklist + DLZ, unit/contact-report panel. Maps each finding onto the **chosen roadmap items 1–10 above** (does NOT replace `cmo-functional-rules/exhaustive/` as source of truth, nor add off-list functions). Use for grounding item 4/6/7/8/9.
- Cross-session "why" lives in the memory dir (`MEMORY.md` index) — this file is the "what", memory is the "why".
