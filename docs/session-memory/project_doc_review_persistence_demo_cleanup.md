---
name: project_doc_review_persistence_demo_cleanup
description: "RMOOZ-DOC-REVIEW-PERSISTENCE + FIX-A/B + STEP1 base-type + JSON-loss + Arabic-RTL + UTF8 mojibake — ALL LANDED ON main @ 52dfc6e (2026-06-13), NOT pushed; live-verified; server/client cache gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: be6a96a9-1bf6-4297-8c95-f319974a17e7
---

✅ **STATUS 2026-06-13 — ALL LANDED ON `main` @ `52dfc6e` (not pushed; `main` is +12 vs `origin/main`).** The branch work was integrated: rebased onto `af30d3c`, **merged → `d92549b`** into `integration/doc-review-persist-plus-json-loss` (3 conflict files resolved keeping both sides + Arabic), **+ UTF8 mojibake fix `52dfc6e`**, then `main` **fast-forwarded** `af30d3c → 52dfc6e` (no merge commit). `main` now carries: doc-review persistence / reviewed base anchors (FIX-A) · FIX-B import Red+Blue DOCX gating + Objective X sync + Unit Status `displayUnitName` fallback · STEP1 base-type symbols · **JSON import loss fix** · **Arabic-RTL fixes** · **UTF8 mojibake fix**. The `integration/...` branch was **deleted** after the FF (fully merged). `doc-review-persist-fix-a` @ `d3ca76f` is **kept** (post-rebase tip, NOT an ancestor of main; checked out in worktree `MAP_2_fixb`). `rescue-after-accidental-pull` @ `5f55f5f` kept pending owner OK. Tests green: utf8-mojibake 17/0 · arabic-rtl 23/0 · base-placement 16/0 · objective-unitstatus 27/0 · step1-base-type 26/0 · base-status 38/0 · json-loss 8/0. Live-verified Arabic renders correctly in-browser. Mojibake guardrail: [[feedback_arabic_utf8_guardrail]].

_History (superseded by the STATUS line above):_ 2026-06-13 **RMOOZ-DOC-REVIEW-PERSISTENCE-AND-DEMO-CLEANUP-A + IMPORT-UNITS-BASE-PLACEMENT-FIX-A** — done + live-verified, originally committed to branch **`doc-review-persist-fix-a` (3d97b1c)** off-main (private `GIT_INDEX_FILE`), then NOT-pushed/NOT-on-main until the integration above.

What it does (10 files):
- **Part A** demo overlays no longer stack — `free-fight-demo.js`/`demo-movement.js` `mount()` cross-clear the other; labels "Legacy Preview" / "Free Fight Preview Group" + "preview overlay only — not actual imported positions" legend; review_only/demo_only/exact_unit_position:false.
- **Part B / FIX-A** `base-status-panel.js` `baseIdMatches()` (assigned_base_id/base_id → anchor base_id/id/assigned_base/location_id) checked **before** name/coord; `doc-understanding-review.js` groups by base id when present.
- **Part C** `brief-to-scenario.js` persists `scenario.review_placement_candidates` (top-level + `generation.*` mirror) + `generation.review_proposed_units`; `scenario-workspace.js` fires `rmooz:live-scenario-loaded` on load; `placement-candidates-panel.js` `drawSavedReviewAnchors()` redraws them. Never draws proposed_units as exact markers.
- **Part D / FIX-A** draft units get `exact_unit_position:false` + `placement_source` (`reviewed_base_anchor` | `template_geometry_relative_to_objective`) + `draft_template_position`; `placeFromAnchors()` puts units AT reviewed anchors when `ob.placement_candidates` exist.
- New tests: `test-import-base-placement-a.js` (16/0), `test-demo-overlay-no-stack-a.js` (8/0). app.html cache bumps `?v=review-persist-a` on the 5 edited scripts.

**Why committed to a branch, not main:** the shared working tree had **107 files staged by another agent** (incl. app.html with their `preview-decision-window` hunk, entangled with my cache bumps; and the agent's uncommitted `querySelectorAll` render rewrite in `doc-understanding-review.js` interleaved with my 7-line grouping hunk). Moving `main` would create staged-reverts of my own files (index froze HEAD blobs) and risk losing the other agent's app.html work. Built the commit via a **private `GIT_INDEX_FILE` + `git commit-tree` + `git update-ref refs/heads/<branch>`** — main, the shared index, and the 107 staged files untouched. doc-review hunk reproduced cleanly on HEAD (9 lines, 0 querySelectorAll). [[feedback_shared_working_tree_concurrency]] [[feedback_controlled_bucketed_imports]]

**Two cache gotchas that hid the changes during live verify (both real, both fixed):**
1. **Server require-cache:** `brief-to-scenario.js` is server-side; the running verify server (`:8002` `rmooz-coalition-verify` = `node UI_MOdified/server/web-server.js`) had it cached from before my edits → generated scenarios used the OLD ring-placement and lacked review_placement_candidates. **Restart the API server** (preview_stop+preview_start `rmooz-coalition-verify`) after editing any server module.
2. **Client `?v=` cache:** edited client scripts must get their `?v=` bumped in app.html or the browser serves the stale file (`RmoozPlacementPanel.drawSavedReviewAnchors` was absent until I bumped `placement-candidates-panel.js?v=1`→`review-persist-a`).

**Live proof** (real `/api/wargame-sim/{analyze,placement,generate}` via auth-stub proxy `:8003`→`:8002`, GCC-vs-Iran Step 1 fixture, 7 countries 1 RED/6 BLUE): analyze `multi_country_step1` → 22 base anchors on map → per-base grouping correct (Bandar/Hamedan/Al-Udeid, no leakage; real coalition data has **no base_id**, so grouping is by name/coord — baseIdMatches is additive/non-interfering) → Demo→FreeFight cross-clear (demo markers 5→0, anchors persist 22) → Clear/Reset returns-to-anchors/removes, anchors remain → generate: RED 9/9 on Iran anchors, BLUE on GCC anchors, all `placement_source:reviewed_base_anchor` → bare refresh clears transient anchors (0) but reloading the saved scenario redraws 22. **Regression: zero from my changes** — every test delta vs HEAD (3252→3268 pass) is the other agent's `querySelectorAll` rewrite (a test-stub artifact only; real browser DOM has querySelectorAll, review UI renders fine).

**+ RMOOZ-IMPORT-OBJECTIVE-UNITSTATUS-FIX-B (2026-06-13, commit `154495c` on the SAME branch, on top of 3d97b1c):**
- (A) Import wizard `Review AI Understanding` gating: DOCX review now needs BOTH Red+Blue staged (was red||blue||mdmp → incomplete one-sided review); staged MDMP JSON is the only single-input path. Added explanatory text + dynamic amber one-DOCX hint (`#wg-wz-analyze-hint`). Legacy/advanced tools + DOCX inputs kept.
- (B) Objective X sync: `loadObjective()` labels default-vs-override (`#wg-wz-obj-source`) and pushes the effective objective to Free Fight/review (`window.__rmoozFreeFightObjective` + live `RmoozFreeFightDemo.setObjective` + `rmooz:objective-x-changed` event) so they can't silently diverge. Generate from Reviewed Brief already reads the same `el.objLon/objLat`.
- (C = prior item) anchor placement already in 3d97b1c.
- (D) `unit-status-panel.js` `displayUnitName(unit)` = label||name||name_en||name_ar||unit_name||callsign||code||uid||unit_uid||id||base_id||'—' (extended past the spec's `id` with `base_id` to satisfy the base_id test case); used at unit-label/unit-uid-fallback/img.alt/both fuel name fields; exposed on `AppUnitStatusPanel` for tests. Fixes wrong names for markers passing name/name_ar/unit_uid/base_id.
- Test `test-import-objective-unitstatus-b.js` 27/0; live-verified in the real app via a **worktree server** (`PORT=8010 node web-server.js` + main-tree proxy `PORT=8011 TARGET=:8010`, navigated the preview browser to :8011): unit-status names for every identity field, wizard wiring + hint, full objective-save sync. Zero regressions (wizard tests 18/0; test-unified-import-2-wizard 21/4 byte-identical to HEAD = pre-existing server-dependent).

**Concurrency learning (reinforces [[feedback_shared_working_tree_concurrency]]):** mid-session another agent **`git reset`/checkout the shared MAIN working tree**, wiping ALL my uncommitted FIX-A/Parts-A-E changes from the working tree (they survived only because committed to this branch). Lesson: never rely on the shared working tree persisting; commit to a branch and do follow-on work in a **dedicated worktree** (`MAP_2_fixb`). The committed verify server reports `docs:{red:true,blue:true}` from a persisted stage, and the wizard restores `st.red/st.blue` from `status.docs` (line ~1180) — so a fresh load shows review enabled when both are already staged (correct).

**+ STEP1-BASE-TYPE-SYMBOL-RESTORE-A (2026-06-13, commit `471d0ca` on the branch):** restored typed review base anchors (air ✈ / naval ⚓ / land ▣ / facility ⬢) without drawing bases as units. `multi-country-orbat.js` stamps explicit `base_type` on coalition candidates; `brief-to-scenario.js` `normalizeBaseType()` persists `base_type` (never null, unknown→base_facility) into review_placement_candidates + generation mirror; `placement-candidates-panel.js` makes the normalized base type AUTHORITATIVE for the marker class (`step1-anchor-<type>`) in every resolver branch (glyph still from RmoozSymbolRegistry); `base-status-panel.js` base_type row shows the human label. Test `test-step1-base-type-symbol-a.js` 26/0; live-verified on GCC fixture: 22 anchors → 8 air/7 naval/7 land, 0 unknown.

**GIT (2026-06-13): owner's accidental `git pull` was HARMLESS** — fetch-only; `ORIG_HEAD==HEAD==5f55f5f`, `diff ORIG_HEAD..HEAD` empty, nothing overwritten/merged/conflicted. Created snapshot branch `rescue-after-accidental-pull` @ 5f55f5f. Owner chose: skip stash (would've swept another agent's uncommitted doc-understanding-review.js), proceed with symbol task. **`main` has since advanced to `13cceef`** (another agent reconciled origin/main's 3 docs commits incl. `97ceb6d` = the START_HERE guide, + 2 "h" commits). 

✅ **RESOLVED 2026-06-13** — the branch work is integrated onto `main` @ **`52dfc6e`** (see the STATUS banner at the top; merge `d92549b`, mojibake `52dfc6e`, FF from `af30d3c`). `doc-review-persist-fix-a` @ **`d3ca76f`** (post-rebase: FIX-A `3c868e6` + FIX-B `8438c63` + STEP1 `7e4edcc` + 2 test-drift fixes) survives as the source branch in worktree `C:\Users\ADMIN\Desktop\MAP_2_fixb`. Its **content** is on main (the merge brought the pre-rebase equivalent `471d0ca`; the corrected tests were cp-synced into the merge), but its **commits are NOT ancestors of main**, so `git branch --merged main` won't list it and it isn't auto-deletable — retire on owner say-so. **NOT pushed** (`main` +12 vs `origin/main`). Part of [[project_doc_understanding_pipeline]]. Mojibake guardrail: [[feedback_arabic_utf8_guardrail]].
